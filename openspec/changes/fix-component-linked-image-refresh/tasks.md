# Tasks

- [ ] Update the canonical component data-model spec with linked name, image, and description refresh behavior.
- [ ] Add `CraftingSystemManager` name refresh to the linked component metadata sync path.
- [x] Register the refresh method with Foundry's `updateItem` hook after Fabricate is ready.
- [ ] Add focused unit tests for linked item name refresh.
- [ ] Update component documentation for automatic linked item name, image, and description refresh.
- [ ] Run targeted tests.
- [ ] Run required validation gates: `npm test` and `npm run build`.
