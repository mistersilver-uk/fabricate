# Tasks

- [x] Remove selected-record action strip and Source card from `RecordInspector.svelte`.
- [x] Remove now-unused right-inspector callback props and source-routing helper.
- [x] Remove unused inspector action/source CSS selectors.
- [x] Add/update source and mounted tests confirming the right inspector no longer renders these controls.
- [x] Run focused tests, `npm test`, `npm run build`, and `git diff --check`.
