# Tasks

- [x] Update `MatchingEvidenceChips.svelte` inspector variant to render a two-column evidence table.
- [x] Add CSS for the inspector evidence table and value pills.
- [x] Update source/mounted tests for table rows and pill values.
- [x] Run focused tests, `npm test`, `npm run build`, and `git diff --check`.
- [x] Refresh proposal/design for the compact CSS-only inspector table refinement.
- [x] Restyle the inspector evidence table rows and pills without changing evidence markup or semantics.
- [x] Re-run focused component tests, `npm run build`, and `git diff --check`.
- [x] Re-run `npm test` to green after unrelated `lang/en.json` localization fallback drift is resolved.
