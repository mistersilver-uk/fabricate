# Tasks

## Implementation

- [x] Add OpenSpec change documents.
- [x] Add `showInput` prop to `ImagePathPicker`.
- [x] Replace the environment image text input with a button-only `ImagePathPicker`.
- [x] Replace the scene dropdown and raw UUID input with a drag-drop zone.
- [x] Replace the enabled checkbox with the `manager-v2-status-toggle` button pattern.
- [x] Remove the right-hand evidence column.
- [x] Add a collapsible validation band above the task workspace.
- [x] Update workspace CSS to a two-column grid; add scene-drop-zone and validation-band styles.
- [x] Add new localization keys for scene drop, validation band, and toggle aria labels.

## Validation

- [x] Update manager-v2 contract, layout, and mounted tests for the new DOM.
- [x] Run `npm test`.
- [x] Run `npm run build`.
