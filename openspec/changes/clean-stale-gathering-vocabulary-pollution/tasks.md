# Tasks

- [x] Add `src/migration/migrateGatheringConfig.js` exporting a pure `migrateGatheringConfig(config)` that returns a deep-cloned config with `vocabularies.regions = []`, preserving everything else. Defensive for `null`, non-object, and missing `vocabularies`.
- [x] Extend `src/migration/MigrationRunner.js`:
  - Load `SETTING_KEYS.GATHERING_CONFIG` alongside recipes and craftingSystems.
  - Switch per-migration assignment from `data = result` to spread-merge `data = { ...data, ...result }` so legacy migrations that return only `{ recipes, systems }` preserve `gatheringConfig`.
  - Add a third JSON diff-check + persist block for `gatheringConfig`.
  - Register the 0.2.0 migration entry pointing at `migrateGatheringConfig`.
- [x] Relocate `scripts/foundry-test-run.mjs:2208-2253` writes:
  - Drop top-level `vocabularies` from the `gatheringConfig` payload.
  - Add `vocabularies: { regions: { values: ['northreach'] } }` under `systems[systemId]`.
- [x] Add `tests/migrate-gathering-config.test.js` covering the pure-function happy path, already-empty no-op, missing `vocabularies`, `null`/non-object input, and per-system vocabulary preservation.
- [x] Extend `tests/migration-runner.test.js`:
  - 0.2.0 clears stale top-level regions when non-empty.
  - 0.2.0 is a no-op (no `setSetting('gatheringConfig', ...)`) when regions are already empty.
  - 0.2.0 is a no-op when `gatheringConfig` has no `vocabularies` key at all.
  - 0.1.0 backward-compat: `gatheringConfig` survives the 0.1.0 step unchanged.
  - `null` `gatheringConfig` setting handled gracefully.
- [x] Fix admin-store `_normalizeGatheringConfig` to produce normalised top-level vocabularies (object records) for regions/biomes/weather/timeOfDay with seeded defaults; introduce `_seedGatheringVocabularyOptions` helper for symmetry with `_seedGatheringConditionOptions`.
- [x] Drop bare-string fallback lists from `EnvironmentsBrowserView.svelte` (lines 139, 144, 147, 150) so the fallback uses `[]` if the normalised store has no values, never bare strings.
- [x] Fix `_normalizeGatheringVocabularyOption` (admin store) and `normalizeVocabularyOption` (runtime) so a bare-string input does not short-circuit the auto-capitalised label.
- [x] Update existing adminStore tests whose expectations encoded the pre-fix behaviour (lowercase labels, bare-string top-level vocab).
- [x] Add two adminStore regressions: (a) top-level bare strings normalise to capitalised labels with per-biome colour tokens; (b) empty persisted config seeds the full default biome / weather / time-of-day vocabularies as objects.
- [x] `npm test` — 2647/2647 passing.
- [x] `npm run build` — clean.
