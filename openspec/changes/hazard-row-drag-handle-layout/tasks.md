# Tasks

- [x] Pass `selectedGatheringRules.hazardSelectionMode` from `CraftingSystemManagerRoot.svelte` into `EnvironmentEditView`.
- [x] Pass `hazardSelectionMode` from `EnvironmentEditView` through `EnvironmentHazardsTab` to `CompositionList`.
- [x] Gate included hazard drag handles, draggable behavior, and Move up/down actions on `highestRankedDrop`.
- [x] Remove blank hazard handle placeholders from available-to-add, excluded, and non-matching rows.
- [x] Update CSS so no-handle rows are the default and ranked included hazard rows opt into the handle grid.
- [x] Update mounted and source contract tests.
- [x] Run targeted tests, `npm test`, `npm run build`, and `git diff --check`.
