# Tasks

## OpenSpec And Review

- [ ] Run plan-review with `fabricate_domain_expert`, `fabricate_ux_designer`, and `fabricate_quality_engineer`.
- [ ] Revise `proposal.md`, `design.md`, and spec deltas until every plan reviewer approves.

## Step 1 — Persistence + migration + runtime (atomic)

- [ ] `git mv src/systems/gatheringRegions.js src/systems/gatheringRealms.js`. Rename exports `GATHERING_REGION_REVEAL_MODES`/`GATHERING_REGION_MODIFIER_VISIBILITIES`/`GATHERING_REGION_MODIFIER_KINDS`/`GATHERING_REGION_MODIFIER_OPERATIONS`→`GATHERING_REALM_*`; `normalizeGatheringRegion[List|Modifier|SceneMapping|Settings]`/`validateGatheringRegion[List|Modifiers|SceneMappings|Settings]`/`isGatheringRegionsEnabled`→`…Realm…`. KEEP modifier `kind`/`operation`/`visibility` VALUES (`eventChance`, `dropRate`, `add`, `gmOnly`, …), the `sceneMappings` field, and `sceneRegionUuid`/`sceneUuid` (`normalizeGatheringRealmSceneMapping` lines 86-92). The `region-${seq}` id fallback prefix (line 75) → `realm-${seq}`; default name `'New Region'` (line 146) → `'New Realm'`. NOTE the legacy-`hazardChance` read coercion (lines 111-114) stays as-is — it is an event-concept fallback unrelated to this rename.
- [ ] `git mv src/systems/GatheringRegionStore.js src/systems/GatheringRealmStore.js`. Class `GatheringRegionStore`→`GatheringRealmStore`; error `GatheringRegionValidationError`→`GatheringRealmValidationError`; persisted field `gatheringRegions`→`gatheringRealms` (lines 45, 172) + `gatheringRegionSettings`→`gatheringRealmSettings` (lines 59, 68). KEEP `sceneMappings`/`sceneRegionUuid` round-trip. The `currentRegionOverrides`/`override.regionIds` reference in `_collectReferences` (lines 154-155) and `includedRegionIds`/`excludedRegionIds` (lines 145-146) rename to the realm equivalents in lockstep with their owning stores.
- [ ] `git mv src/systems/gatheringRegionDiscovery.js src/systems/gatheringRealmDiscovery.js`. `getDiscoveredGatheringRegions`/`isGatheringRegionDiscovered`/`revealGatheringRegion`/`hideGatheringRegion`/`getDiscoveredRegionIdsForSystem`→`…Realm…`; inner `regionId`→`realmId`, `resolveRegionBelongsToSystem`→`resolveRealmBelongsToSystem`, `validation.regionBelongsToSystem`→`realmBelongsToSystem`, snapshot field `gatheringRegions`→`gatheringRealms` (line 60). KEEP entry members `sceneUuid`/`sceneRegionUuid` (lines 102-103). **Legacy-read fallback (design §3):** `DISCOVERY_FLAG_KEY` becomes `'discoveredGatheringRealms'` and the getter reads `getFabricateFlag(actor,'discoveredGatheringRealms', getFabricateFlag(actor,'discoveredGatheringRegions',{}))`; every write persists only the new key.
- [ ] `src/systems/GatheringLocationService.js`: `resolveCurrentRegions`→`resolveCurrentRealms`, `buildCurrentRegionContext`→`buildCurrentRealmContext`, `_getRegions`→`_getRealms`, `_regionsEnabled`→`_realmsEnabled`; returned `regions`/`regionIds`/`staleRegionIds`→`realms`/`realmIds`/`staleRealmIds`; party override read `currentRegionOverrides`/`override.regionIds`→`currentRealmOverrides`/`override.realmIds`; system field `gatheringRegions`→`gatheringRealms` (line 39). KEEP `sceneMappings`/`sceneRegionUuid` sensing (lines 117-118) and the injected `senseSceneRegions` collaborator name.
- [ ] `src/systems/GatheringPartyStore.js`: `currentRegionOverrides`→`currentRealmOverrides` (lines 160, 174, 237, 283, plus `_normalizeParty`/`normalizeOverrides`), `setCurrentRegionOverride`/`clearCurrentRegionOverride`→`…Realm…` (lines 155, 172), inner `regionIds`→`realmIds` (override payload + `normalizeOverrides` lines 302). Read fallback: accept `currentRealmOverrides ?? currentRegionOverrides` and inner `realmIds ?? regionIds`.
- [ ] `src/systems/gatheringLocation.js`: `buildRegionDisclosure`→`buildRealmDisclosure`; `UNDISCOVERED_PLACEHOLDER_KEY` value `FABRICATE.Gathering.Region.UndiscoveredPlaceholder`→`…Realm.UndiscoveredPlaceholder` (line 16); env reads `includedRegionIds`/`excludedRegionIds`→`includedRealmIds`/`excludedRealmIds` (lines 37-40, 53-54, 265); locals/params `regions`/`matchedRegionIds`/`excludedRegionIds`/`regionsById`/`discoveredRegionIds`/`GatheringRegionDisclosure` typedef → realm equivalents. **Reason-code/state STRING LITERALS — `gatheringLocation.js` is the PRODUCER of the `NO_CURRENT_REGION`→`NO_CURRENT_REALM` reason and the `noCurrentRegion`→`noCurrentRealm` disclosure state (rename in the SAME lockstep slice as the engine reason-code in Step 1 line 17, the `gatheringBlockedReasons.js`/`EnvironmentCard.svelte` consumers, and the line-70 producer tests — see design §Engine reason-code consumers):** line 78 `reasons: ['NO_CURRENT_REGION']`→`['NO_CURRENT_REALM']`; line 256 `result.reasons.includes('NO_CURRENT_REGION')`→`'NO_CURRENT_REALM'`; line 257 disclosure-state value `state: 'noCurrentRegion'`→`'noCurrentRealm'`, and the matching `:235` JSDoc return-union member `state: 'noCurrentRegion'|…`→`'noCurrentRealm'|…`. These align with the spec deltas already committing to `NO_CURRENT_REALM` (`specs/gathering-and-harvesting/spec.md:28`) and the `noCurrentRealm` guidance state (`:34`). Pinning tests in the same lockstep slice that go red otherwise: `tests/gathering-location-availability.test.js:32`, `tests/gathering-blocked-reasons.test.js:18,25,31,46`, `tests/gathering-engine-location-gating.test.js:104`, `tests/gathering-region-disclosure.test.js:52,63` (already in the Tests-section realm-token list).
- [ ] `src/systems/GatheringEnvironmentStore.js`: env fields `includedRegionIds`/`excludedRegionIds`→`includedRealmIds`/`excludedRealmIds` (lines 275-276) with read fallback (`?? data?.includedRegionIds`); validation block (lines 328-344) `gatheringRegions`→`gatheringRealms`, `regionIds`→`realmIds`, error strings `includedRegionIds references unknown region`→`includedRealmIds references unknown realm`. LEAVE `includedBiomeIds`/`excludedBiomeIds` (lines 277-278), `biome`/`biomes` (271-272), and the inert legacy `region` string (270). NOTE the hazard-era `forcedHazardIds` fallback (line 263) is unrelated; do not touch it.
- [ ] `src/systems/GatheringEngine.js`: `_currentRegionSummary`→`_currentRealmSummary` (line 1600), `_resolveRegionContext`→`_resolveRealmContext` (1501), `_regionRevealMode`→`_realmRevealMode` (1617), `_locationBlockedReasons` realm internals; `NO_CURRENT_REGION` reason key + i18n value `FABRICATE.Gathering.Blocked.NoCurrentRegion`→`…Realm` (line 38) — this reason CODE rename (`NO_CURRENT_REGION`→`NO_CURRENT_REALM`) is a cross-file lockstep: its two player-facing consumers (`gatheringBlockedReasons.js` `BLOCK_LABEL_KEYS` key + `EnvironmentCard.svelte` `reason?.code === 'NO_CURRENT_REGION'`) MUST rename in the same slice or the locked-card alert/label silently falls back. Rename `gatheringBlockedReasons.js:26` `BLOCK_LABEL_KEYS` key `NO_CURRENT_REGION`→`NO_CURRENT_REALM` here (engine-consumer lockstep); `EnvironmentCard.svelte` is in Step 3; `buildRegionDisclosure`→`buildRealmDisclosure` import (line 5) + call sites (1552, 1609); `getDiscoveredRegionIdsForSystem`→`getDiscoveredRealmIdsForSystem` (10) and `isGatheringRegionsEnabled`→`isGatheringRealmsEnabled` (11) imports; listing output `region`/`regionsEnabled`/`currentRegions`→`realm`/`realmsEnabled`/`currentRealms` (1126, 1140-1141, 1248, 1262-1263, 1275); `regionContextCache`→`realmContextCache`, `regionsById`/`discoveredRegionIds`/`revealMode`-region locals → realm. KEEP the `region` field copied from `environment.region` (lines 1126, 1248, 3602) — that is the inert legacy free-text string, not a Realm.
- [ ] `src/systems/CraftingSystemManager.js`: import `normalizeGatheringRegionList`/`normalizeGatheringRegionSettings` from `./gatheringRealms.js` (line 10) → realm names; persisted fields `gatheringRegions`/`gatheringRegionSettings`→`gatheringRealms`/`gatheringRealmSettings` (lines 119-123) with read fallback (`system.gatheringRealms ?? system.gatheringRegions`).
- [ ] `src/systems/CraftingSystemExporter.js` (lines 75-81): rename the validation read of `data.system.gatheringRegions`→`gatheringRealms` **with a legacy READ fallback** (`data.system.gatheringRealms ?? data.system.gatheringRegions`, per design §4) so pre-rename exports still validate; reword the emitted strings `'System "gatheringRegions" field must be an array'`→`'…"gatheringRealms"…'` and `'Gathering region at index ...'`→`'Gathering realm at index ...'`. Update `tests/crafting-system-exporter.test.js` realm tokens in lockstep (already in the Tests section list).
- [ ] Add `src/migration/migrateRenameGatheringRegionsToRealms.js` (pure, deep-cloning, idempotent; returns `{ systems, environments, gatheringParties }`): per system `gatheringRegions→gatheringRealms`, `gatheringRegionSettings→gatheringRealmSettings`; per env `includedRegionIds/excludedRegionIds→includedRealmIds/excludedRealmIds`; per party `currentRegionOverrides→currentRealmOverrides` + inner `regionIds→realmIds`. Leave `sceneMappings`/`sceneRegionUuid`/`sceneUuid` and modifier kinds untouched. Model on `migrateRenameGatheringHazardsToEvents.js` (`renameKey` guard helper).
- [ ] Register it in `MigrationRunner.MIGRATIONS` (`src/migration/MigrationRunner.js`) as version `1.1.0` (new highest), import the new module. **Ordering invariant (design §Migration ordering):** `1.1.0` MUST remain the highest version and MUST NOT be reordered before `1.0.0`, which still consumes the pre-rename `gatheringRegions` key (`migrateRenameGatheringHazardsToEvents.js:140` reads `system.gatheringRegions`; `0.9.0` unify writes it). Semver-sorted application guarantees this — do not renumber or reorder the pass. **Wire `gatheringParties` through the runner:** add `SETTING_KEYS.GATHERING_PARTIES` to the raw read (lines ~170-173), the original-JSON snapshot (175-178), `data` bundle (180-185), the changed-diff check (221-224), and the conditional persist (226-237) — the runner does NOT read `gatheringParties` today, so without this the party-override rename never persists. LEAVE `migrateUnifyGatheringRegions.js` and `_unifiedRegionSystems` untouched.
- [ ] **Migration tests:** update `tests/migrate-rename-gathering-hazards-to-events.test.js`:
  - line 6 `import { normalizeGatheringRegionModifier } from '../src/systems/gatheringRegions.js'` → `normalizeGatheringRealmModifier` from `../src/systems/gatheringRealms.js` (file git mv'd + export renamed in Step 1); line 23 fixture key `gatheringRegions:`→`gatheringRealms:`; line 119 `systems[0].gatheringRegions[0].modifiers`→`gatheringRealms[0].modifiers`; line 211 (`'1.0.0'`→`'1.1.0'`); line 216 (`gatheringRegions[0]`→`gatheringRealms[0]`); line 224 `normalizeGatheringRegionModifier(...)`→`normalizeGatheringRealmModifier(...)`.
  - **Stale comments/titles (post version-bump):** lines 194, 196-197 ("lands at 1.0.0" / "only the 1.0.0 migration pending") → update to 1.1.0 phrasing.
- [ ] **`tests/migrate-unify-gathering-regions.test.js`:** beyond the version bump at lines 233, 299 (`'1.0.0'`→`'1.1.0'`), the through-runner tests seed `migrationVersion:'0.8.0'`, so `1.1.0` now runs and rewrites keys — update the post-runner payload assertions: line 243 (`savedSystems[0].gatheringRegions[0].id`), 245 (`savedEnvs[0].includedRegionIds`), 267, 279, 281 → realm keys. **KEEP** lines 247-248 (`vocabularies.regions` / `'region' in tasks[0]`) — inert legacy vocab, not a Realm. **Stale comment:** line 223 ("0.9.0 and 1.0.0 migrations are pending") → update to include 1.1.0.
- [ ] Other migration-version bumps: `tests/migration-runner.test.js:285,353,467,496,568,612` (`'1.0.0'`→`'1.1.0'`); `tests/migrate-catalysts-to-tools.test.js:388,418` (`'1.0.0'`→`'1.1.0'`); `tests/migrate-tools-to-system.test.js:118,141` (`'1.0.0'`→`'1.1.0'`). Do NOT touch the module/fabricate version `'1.0.0'` in `tests/crafting-system-exporter.test.js`, `tests/release-build.test.js`, or `tests/stores/adminStore.test.js:1383,1417` (those are `getModuleVersion`, not `migrationVersion`).

## Step 2 — Public API (main.js) + surface test

- [ ] `src/main.js`: import from `./systems/gatheringRealms.js`/`GatheringRealmStore.js`/`gatheringRealmDiscovery.js` (lines 13, 16, 18). Instance field `gatheringRegionStore`→`gatheringRealmStore` (line 526). Rename methods `getGatheringRegionStore`→`getGatheringRealmStore` (736), `setGatheringPartyRegionOverride`→`setGatheringPartyRealmOverride` (780), `clearGatheringPartyRegionOverride`→`clearGatheringPartyRealmOverride` (796), `revealGatheringRegionForActor`→`revealGatheringRealmForActor` (812), `hideGatheringRegionForActor`→`hideGatheringRealmForActor` (836); `getGatheringLocationForActor` keeps its name but its internals + returned `regions`/`regionIds`/`staleRegionIds` keys → realm; `isGatheringRegionsEnabled`→`isGatheringRealmsEnabled` gate calls; `validateRegionInSystem`→`validateRealmInSystem` (825). `gathering.*` helpers (1304-1310): canonical `getRealmStore`/`setPartyRealmOverride`/`clearPartyRealmOverride`/`revealRealmForActor`/`hideRealmForActor`. `api.GatheringRegionStore`→`GatheringRealmStore` (1326). KEEP `sceneRegionUuidsContainingToken` import (41), `senseSceneRegions` collaborator (544-561), `registerInteractableRegionBehavior`/`RegionBehavior`/`RegionBehaviorConfig` (65, 1251-1276), `resolveRegion`/`fromUuidSync` (1511-1515), and `unifiedRegionSystems`/`UnifyRegions.Notice` (661-665).
- [ ] **Deprecated delegates (non-breaking, design §2):** add thin `*Region*` methods (`getGatheringRegionStore`, `setGatheringPartyRegionOverride`, `clearGatheringPartyRegionOverride`, `revealGatheringRegionForActor`, `hideGatheringRegionForActor`) + `gathering.*` aliases (`getRegionStore`/`setPartyRegionOverride`/`clearPartyRegionOverride`/`revealRegionForActor`/`hideRegionForActor`) that emit one console deprecation warn and forward to the new method. Keep `api.GatheringRegionStore` as an alias to `GatheringRealmStore`.
- [ ] **Realm API call sites (canonical, no delegate):** `src/ui/SvelteCraftingSystemManagerApp.svelte.js:99` `getGatheringRegionStore: () => game?.fabricate?.getGatheringRegionStore?.()` → `getGatheringRealmStore` (the canonical name). KEEP its Foundry-scene lines (12/14/101-121: `sceneRegions.js`, `regionHitTest.js`, `getCurrentSceneRegions`, `getActorUuidsInSceneRegion`, `sceneRegionUuid`). `scripts/foundry-test-run.mjs:2376` `game.fabricate.getGatheringRegionStore?.()` → `getGatheringRealmStore` (DECISION: switch to canonical so the smoke run does not trip the one-time deprecation warn).
- [ ] Update `tests/fabricate-api-surface.test.js` (pins every method by source-substring): add assertions for the new `*Realm*` names AND retain assertions for the deprecated `*Region*` delegates (lines 35-82); update the `isGatheringRealmsEnabled` import/gate string assertions (49-63) and `validateRealmInSystem` (69).

## Step 3 — Admin store + manager root

- [ ] `src/ui/svelte/stores/adminStore.js`: RENAME `selectedSystemRegions`→`selectedSystemRealms` (846, 2128, 5-listing), `getRegionStore`→`getRealmStore` (1966, 2284, 2318, 2332, 2377, 2446, 2360), region quick-list methods `createRegionQuick`/`renameRegion`/`toggleRegionEnabled`/`updateRegion`/`deleteRegion`→`…Realm…` (2283-2452, 5487-5492), `setGatheringRegionsEnabled`→`setGatheringRealmsEnabled` (2359, 5493), `setEnvironmentRegionMembership`→`setEnvironmentRealmMembership` (3972, 5390), override methods `setPartyRegionOverride`/`clearPartyRegionOverride`/`dropStaleOverrideRegion`→`…Realm…` (2260-2280, 5482-5486), `_regionPatch`/`_collectRegionReferences`→`…Realm…`, `gatheringRegionSettings`→`gatheringRealmSettings` (2145), `partiesInFabricateRegion`→`partiesInFabricateRealm` (2116-2117), draft field-merge list `'includedRegionIds'`→`'includedRealmIds'` (3555, 3591), env reads `includedRegionIds`/`excludedRegionIds`/`currentRegionOverrides`/`override.regionIds`→realm (2027-2047, 2077, 2429-2438); the `currentRegionEvidence` projection field (2047)→`currentRealmEvidence` (in lockstep with `CraftingSystemManagerRoot.svelte:4282,4284` and the pin in `tests/components/gathering-parties-tab.test.js:28,132`). KEEP the Foundry-side `currentSceneRegions`/`getCurrentSceneRegions`/`setMapRegionLink`/`partiesInMapRegion`/`linkBySceneRegionUuid`/`getActorUuidsInSceneRegion`/`sceneRegionUuid` (2085-2117, 2313-2347, 5330, 5491) and `dropStaleOverrideRegion`'s `sceneRegionUuid` reads.
- [ ] `src/ui/svelte/apps/manager/CraftingSystemManagerRoot.svelte`: rename selected-realm state, realm handlers/props, the `currentRegionEvidence` consumer (lines 4282, 4284)→`currentRealmEvidence` (in lockstep with adminStore.js:2047), and the imports of the renamed components. **Travel-tab sub-route token `'regions'`→`'realms'` (design §Travel-tab sub-route token):** `['parties','regions','map'].includes(tabId)` (line 45), and the `'regions'` token at lines 2928, 4336 → `'realms'`, in lockstep with persisted/deep-link active-tab state and any selecting test. Update `EnvironmentOverviewTab.svelte` (`data-environment-field="includedRegionIds"`→`"includedRealmIds"` line 169, live-realm `is-region`→`is-realm` line 186, the realm empty-state hook+class `data-environment-region-empty` + `.manager-environment-region-empty` line 173 → `data-environment-realm-empty` / `.manager-environment-realm-empty` (live realm surface, pinned by `tests/components/environment-overview-regions.test.js:84` and `environment-editor.test.js:185` — update those in lockstep), realm props), `GatheringTravelView.svelte` (realm state/handlers/props; **live Realm-override authoring hooks/CSS, RENAME — design §Live Realm `data-region-*` hooks, in lockstep with `tests/components/gathering-travel-view.test.js:199,278,310-350`:** `data-region-id` (510)→`data-realm-id`, `data-stale-region` (555)→`data-stale-realm`, `.manager-travel-region-chip`/`-chips`/`-chip-flag` (503/508/516)→`.manager-travel-realm-chip*` — these realm-only chip classes are DIFFERENT from the shared `.manager-travel-region-thumb`/`-item-name`/`-parties` KEEP classes in §B/Validation-Gates; do not conflate), `GatheringView.svelte` (realm-locked chip + props, AND the actor-bar producer call at line 281 `store?.setRegionContext({ enabled, regions })`→`setRealmContext({ enabled, realms })`, reading `selectedEnvironment?.realmsEnabled`/`selectedEnvironment?.currentRealms` in lockstep with the Step 1 engine listing-output rename). Update `tests/stores/adminStore.test.js`, `tests/stores/admin-store-environments.test.js`, `tests/stores/admin-store-travel.test.js` realm tokens in lockstep.
- [ ] `src/ui/svelte/apps/manager/EnvironmentsBrowserView.svelte` (large realm surface): rename props/handlers `gatheringRegionSettings`, `onSetGatheringRegionsEnabled`, `travelSystemRegions`, `travelSelectedRegionId`, `onSelectRegion`, `onAddEnvironmentToRegion`, `onRemoveEnvironmentFromRegion`, `onSetPartyRegionOverride`, `onClearPartyRegionOverride`, `onDropStaleOverrideRegion`, `onCreateRegionQuick`, `onRenameRegion`, `onToggleRegionEnabled`, `onUpdateRegion`, `onDeleteRegion` → `…Realm…`; `import GatheringRegionsTab` (line 14) → `GatheringRealmsTab` (this BREAKS the build on the Step 4 git mv unless updated in lockstep); `regionsEnabled` derived → `realmsEnabled`; env projection `includedRegionIds` (line 202) → `includedRealmIds`; test hook `data-gathering-region-toggle-panel` (line 912) → `data-gathering-realm-toggle-panel`; `Environment.RegionToggle.*` i18n refs → realm; `activeTravelTab === 'regions'` (line 881) → `'realms'`.
- [ ] `src/ui/svelte/apps/manager/GatheringPartiesTab.svelte`: rename props `systemRegions`/`onSetRegionOverride`/`onClearRegionOverride`→`systemRealms`/`onSetRealmOverride`/`onClearRealmOverride`; locals `party.overrideRegionIds`→`party.overrideRealmIds`, `chooseOverride(party, regionId)`→`(party, realmId)`; the `import RegionOverridePicker` + render (line 22/165, `regions={systemRegions}` line 167) → `RealmOverridePicker` with `realms={systemRealms}`; reword the region-named doc comment (lines 11-13).
- [ ] **`git mv src/ui/svelte/apps/manager/RegionOverridePicker.svelte → RealmOverridePicker.svelte`** (DECISION). It imports `SearchablePopover` only (NOT any of the 4 git-mv'd components). Rename its `regions` prop → `realms`, the `region`-named internals (`options` map var, `selectedName` lookup), and its `Travel.Parties.Override*` / `NoRegionMatches` / `'Unknown region'` i18n → realm copy. Update its `writeCompiledSvelte` consumers: `tests/components/manager-mounted.test.js:57` and `tests/components/gathering-parties-tab.test.js:74` → the new `RealmOverridePicker.svelte` path.
- [ ] `src/ui/svelte/apps/manager/GatheringTravelTabs.svelte`: imports NO components — its only edit is the `{ id: 'regions', icon, key: 'Regions', fallback: 'Regions' }` tab descriptor (line 14) → `{ id: 'realms', … key: 'Realms', fallback: 'Realms' }`, in lockstep with the i18n `Travel.Tabs.Regions`→`Realms` key (Step 5) and the route-token rename above. **Tab↔panel ARIA lockstep (design §Travel-tab sub-route token):** the button side already derives `id={`travel-tab-${tab.id}`}` (34) / `aria-controls={`travel-panel-${tab.id}`}` (37), so renaming the descriptor to `'realms'` makes the buttons emit `travel-tab-realms`/`travel-panel-realms`. Rename the hardcoded panel ids `id="travel-panel-regions"` (`GatheringRegionsTab.svelte:99`) → `travel-panel-realms` and `aria-labelledby="travel-tab-regions"` (`:101`) → `travel-tab-realms` in lockstep, or the tab↔panel ARIA relationship strands.
- [ ] **Actor-bar Realm-context source (design §Actor-bar Realm-context source):** rename the true-Realm actor-bar current-realm chip surface. `src/ui/svelte/stores/actorBarStore.svelte.js`: `regionContext` state (37)→`realmContext`, `setRegionContext` (147)→`setRealmContext`, `get regionContext()` (170-171)→`get realmContext()`, the `regions:` payload (37, 150)→`realms:` (read `next?.realms`), and the doc comments (32-36, 140-145) → realm. `src/ui/svelte/components/ActorSelectTopBar.svelte`: `showRegion` (87)→`showRealm` (`store?.realmContext?.enabled`), `regionNames` (88)→`realmNames` (`store?.realmContext?.realms`), `regionLabel` (95)→`realmLabel`, the `region` map param (90)→`realm`, `localize('FABRICATE.App.ActorBar.Region.None')` (96)→`…ActorBar.Realm.None`, the placeholder key `FABRICATE.Gathering.Region.UndiscoveredPlaceholder` (91)→`…Realm.…` (Step 1 renames that value), `.actor-bar-region` scoped class (264)→`.actor-bar-realm`, the comments (84-86). Update `tests/stores/actor-bar-store.test.js` (store side, already in Tests) and `tests/components/actor-select-top-bar-mounted.test.js` (consumer side, in Step 4) realm tokens in lockstep.
- [ ] **Engine reason-code consumer — `src/ui/svelte/apps/gathering/EnvironmentCard.svelte` (design §Engine reason-code consumers):** rename `reason?.code === 'NO_CURRENT_REGION'` (90)→`'NO_CURRENT_REALM'` (the Step 1 engine reason-code lockstep — silently breaks the locked-card header alert if it lags); `notInRegion` (83)→`notInRealm`; `regionAlertTitle` (88)→`realmAlertTitle`; the `RegionLockedChip` localize calls (92, 131)→`FABRICATE.App.Gathering.Environments.RealmLockedChip` (Step 5 i18n lockstep); scoped CSS `.gathering-env-card-region-alert` (461, render 129)/`.gathering-env-card-region-label` (475, render 131)→`…-realm-alert`/`…-realm-label`; the region-named comments (79-82, 457-459). KEEP the `'LOCATION_BLOCKED'` code (90) — separate, un-renamed engine reason.
- [ ] **`tests/components/gathering-parties-tab.test.js`** (Step 3/4 lockstep): pins `overrideRegionIds`, `currentRegionEvidence`, `systemRegions`, `onSetRegionOverride`, `onClearRegionOverride`, `staleRegionIds`, `.manager-travel-parties-current-region`, and `writeCompiledSvelte('…/RegionOverridePicker.svelte')` (line 74) → realm tokens + the renamed `RealmOverridePicker.svelte` path.

## Step 4 — Svelte components (git mv) + test-hooks + CSS

- [ ] `git mv` 4 components, update every import + `writeCompiledSvelte` path in mounted tests: `GatheringRegionQuickList.svelte`→`GatheringRealmQuickList.svelte`; `GatheringRegionsTab.svelte`→`GatheringRealmsTab.svelte`; `RegionNameField.svelte`→`RealmNameField.svelte`; `RegionEnvironmentsEditor.svelte`→`RealmEnvironmentsEditor.svelte`. KEEP `GatheringMapLinksTab.svelte` and `MapRegionLinkPicker.svelte` (Foundry Map Region Links surface) — reword only their Fabricate-destination copy.
- [ ] Rename test hooks `data-manager-region-editor`/`-env-editor`/`-name-field`→`data-manager-realm-*` (GatheringRegionsTab 160, RegionEnvironmentsEditor 66, RegionNameField 48). **Per-row Realm data-hooks on the git-mv'd components (design §Live Realm `data-region-*` hooks — DISTINCT from the `-editor`/`-env-editor`/`-name-field` hooks above):** in `GatheringRealmQuickList.svelte` (formerly GatheringRegionQuickList) `data-region-id` (139)→`data-realm-id`, `data-region-select` (144)→`data-realm-select`, `data-region-detail` (170)→`data-realm-detail`, `data-region-field` (186/202/222/238/244)→`data-realm-field`; in `RealmEnvironmentsEditor.svelte` (formerly RegionEnvironmentsEditor) `data-region-env-column` (67/118)→`data-realm-env-column` + its `.manager-region-env-column` class → `.manager-realm-env-column`; in `GatheringRealmsTab.svelte` (formerly GatheringRegionsTab) `data-manager-travel-region-id` (129)→`data-manager-travel-realm-id`; in `GatheringTravelView.svelte` (in-place, Step 3) `data-region-id` (510)→`data-realm-id`, `data-stale-region` (555)→`data-stale-realm`, `.manager-travel-region-chip`/`-chips`/`-chip-flag` (503/508/516)→`.manager-travel-realm-chip*` (realm-only chips — NOT the shared `-thumb`/`-item-name`/`-parties` KEEP classes). `data-gathering-region-toggle-panel`→`data-gathering-realm-toggle-panel` (EnvironmentsBrowserView 912); the live-realm `is-region`→`is-realm` (`environment/EnvironmentOverviewTab.svelte` 186, fabricate.css **2340/2344** only); `data-environment-field="includedRegionIds"`→`"includedRealmIds"`. **KEEP (do NOT rename):** the inert-legacy `is-region` chip in `gathering/GatheringEventDetail.svelte:192` (iterates the inert legacy `event.regions[]` composition-tag array — `regions` derived at line 71 = `event?.regions` — same inert-legacy `region` family as the `environment.region` free-text, not a Realm; see Step 1 line 17 + data-models spec), and the dead legacy-vocab `.manager-region-add` (fabricate.css 6965) / `.manager-vocabulary-pill.is-region` (7009) — keep as legacy-vocab CSS, do not rename or delete (their pinning test `tests/components/manager-layout.test.js:438,441,442` is KEEP-verifying). KEEP `data-manager-map-region-uuid` (GatheringMapLinksTab 59). Rename the live `.*-region-*` CSS scoped to the renamed realm surfaces (`.manager-region-*`, `.manager-travel-regions`, `.manager-region-env-column`→`.manager-realm-env-column`, the realm-only `.manager-travel-region-chip*`→`.manager-travel-realm-chip*` chip classes in `styles/fabricate.css`); **KEEP** `.manager-travel-map-*` / scene-region CSS, the legacy-vocab CSS at 6965/7009, AND the shared travel-inspector / Map-Links scene-region structural classes `.manager-travel-region-thumb`/`-item-name`/`-parties` + `.manager-map-link-region-card` (10046, 10230-10272) per design §Shared travel CSS classes (Validation-Gates keep-list) — these straddle the Realm + KEEP Map-Links surfaces and must NOT be conflated with the realm-only `-chip*` classes above.
- [ ] `git mv` + update component tests: `tests/components/gathering-regions-tab.test.js`, `tests/components/region-environments-editor.test.js`, `tests/components/gathering-region-toggle.test.js`, `tests/components/environment-overview-regions.test.js`, `tests/components/gathering-travel-view.test.js`, `tests/components/environment-editor.test.js`, and the mounted/source tests that `writeCompiledSvelte` the renamed files or pin realm tokens via `inspectorSource.includes(...)`: `tests/components/manager-mounted.test.js` (incl. the `RegionOverridePicker.svelte` writeCompiledSvelte at line 57 → `RealmOverridePicker.svelte`), `tests/components/gathering-environments-mounted.test.js`, `tests/components/gathering-environments-source.test.js`, `tests/components/gathering-detail-mounted.test.js`, `tests/components/gathering-view-actor-bar.test.js`, `tests/components/manager-contract.test.js`. **`tests/components/actor-select-top-bar-mounted.test.js`** pins `regionContext` (lines 58, 312, 328, 342, 358)→`realmContext`, `.actor-bar-region` (304, 316, 332, 349, 362)→`.actor-bar-realm`, `FABRICATE.App.ActorBar.Region.None` (334)→`…ActorBar.Realm.None`, `FABRICATE.Gathering.Region.UndiscoveredPlaceholder` (344, 350)→`…Realm.…`, and the `regionContext.regions` shape→`realmContext.realms` → rename realm tokens in lockstep with the Step 3 actor-bar source rename (`actorBarStore.svelte.js` + `ActorSelectTopBar.svelte`); the store side is `tests/stores/actor-bar-store.test.js`, already in the Tests section. KEEP `tests/components/gathering-map-links-tab.test.js` / `map-region-link-picker.test.js` token shapes except the reworded Fabricate-destination copy.
- [ ] Screenshot id `player-gathering-region-locked`→`player-gathering-realm-locked`: update `scripts/foundry-test-run.mjs:2835`, `tests/ui-pr-screenshot-evidence.test.js:43,49,51`, and **`scripts/ui-pr-screenshot-evidence.mjs`** — line 80 regex `/^src\/ui\/svelte\/apps\/manager\/GatheringRegionQuickList\.svelte$/` (stale after the Step 4 git mv → `GatheringRealmQuickList\.svelte`), and lines 96-99 `id`/`label`/`smokeLabels` `'player-gathering-region-locked'`→`'…realm-locked'` (and the `'region-locked environment'` label copy → realm). Ensure the publish/clean step removes the orphaned `*-region-locked` id.

## Step 5 — i18n keys + values + Map-Links per-string split

- [ ] `lang/en.json`: rename the gathering-scoped Region keys/values to Realm — `Admin.Manager.Travel.Regions.*` (2136-2181), `Travel.Tabs.Regions`/`RegionsPlaceholder`/`Inspector.RegionsPlaceholder` (1988-1996), `Travel.NoCurrentRegion`/`OverrideLabel`/`RegionSelectLabel`/`CreateRegion`/`DefaultRegionName`/`StaleRegionLabel` (2024-2181), and the enumerated `Travel.Parties.*` region values (review each VALUE for "region"→"realm"): `NoCurrentRegion` (2024), `OverrideStale` "Unknown region" (2030), `OverrideSearchPlaceholder` "Search regions..." (2031), `OverrideSearchLabel` "Search regions" (2032), `NoRegionMatches` "No regions match your search." (2033), `WorldScopeNote` "…current-region override…" (2065), `DeletePartyContent` "…current-region overrides…" (2085), `RegionSelectLabel` "Override regions" (2106), `DisabledRegionChip` (2109), `EvidenceLabel` "Current region" (2110), `EvidenceSourceUnresolved` "No current region" (2113), `EvidenceNoRegions` "No current region set…" (2115), `RemoveStaleRegion` "Remove stale override region {id}" (2122), `ChecklistStep1` "Create at least one region." (2124), `ChecklistStep5` "Set the party's current region." (2128), `InspectorNoPartySelected` "…current-region evidence." (2132) — KEEP `EvidenceSourceManualOverride`/`EvidenceSourceTravelActor` (no region token); `GatheringRegions`-scoped keys, `App.ActorBar.Region.{Label,None}` (293-296 — reword BOTH `Label` "Region"→"Realm" and `None` "No region selected"→"No realm selected"; consumed by `ActorSelectTopBar.svelte` as `FABRICATE.App.ActorBar.Realm.{Label,None}`), `App.Gathering.Environments.RegionLockedChip` (306 → `RealmLockedChip` "Not in current region"→"Not in current realm"; consumed by `EnvironmentCard.svelte`), `EventRegions` (359), `NoRegion` (419, 2789), `RegionFilter`/`AllRegions` (2767-2772), `EnvironmentRegion` (1609), `Composition.MatchRegion` (1629), `EnvironmentComposition.Regions.*` (1769-1774), `Blocked.NoCurrentRegion`/`Region.UndiscoveredPlaceholder`/`Travel.Guidance.*` (2903-2915), `RegionToggle` (1135-1139). Reword surviving danger/region phrasing to Realm.
- [ ] **Map Region Links per-string split (design §boundary):** in `Travel.MapLinks.*` (1999-2016), KEEP key names + map-side copy (`PartiesInMapRegionTitle`, `NoPartiesInMapRegion`, `InspectorKicker` "Selected map region", `Tabs.MapLinks` "Map Region Links", `NoScene`/`NoRegions`/`UnnamedRegion`); RENAME the Fabricate-destination copy to Realm (`LinkSectionTitle`/`LinkLabel` "Linked Fabricate region"→"…realm" (2004, 2010), `PartiesInFabricateRegionTitle`/`NoPartiesInFabricateRegion`/`NotLinked` (2006, 2008, 2009)). **`MapRegionLinkPicker` destination-picker VALUES (2011-2016, design §Map Region Links split — DECISION: reword to Realm; key names stay):** `None` "Not linked" (2011, review), `Stale` "Unknown region"→"Unknown realm" (2013), `SearchPlaceholder` "Search regions..."→"Search realms..." (2014), `SearchLabel` "Search regions"→"Search realms" (2015), `NoMatches` "No regions match your search."→"No realms match your search." (2016) — these list Fabricate realms to link a scene region to. KEEP `DisabledSuffix` (2012). KEEP `Canvas.Interactable.Region.Label` (294-ish under Canvas), `PlaceRegionOnly`/`RegionOnlyHint`/`JumpToRegion` (98, 132-133).
- [ ] `FABRICATE.Migration.UnifyRegions.Notice` (11-12): KEEP the **key** name and reword only the **value** copy to "Realm" (the key mirrors the shipped 0.9.0 `migrateUnifyGatheringRegions` history that is kept). Update the `main.js:664-665` fallback string to match.

## Step 6 — Docs + specs

- [ ] Update `DOMAIN.md` ubiquitous-language rows: **Gathering Region** (line 32)→**Gathering Realm**, **Gathering Region Settings** (35), **Current Region Resolution** (36), **Environment Location Availability** (37), **Region Disclosure / Travel Guidance** (38), **Discovered Gathering Regions** (39); the `gatheringRegions`/`gatheringRegionSettings`/`includedRegionIds` data-shape lines (149-167); and the **Gathering Task** row's "Region is NOT a composition axis" mention (27). KEEP the **Canvas Interactable** / **Linked Visual** / activation rows (51-57) — those are Foundry Scene Regions.
- [ ] Update `docs/gathering-regions.md` (rename file → `docs/gathering-realms.md` and its internal vocabulary; check Jekyll nav/links), `docs/gathering-environments.md`, `docs/agents/gathering-environment-data-model.md`. KEEP `docs/agents/travel-current-region-sensing.md`'s Foundry-Region content but reword its Fabricate-realm references; KEEP `AGENTS.md:101,121` travel-region-sensing pointer text where it describes Foundry membership (`TokenDocument#regions`), reword the "Fabricate region" phrasing to Realm.
- [ ] Promote spec deltas into canonical `openspec/specs/{data-models,gathering-and-harvesting,ui-integration}/spec.md` (the GatheringRegion / location-availability / current-region-resolution sections).

## Tests

- [ ] NEW `tests/migrate-rename-gathering-regions-to-realms.test.js` (top-level dir — confirmed gated by the `package.json` test glob, same as `tests/migrate-unify-gathering-regions.test.js`). Assertions: pre-1.1.0 fixture (`gatheringRegions`/`gatheringRegionSettings`/`includedRegionIds`/`excludedRegionIds`/`currentRegionOverrides`{`regionIds`}) → `gatheringRealms`/`gatheringRealmSettings`/`includedRealmIds`/`excludedRealmIds`/`currentRealmOverrides`{`realmIds`}; **idempotent** second run is a no-op; **keep-list preserved** (`sceneMappings`/`sceneRegionUuid`/`sceneUuid` and modifier `kind:'eventChance'` unchanged); **partial payload** (`gatheringRealms` already present + stale `gatheringRegions` → not clobbered, stale left inert); **mixed old/new keys** migrate independently; **through-the-runner** run from <1.1.0 lands at `migrationVersion==='1.1.0'` and persists the party-override rewrite (proves the new `gatheringParties` runner wiring). Confirm `npm test` total count rises.
- [ ] Legacy-read-fallback units: `gatheringRealmDiscovery` getter reads an old `discoveredGatheringRegions` flag and writes the new `discoveredGatheringRealms` key; `GatheringEnvironmentStore` accepts `includedRegionIds` on import → `includedRealmIds`; `GatheringPartyStore` accepts `currentRegionOverrides`/`regionIds` on import → realm; `CraftingSystemManager` accepts `gatheringRegions`/`gatheringRegionSettings` on import.
- [ ] Update the realm-token tests in lockstep: `tests/gathering-region-store.test.js`→`tests/gathering-realm-store.test.js` (git mv), `tests/gathering-region-normalization.test.js`, `tests/gathering-region-disclosure.test.js`, `tests/gathering-region-discovery-flags.test.js`, `tests/gathering-location-resolver.test.js`, `tests/gathering-location-availability.test.js`, `tests/gathering-engine-location-gating.test.js`, `tests/gathering-blocked-reasons.test.js`, `tests/gathering-party-store.test.js`, `tests/gathering-environment-store.test.js`, `tests/gathering-environment-composition.test.js`, `tests/gathering-bootstrap-api.test.js`, `tests/gathering-scene-access.test.js`, `tests/crafting-system-exporter.test.js`, `tests/stores/actor-bar-store.test.js`, `tests/helpers/gathering.js`. KEEP the Foundry-Region tests under `tests/canvas/` and `tests/scene-regions.test.js`, and the `region: 'eu-west-2'` AWS config in `tests/ui-pr-screenshot-evidence.test.js`.
- [ ] Smoke harness (`scripts/foundry-test-run.mjs`): update the `includedRegionIds: [hiddenVale.id]` fixture (line 2428) and any `gatheringRegions`/`gatheringRegionSettings`/`currentRegionOverrides` seeds → realm keys (these go through the runtime normalizer's legacy fallback, not the migration); the `game.fabricate.getGatheringRegionStore?.()` call (line 2376) → `getGatheringRealmStore` (canonical, so the smoke run does not trip the deprecation warn — also covered in Step 2); and the `player-gathering-region-locked` screenshot id (2835). KEEP `senseSceneRegions`/scene-region selectors.

## Validation Gates

- [ ] `npm test` (total count rises).
- [ ] `npm run build`.
- [ ] Grep sweep over `src/`, `tests/`, `lang/`, `styles/`, `docs/`, `openspec/specs/`: no `Region`/`region` tokens for the Fabricate gathering concept outside the keep-list. **Keep-list (must STAY `region`):**
  - Foundry scene region: `sceneMappings`/`sceneRegionUuid`/`sceneUuid`; `src/canvas/regions/**`/`RegionDocument`/`RegionBehavior`/`senseSceneRegions`/`sceneRegionUuidsContainingToken`; the Map Region Links tab key names + `data-manager-map-region-uuid`; `Canvas.Interactable.Region`/`PlaceRegionOnly`/`RegionOnlyHint`/`JumpToRegion`/`PartiesInMapRegionTitle`.
  - **Shared travel-inspector / Map-Links scene-region structural CSS classes (design §Shared travel CSS classes — DECISION: KEEP):** `.manager-travel-region-thumb` — shared travel-inspector / Map-Links scene-region structural class — kept; `.manager-travel-region-item-name` — shared travel-inspector / Map-Links scene-region structural class — kept; `.manager-travel-region-parties` — shared travel-inspector / Map-Links scene-region structural class — kept; `.manager-map-link-region-card` (`styles/fabricate.css:10046`, render `CraftingSystemManagerRoot.svelte:4408`) — Foundry-scene Map-Links card structural class — kept. **These are DISTINCT from the realm-only `.manager-travel-region-chip`/`-chips`/`-chip-flag` classes (`GatheringTravelView.svelte`), which RENAME → `.manager-travel-realm-chip*` (Step 4 / design §Live Realm `data-region-*` hooks) — do not conflate.**
  - Shipped history: `migrateUnifyGatheringRegions`/`_unifiedRegionSystems`/`UnifyRegions.Notice` key; archived `openspec/changes/*-regions/`.
  - Cloud: AWS `region: 'eu-west-2'`.
  - **Inert-legacy `region`/`vocabularies.regions`/`environment.region` free-text family** (kept verbatim — not a Realm; see design §Boundary corrections): `GatheringEventDetail.svelte`'s `{region}` chip + `data-evidence-field="region"`; the legacy-vocab CSS `.manager-region-add` (6965) / `.manager-vocabulary-pill.is-region` (7009); `tests/gathering-match.test.js:7` (`region:'north'`); `tests/gathering-rich-library.test.js`, `tests/gathering-engine-listing.test.js` (legacy `regions` composition tag); `tests/migrate-gathering-config.test.js` (`vocabularies.regions`); `tests/migrate-unify-gathering-regions.test.js:247-248` (`vocabularies.regions` / `'region' in tasks[0]`); `tests/components/manager-layout.test.js:1445,1545` (Region evidence row) and `:438,441,442` (legacy-vocab `.manager-region-add` / `.manager-vocabulary-pill.is-region` CSS pins).
  Run the same sweep over `tests/` to catch missed lockstep updates.
- [ ] UI-changing slice: screenshot planning → local Foundry smoke capture → publish → clean per `docs/agents/ui-pr-screenshots.md` (Travel realm list, environment realm selector, player realm-locked card).
- [ ] Run implementation review with reviewer, UX, and quality coverage.

## Docs Loop

- [ ] Run the paired `fabricate_domain_expert` / `fabricate_docs_writer` loop over the diff until both approve.
