# Tasks

- [x] Update the store reorder path so hazard IDs come from all visible included hazard composition states, not runtime availability.
- [x] Keep task reorder behavior unchanged.
- [x] Clarify the canonical gathering spec for force-included hazard ordering under highest-ranked drops.
- [x] Add store regression tests for reordering matching plus force-included hazards, including the last included hazard.
- [x] Add mounted UI coverage for force-included hazard rank handles in highest-ranked mode and hidden controls in non-highest-ranked editor modes.
- [x] Add runtime composition coverage for `hazardOrder` sorting matching and force-included hazards together.
- [x] Run focused tests, `npm test`, `npm run build`, and `git diff --check`.
