# Agentic Workflow Delta

## ADDED Requirements

### Requirement: Canonical OpenSpec layout

Fabricate MUST use `openspec/specs/` for durable specifications and `openspec/changes/` for per-change planning.

### Requirement: Shared skill reuse

Fabricate MUST keep shared skills in the repository `skills/` directory and point provider-specific discovery paths back to that directory.
