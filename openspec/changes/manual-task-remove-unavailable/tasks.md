# Tasks

- [x] Update `excludeEnvironmentRecord` so manual task removal clears include/force state without adding local exclusion.
- [x] Extend `excludeEnvironmentRecord` so manual hazard removal clears include/force state without adding local exclusion.
- [x] Update `CompositionList.svelte` manual hazard layout, quick action, and menu copy to match manual tasks.
- [x] Update store/runtime tests for manual hazard removal, stale manual disabled hazard IDs, and automatic hazard exclusion.
- [x] Update mounted/source component tests for manual hazard removal copy and Available-to-add classification.
- [x] Update canonical gathering composition spec language.
- [ ] Run focused component/store tests.
- [ ] Run `npm test`, `npm run build`, and `git diff --check`.
