{
  "designSystemPrimitives": [
    {
      "path": "src/ui/svelte/apps/manager/ArmedDangerButton.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "The two-step arm-then-confirm destructive control (4 callers). `library.html:581-582` specifies `danger` as one of six ROLES on the single button primitive and `library.html:1815` records a bespoke destructive panel as a surface Foundry already owns, so the arm is a carve-out neither entry claims by name. It is a hand-written carrier of `ManagerButton`'s CSS contract rather than a caller of it, and since issue 1502 that contract is rooted at the class the primitive emits, so this file writes `class=\"fabricate-button manager-button is-danger\"` — the root token, first, with no `fab-manager-button`. The deferral is unchanged and still count-pinned; only the class list moved."
    },
    {
      "path": "src/ui/svelte/apps/manager/BulkDeleteCard.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "why": "The shared bulk-DELETE card (3 callers, issue 1132). `library.html:1058` specifies `<SelectionBar>` and `<BulkEditPanel>` for this surface and names no delete card. Targeted because it renders in all three studios but is photographed only by the four `*-bulk-delete-*` frames, which `ui-pr-screenshot-evidence.mjs` routes it to; the bulk-EDIT frames do not contain it."
    },
    {
      "path": "src/ui/svelte/apps/manager/BulkEditPanelShell.svelte",
      "library": "<BulkEditPanel>",
      "status": "target",
      "evidence": "targeted",
      "why": "The bulk panel shell (3 callers, issue 1010). `library.html:1058` names it and calls this \"the largest uncovered surface in the corpus\". Targeted: exactly two studios consume it and both claim it by `sourceMatches`."
    },
    {
      "path": "src/ui/svelte/apps/manager/BulkEditSection.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "why": "A section inside the bulk panel (3 callers, issue 1010). `library.html:1058` specifies the PANEL, not its internal sectioning. Targeted for the same reason as its shell."
    },
    {
      "path": "src/ui/svelte/apps/manager/BulkEditSelect.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "why": "The bulk panel's leave-unchanged select (2 callers, issue 1010; converted at issue 1504). `library.html:665` specifies `<Select>` as one shell shared with text, number and search, and records the leave-unchanged clear state on `<TintPicker>` rather than on the field shell. Targeted for the same reason as its shell. IT IS A `<Select>` NOW AND ITS OPTIONS ARE DATA. It was a native `<select>` with its own scoped block, and the one thing that block insisted on — an OPAQUE `--fab-bg-1` fill — existed because a browser derives an OS drop-down's surface from the control's computed background, so a translucent tint opened light against every other dark manager list. That reason is RESOLVED rather than overridden: the list is the app's own markup now, no fill of this control's decides what colour a popup opens at, and the shared `form` rung's `--fab-surface-soft` is free to be the right answer. Two recorded debts left with the block, a retired 32px control height and an off-ladder 8px corner. The OPTIONS are still the caller's, for the reason this component has always given — the sentinel's meaning differs per studio and per axis — but their FORM changed: `Select` takes an `options` array, a snippet of arbitrary `<option>` elements cannot be turned into one without parsing rendered DOM, so the `children` prop is retired and each caller builds the array where it already decided what the sentinel means. `showTick` is FORWARDED rather than fixed, because the judgement belongs to the list and this wrapper has three across two studios. It keeps its own file only because its width is: the panel stages full-width fields in a 300px rail, `Select` exposes no trigger-box prop by design, and the class this component puts on the picker root is what a descendant rule reaches that box by."
    },
    {
      "path": "src/ui/svelte/apps/manager/BulkSelectionToolbar.svelte",
      "library": "<SelectionBar>",
      "status": "target",
      "evidence": "targeted",
      "why": "The bulk selection toolbar (3 callers, issue 1010). Named at `library.html:1058`. Targeted for the same reason as its shell."
    },
    {
      "path": "src/ui/svelte/apps/manager/Callout.svelte",
      "library": "<Callout>",
      "status": "shipped",
      "evidence": "broad",
      "why": "Named at `library.html:938`, and `shipped` as of issue 1505, which wrote the Svelte API that entry had for `<Notice>` alone — the missing API section was this row's whole stated reason for `target`, and it is now `library.html:962`. THREE FACTS IN THE SENTENCE THIS REPLACES WERE STALE AND ARE CORRECTED TOGETHER. It said `library.html:911`, which is neither the specimen nor its heading. It said \"7 callers across the manager\": measured by `componentImporters.js` at this head, 14 FILES import it over 19 call sites, and one of them — `apps/inventory/detail/InventorySalvagePanel.svelte` — is in the PLAYER window, so \"across the manager\" is false as well as the count. And it said \"so no frame is a non-arbitrary choice\", which issue 1505 falsified by CHOOSING one: it carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry naming the salvage frames that draw the widened component, and it left `PRIMITIVES_WITH_NO_FRAME` in the same change, which is the direction that list is meant to move. `evidence` stays `broad`: `primitivePathsByEvidence` filters on `evidence` alone and never on `status`, so the `target` → `shipped` flip cannot widen `MANAGER_PRIMITIVES` or move the pinned `BROAD_SIGNAL_PATTERN` source. THE WIDENING IS WHAT MAKES `shipped` HONEST rather than merely recorded: the component converged onto the specimen's own geometry — r11, `--fab-surface-soft`, `--fab-border`, 11.5px/1.6 `--fab-text-muted`, a 13px `--fab-text-subtle` glyph, `align-items: flex-start` — and its default tone is `neutral`, so the info tint is opt-in and reached only when the note is about live state. It gained a `title` borrowing the notice title's treatment whole, an `actions` snippet, and the `accent`/`success`/`danger` tones its callers derive. Two caller overrides deleted with it, the `[data-failure-salvage-note]` block and `.manager-recipe-info-strip`, and the docblock that called its old geometry \"the one already approved visually\" was rewritten rather than left to contradict the shipped component."
    },
    {
      "path": "src/ui/svelte/apps/manager/Chip.svelte",
      "library": "<IconChip>",
      "status": "target",
      "evidence": "broad",
      "why": "The highest-traffic primitive in the codebase at 67 callers. `library.html:858` groups `<Kicker> <IconChip> <StatBox>` as the mark vocabulary and this is the icon chip. Broad by an enormous margin — attributing a chip change to one window would be a lie about where it lands."
    },
    {
      "path": "src/ui/svelte/apps/manager/ComplicationSummaryRow.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "why": "The complication summary row (5 callers, reaching the player crafting and inventory apps as well as the component editor). `library.html:1429` names component complications among the rows `<SortableList>` \"opens in place\", so this row is specified as a STATE of that primitive rather than as an entry of its own. Targeted: three complication frames claim it, led by `manager-component-complications-collapsed`."
    },
    {
      "path": "src/ui/svelte/apps/manager/EditorTabs.svelte",
      "library": "<TabBar>",
      "status": "target",
      "evidence": "targeted",
      "why": "The editor tab strip. TEN importers under `src/`, which is the membership bar's own predicate, and NINE of them are wrapping strips: the system, environment, recipe-item, Checks, Knowledge, recipe, essence, tool and vocabulary strips each wrap it, while `scoped/WorldEssenceEntryPage` renders it inline without a wrapper. Both numbers are written out because this row said `8 callers` while nine files imported it — the inline caller was uncounted, and a single figure cannot say whether it means importers or wrappers. `library.html:1008` specifies `<PageHeader> <TabBar>` and states that a studio never puts its tabs in a side rail. Its specimen also draws TWO Rail Marker Family vehicles on one bar — `Tiers 3` and `Businesses 23` as bare counts beside a warning badge on Validation — so drawing the family is part of what the entry specifies, not an extension of it, and spec.md requirement \"Near-neighbour primitives are routed by a stated rule\" forbids substituting one vehicle for another. Issue 1038 ruled the Checks section strip OUT of conversion, led by the reason that its count \"is a bare mono numeral, not a chip — the two treatments are deliberately different\"; that ruling was never recorded in this manifest, and it was wrong twice over — a deliberate STYLE divergence is the one kind a shared primitive may not record, and the strip was RIGHT about the marks while the primitive was too narrow to express them. Issue 1429 made the family a capability instead, and the Checks strip is a caller as of that change. Issue 1038's OTHER three conversions stand and are counted above — the recipe, essence and tool editors — and the capabilities they needed (a chip accessible name, a chip modifier class, the `positive` tone spelling and a suppressible tab hook) live on the primitive rather than in a hand-rolled strip. Issue 1429 converted the LAST hand-rolled strip, `TagsCategoriesView`'s inlined vocabulary tablist, by extracting it as `VocabularyTabs` first — it had survived issue 1038 only because it was not a standalone component and so was never counted. Its record counts moved from the neutral chip onto the count vehicle in the same change, the same substitution the Knowledge strip carried. The one manager strip still recorded as NOT a caller is `downtime/WorldDowntimeTabs`, in {@link NOT_A_PRIMITIVE}, and that row records CONVERSION-PENDING work rather than a justified divergence — read it there before re-deriving the question. Targeted: one frame per wrapping editor claims it."
    },
    {
      "path": "src/ui/svelte/apps/manager/EditorValidationSurface.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "THE editor validation surface, and a CLOSED one since issue 1444 (4 direct callers, 7 renderers): every surface in the manager that draws the aggregate header over the grouped row stack goes through it, so `manager-recipe-val-*` and `manager-recipe-rail-*` have exactly one writer. `library.html` splits validation into `<ValidationList>` and `<ValidationSummary>`; this one component covers both roles, so the correspondence is still not established and is left for the conformance change to adjudicate rather than guessed at here."
    },
    {
      "path": "src/ui/svelte/apps/manager/EmptyState.svelte",
      "library": "<Empty>",
      "status": "target",
      "evidence": "broad",
      "why": "Named at `library.html:911`. 43 callers, second only to `Chip`. Carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because both representative frames are POPULATED states, so the dashed panel it draws appears in neither. `target` rather than `shipped` because that entry states a Svelte API for `<Notice>` alone: a specimen that names no props gives nothing to match."
    },
    {
      "path": "src/ui/svelte/apps/manager/ExplainerCard.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "The explainer card (3 callers). `library.html:911` groups `<Callout> <Notice> <InfoStrip> <Empty>` under one head and carries no prose distinguishing `<Notice>` from `<InfoStrip>`, so which of the two this card is cannot be read off the library."
    },
    {
      "path": "src/ui/svelte/apps/manager/IconFactRow.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "The labelled icon-and-fact row (5 callers). `<StatBox>` (`library.html:858`) is the nearest mark, but a stat box is a boxed value and this is a row of label-plus-fact, so the correspondence is not made."
    },
    {
      "path": "src/ui/svelte/apps/manager/InlineVocabularyAdd.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "why": "The inline vocabulary add field (2 callers, `VocabularyPanel.svelte` and `ImportFolderMappingModal.svelte`). No `library.html` entry names it. Targeted: `manager-tags-categories-normal` claims it."
    },
    {
      "path": "src/ui/svelte/apps/manager/InspectorActionButton.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "why": "THE right-hand inspector's action button, and the row issue 1446 finally moved. `src/ui/svelte/apps/manager/essences/EssenceBrowserInspector.svelte` (issue 1036) and `src/ui/svelte/apps/manager/scoped/WorldEssenceCataloguePage.svelte` (issue 1400) both import it, so it clears the two-caller bar. It sat in {@link NOT_A_PRIMITIVE} through two issues after the second adopter landed, with its own text saying it was OWED A MOVE nobody had made — which is why the change that moved it also ships the guard that re-measures a caller claim against the tree, rather than only the move. `library: null` is a judgement, and it follows `ArmedDangerButton` rather than `InspectorCard`. `library.html:570` heads its block `<Button> <IconButton>` and specifies \"One component, six roles, two sizes\"; `ManagerButton.svelte` claims that entry and IS that closed role set. This file's four tones — neutral, primary, danger and warning — are four of those six roles by name, and it exists precisely BECAUSE it could not be `.manager-button`: its own header records that `.fabricate-manager .manager-button.is-primary` is three classes against a scoped rule's two, so reusing the class would put the primitive's own tones behind the global sheet's. A second file claiming the entry would assert that the tree holds ONE button component where it measurably holds two. That is the `ManagerSearchField` call — `library: null` rather than claim `<Search>` and assert a shell the tree does not have — not the `InspectorCard` one, which claims `<Card>` and diverges on radius and padding, a divergence in VALUES on the file that IS the entry. The one library entry that names this surface at all rules itself out: `library.html:1243`'s inspector rail section is \"Read-only by convention — it shows consequences and links out, and never hosts the editing controls\", and this is the editing controls. Targeted, and its claim set is complete only as of issue 1446. The four `manager-essences-*` frames name this path in `sourceMatches`. `world-essence-catalogue` renders it too — its `essenceInspectorFoot` snippet is the `Open definition` primary, and that case asserts `[data-scoped-list-inspector-foot]` is inside the inspector — and it did NOT claim it, so a change here published four frames of one caller and none of the other. That claim is added. No `BROAD_SIGNAL_CASE_OVERRIDES` entry is owed: an override is consulted only for a BROAD signal, and a targeted row reaches its frames through `sourceMatches`."
    },
    {
      "path": "src/ui/svelte/apps/manager/ItemDropZone.svelte",
      "library": "<LinkField>",
      "status": "target",
      "evidence": "broad",
      "why": "`library.html:747` — \"How a Fabricate record points at a game-world document. Three states in one component.\" 6 callers spread across the checks, component, essence, recipe-item and tool editors, which is why no single frame is the honest evidence."
    },
    {
      "path": "src/ui/svelte/apps/manager/ManagerModal.svelte",
      "library": "<Modal>",
      "status": "target",
      "evidence": "broad",
      "why": "`library.html:1104` and `:1137` name it and its two callers — the import folder-mapping step and the import report — and `:1137` records that ruling it out as single-caller was wrong."
    },
    {
      "path": "src/ui/svelte/apps/manager/ModifierLibraryRow.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "why": "The manager's library ENTRY ROW — glyph tile, name, value in the mono face, and the caller's own trailing control (3 callers, issue 1373). It was inline markup in `checks/CraftingModifierCatalogueCard.svelte` and moved out at round 4, when the Tool Studio's check-bonus picker was ruled onto the same row rather than a stack of option cards; round 5 added the third caller, the Tool Studio's character-prerequisite list, because `proto:4741` states the reference's prerequisite row with the same string as `proto:4752`'s bonus row and the two lists sat one section apart drawn as two different rows. The control is the seam: an eligibility toggle on the Checks screen, a radio on the bonus (one modifier), a checkbox on the prerequisites (several at once). Round 6 made the row's ANATOMY two declared variants of it rather than one shape forced on both lists: `controlPlacement` ('trailing' | 'leading') and `textLayout` ('inline' | 'stacked'), each defaulting to the shipped rendering, because `proto:2331`-`2333` and `proto:2361`-`2364` are one geometry string with two anatomies and round 5 had recorded that difference as a deviation instead of carrying it. The glyph follows `controlPlacement` rather than taking a third prop - the tile is what makes the glyph a row's leading anchor, and a row that leads with a control already has one - so `proto:2332`'s bare accent glyph and `proto:2363`'s tile are one rule. Only the prerequisite list opts in; the Checks Studio caller passes neither prop and its frame is byte-identical. `library` is null because `library.html` specifies no entry row: `:626`'s `<OptionCards>` is the CARD group this row replaced at one of the call sites and is emphatically not it, and no row primitive there covers a library entry. Targeted rather than broad because all three consumers are named and the frames that photograph each claim it by `sourceMatches` — `manager-checks-crafting-modifiers` for the catalogue, the three `*-tool-*bonus*` and `world-tool-entry-requirements` cases for the bonus picker, and `manager-tool-prerequisites-selected-1280x720` for the prerequisite list."
    },
    {
      "path": "src/ui/svelte/apps/manager/RadioCardGroup.svelte",
      "library": "<OptionCards>",
      "status": "target",
      "evidence": "broad",
      "why": "`library.html:626` — \"A radio group with room to explain itself.\" 9 callers."
    },
    {
      "path": "src/ui/svelte/apps/manager/ResolutionModeCard.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "why": "The resolution-mode chooser card (2 callers, `CraftingSettingsView.svelte` and `GatheringEconomyView.svelte`). `library.html:1606` defers the modes themselves to the resolution-modes capability and specifies no card for choosing one. Targeted: `manager-system-edit-normal` claims it."
    },
    {
      "path": "src/ui/svelte/components/SearchablePopover.svelte",
      "library": "<SearchPopover>",
      "status": "target",
      "evidence": "broad",
      "why": "`library.html:784` — \"One panel for picking anything from a catalogue\". 21 importing files over 22 call sites as issue 1503 lands (20 over 24 was recorded at issue 1458 and was never re-measured; 19 files over 20 sites is what it actually was before the two pickers joined): three hand-rolled trigger-plus-listbox add menus (`GatheringEventEditView`, `GatheringTaskEditView`, `ModifierPillSelect`) and the recipe item editor’s Link-recipe menu converted onto it. That change also gave it the `triggerHasPopup` capability, which absorbs the one axis the ten hand-rolled popovers disagreed on — four announced `aria-haspopup=\"dialog\"` and four `listbox`, and the split tracked whether the panel rendered a query field. Carries a THREE-entry `BROAD_SIGNAL_CASE_OVERRIDES` entry because it now has three deliberate presentations (inline-search trigger, value-bearing trigger with the search row, and the search-less option list) and neither representative frame opens a popover at all. Its class family is still the legacy World › Travel name `.manager-travel-*`, which this conversion knowingly concentrates rather than renames, so the rename that follows is a one-file edit plus the sheet. The SCOPING half of that sentence is withdrawn at issue 1472 and written out rather than deleted: it claimed 113 rules in that family and every one of them under `.fabricate-manager`, which was true of the sheet the plan was drafted against and false of the sheet it merged into — issue 1464 re-rooted the family thirteen minutes before the first row repeating it landed, and nothing re-measured. The family is 68 `.manager-travel-*` rules in `styles/fabricate.css`: 32 `.manager-travel-*` rules under `.fabricate-picker` in `styles/fabricate.css`, which are the primitive’s own on its own namespace root, and 35 `.manager-travel-*` rules under `.fabricate-manager` in `styles/fabricate.css`, which are the World › Travel SCREEN plus manager callers’ overrides of their own trigger sites. The picker-rooted figure rose by one at issue 1373: the empty branch took its own class, `.manager-travel-popover-empty`, because a list owns a scroll and an inset and a one-line note owns neither. Those three figures are re-derived from the stylesheet by `tests/design-system-coverage.test.js` rather than asserted, which is exactly what the sentence they replace lacked. The scoping is therefore no longer a reason anything cannot convert, and the two PLAYER-app rows in {@link NOT_A_PRIMITIVE} no longer rest on it. AND IT LIVES UNDER `src/ui/svelte/components/` AS OF ISSUE 1500. The move is the reading of this row's own last sentence carried out: the primitive stopped being a manager component when its family re-rooted at `.fabricate-picker` and the player top bar adopted it, and a file under `apps/manager/` says otherwise to every reader and to `MANAGER_PRIMITIVES`, which named it in `BROAD_SIGNAL_PATTERN`'s manager-name alternation. It reaches the same pattern through the DIRECTORY leg now, so `evidence: \"broad\"` and its seven-frame `BROAD_SIGNAL_CASE_OVERRIDES` entry are unchanged and the pinned pattern source shortens by the 18 characters of `SearchablePopover|`. Its positioning is the shared `anchoredPopover` action over `computeIconPickerPopoverLayout`, and the `parentElement` clipping walk it used to carry is its `bounds` prop's default, from `src/ui/svelte/util/overlayBounds.js` — a shared component must not name an application's own scroller. AND IT KEEPS DOM FOCUS ON ONE ELEMENT AS OF ISSUE 1503, which is `openspec/specs/design-system/spec.md`'s listbox requirement shipped rather than specified. The HOLDER — the search field, or the trigger where `showSearch={false}` suppresses one, at five call sites — carries `role=\"combobox\"`, `aria-controls`, `aria-expanded` and `aria-activedescendant`, and the option rows keep `role=\"option\"` on a `tabindex=\"-1\"` `<button>` that never receives focus: each declares `data-keyboard-focus=\"true\"`, and each suppresses `mousedown`'s default so a click cannot move focus onto it. The cursor index and the option ids are defined over the FLAT RENDERED order, so the grouped branch cannot emit duplicate ids, and the arithmetic lives in `src/ui/svelte/util/listboxNavigation.js` so it is unit-testable without mounting. The keyboard cursor is painted as an inset accent outline rather than a fourth fill rung, because the interaction ladder's three fill rungs are already spent by rest, hover and the marked value — it COMPOSES over whichever fill the row carries. TEN CAPABILITIES ARRIVED WITH THE SAME CHANGE, and they exist because two shipped pickers needed them rather than because the specimen names them: a caller-owned `trigger` snippet (with the primitive omitting every undefined-valued key AND `disabled`/`aria-disabled` from the object it hands over, so a spread-last caller cannot have its own name or its own disabled state erased) receiving the trigger element through a stable `createAttachmentKey()`; an `option` snippet rendered as the row's sole content; `as=\"list\"|\"grid\"` with `columns`, emitted as `data-picker-as`/`data-picker-columns` because `anchoredPopover` replaces the list's whole inline style on every measure; `filterOptions(options, query)`, called on every pass including an empty one; a per-option `class` string; `horizontalAlign`; `measureListMetrics`; `ignoreScrollWithin`; a `triggerOnKeydown` composed AFTER the primitive's own; and `searchClass`/`listClass`/`optionClass` beside the existing `pickerClass`/`popoverClass`. Async `source`, `loading`, `error` and `multiple` are still unbuilt and stay `target` in the library, on a measurement rather than a preference: ZERO of the importers passes a promise, and the only multi-select in the corpus is `ActorSelectTopBar`'s checklist, which is an adjudicated non-member in this same register. AND IT IS THE SUBSTRATE OF EVERY ONE-OF-N LIST AS OF ISSUE 1504. `components/Select.svelte` composes it with `showSearch={false}` and `triggerHasPopup=\"listbox\"` and supplies no `trigger` snippet, so the primitive's OWN button is the combobox holder at that shape — which is why `triggerClass` and `valueClass` are class props of a real element there and dead letters at the two snippet-supplying pickers. 22 importing files over 24 call sites, `Select` among them, and through `Select` this primitive now draws the page size of nine pagers across both applications, two catalogue toolbar lists and three bulk-edit axes. FIVE CAPABILITIES ARRIVED WITH THAT CHANGE: a per-option `disabled` with its `disabledReason`, which the key map steps OVER rather than parking a cursor on a row Enter cannot act on; `triggerAriaLabelledBy`, so a caller can name the trigger by a caption it already renders instead of duplicating the words in an `aria-label`; `dialogAriaLabelledBy`, which names the portaled `role=\"dialog\"` and the `role=\"listbox\"` inside it by the same route and SUPPRESSES `dialogAriaLabel` where both are passed, because a composing caller that holds a caption rather than a string used to resolve the panel's name to `''` and open an unnamed dialog wrapping an unnamed list; an `isDisabled` predicate on `src/ui/svelte/util/listboxNavigation.js`, which returns the current index rather than looping when every option is disabled; and listbox TYPE-AHEAD on that same module, from the CLOSED trigger as well as the open panel, because the closed trigger is where a native `<select>`'s type-ahead is actually used. The type-ahead moves an ACTIVE OPTION and never a value, so a dismissal leaves the value untouched and no provisional-value machinery joins this surface."
    },
    {
      "path": "src/ui/svelte/apps/manager/SegmentedControl.svelte",
      "library": "<Segmented>",
      "status": "shipped",
      "evidence": "broad",
      "why": "`library.html:613` — two to four named options, exactly one chosen, rendered as a radio group. 11 callers."
    },
    {
      "path": "src/ui/svelte/apps/manager/SubjectModifierPicker.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "why": "The subject-and-modifier picker (2 callers, `ComponentEditView.svelte` and `GatheringTaskEditView.svelte`). `library.html:1406` specifies `<RuleRow>` and `<RuleSentence>` as the condition editor behind \"triggers, gates and modifiers\", so this picker is specified as part of that editor rather than as an entry of its own. Targeted: one modifier-pick frame per caller claims it."
    },
    {
      "path": "src/ui/svelte/apps/manager/ToggleCard.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "A titled card wrapping a binary control (7 callers). `<Toggle>` (`library.html:595`) is the bare control and `<Card>` (`:941`) is the container, so a card-plus-toggle is a COMPOSITION of two entries under spec.md requirement \"The primitive set is a closed, versioned vocabulary\" rather than an entry itself. Recorded here because it SHIPS with seven callers; whether the set should contain it is the conformance change's question."
    },
    {
      "path": "src/ui/svelte/apps/manager/VocabularyPanel.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "why": "The vocabulary EDITOR panel: the add form, the search field, the entry count, the row list and its two empty states, which the row's own delete confirm is armed from. TWO callers, and the second is what put it over the bar rather than anything about the component changing \u2014 `src/ui/svelte/apps/manager/TagsCategoriesView.svelte`, the tabbed system-scope screen, and `src/ui/svelte/apps/manager/scoped/WorldVocabularyPage.svelte`, the world screen issue 1392 added, which mounts it three times against three different world vocabularies. Independent is worth stating rather than assuming here: the two are different SCREENS at different scopes, each passing a different vocabulary, a different row attribute and a different cascade sentence, so a change made for one is a change the other has to survive. It is the register asking the question once rather than never: the component shipped at one caller, crossed the bar the day the second screen landed, and the gate that reported it is the closed-vocabulary register. `library: null`, and the nearest entry rules itself out on the same sentence that makes it near. `<SetPicker>` (`library.html:1366`) is \"editing membership of a set too large to render inline\" \u2014 one RECORD's tags \u2014 and this edits the vocabulary those tags are drawn FROM, a world-level list with no record in the frame at all; it also renders every entry, which is what that entry says it never does. No other entry names a vocabulary editor, so this is the ordinary undocumented kind rather than an adjudicated divergence. Its own add field is `InlineVocabularyAdd.svelte`, a member already, and `library: null` there for the same reason. Targeted: `manager-tags-categories-normal` and `world-vocabulary` both claim this path in `sourceMatches`, so a change here publishes the system-scope tab AND the world screen. No `BROAD_SIGNAL_CASE_OVERRIDES` entry is owed: an override is consulted only for a BROAD signal."
    },
    {
      "path": "src/ui/svelte/apps/manager/environment/CompositionList.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "why": "The environment editor's composition list (issue 1321). `src/ui/svelte/apps/manager/environment/EnvironmentTasksTab.svelte` and `src/ui/svelte/apps/manager/environment/EnvironmentEventsTab.svelte` both import it, parameterised by `kind`, so it clears the two-caller bar. Its row claimed ONE caller and named `environment/EnvironmentCompositionTab.svelte` for it — a file that has never existed anywhere in this repository — and every gate passed on it. That is the finding issue 1446 was opened on, and the reason the non-member rows carry a MEASURED `callers` field now instead of a sentence. The two callers are sibling TABS of one editor, so \"independent\" is worth stating rather than assuming. Each renders the list against a different entity kind, a different composition model and a different set of menu verbs, so a change made for one is a change the other has to survive, which is what the bar protects. What they are not is two different SCREENS, and the reading applied here is the mechanical one this module states: an importer is any other file under `src/` that imports the component by path. That is the reading a guard can decide, and a bar decided by judgement alone is the bar this row was recorded against wrongly. `library: null`, and three entries were considered rather than one. `library.html:1049` heads `<ListRow> <BrowseCard> <ViewToggle> <XrefList>` and states that \"every browse list in the app is this row\"; this is measurably not that row, because it renders FOUR sections at once — Included, Available to add, Excluded and a paged Non-matching — each an independent `<ul>` with its own empty state, and its verbs move a row BETWEEN them. `<SetPicker>` (`library.html:1351`) is the entry for editing set membership and rules itself out in its own sentence: it \"never dumps the whole set into the panel it sits in\", and dumping the whole set into the panel is exactly what this does. `<SortableList>` (`library.html:1447`) requires \"both affordances always\", the grip AND the chevrons, and this offers drag alone and only in the ranked-event mode. Claiming any of the three would assert a shape the tree does not have, which is the `ManagerSearchField` call again. Its issue 1458 adjudication was CORRECT AND IS NOW SPENT, and both halves are recorded because a row that quietly loses an argument is how this register drifted before. Its four popovers were adjudicated against `SearchablePopover` and ruled a different widget: each was `aria-haspopup=\"menu\"` opening a `role=\"menu\"` of `role=\"menuitem\"` children — an ACTION menu — while the picker renders `role=\"listbox\"`/`role=\"option\"`, a SELECTION control, so converting them would have changed what a screen reader announces from \"menu, four items\" to \"listbox, four options, none selected\". That reasoning stands. What it did not do is look in the other direction: `component/ComponentIdentityStrip.svelte` had ALREADY gone the way the adjudication forbade, and its two source commands were being announced as selectable options in a listbox under an `aria-haspopup=\"dialog\"` trigger. Issue 1477 answered the register’s own open question — this row said in as many words that the group was \"still a legitimate candidate for a shared ACTION-menu primitive of its own\" with the strip as a fifth site — by extracting `components/ActionMenu.svelte`. So this file is that primitive’s CALLER now, at four sites, and the strip is the second caller that clears the two-caller bar. It carries no `<style>` block at all, so nothing was at risk from the scoped-rule failure a class moving onto a component tag causes; the family it painted lived in `styles/fabricate.css` and is rooted at the primitive. Targeted, and no `BROAD_SIGNAL_CASE_OVERRIDES` entry is owed. All four cases that claim it do so through `^src/ui/svelte/apps/manager/environment/`, the same directory-anchored pattern that claims both callers, so every frame that renders either caller already publishes on a change here — `manager-environment-edit-blind-weights` and `manager-environment-edit-automatic-force-add` are named for two of this component’s own controls. An override would be inert regardless: it is consulted only for a BROAD signal."
    },
    {
      "path": "src/ui/svelte/components/ActionMenu.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "THE overflow action menu (2 callers, issue 1477). It exists because ONE meaning was implemented twice and the copy that reused a shared primitive was the semantically wrong one: `environment/CompositionList.svelte` hand-rolled four correct `role=\"menu\"` blocks, while `component/ComponentIdentityStrip.svelte` reached for `SearchablePopover` — its own comment called that \"the house action-menu vehicle\" — and announced \"Copy source UUID\" and \"Unlink Source Item\" as `role=\"option\"` rows of a `role=\"listbox\"`, with `aria-selected` on each, under an `aria-haspopup=\"dialog\"` trigger. That is exactly the conversion issue 1458 refused to make from the other side. The bar is cleared by those two importing files and by nothing else: `role=\"menu\"` appeared in a single file before this change, so an extraction from `CompositionList` alone would have been a local component rather than a primitive, and the SECOND caller is the one that had gone wrong. `library: null`, and `<Menu>` was considered rather than assumed away. `library.html:801` names it in the `<TintPicker> <ArtPathPicker> <Menu>` block and `:831` says what it is — \"what `<PickerRow>`’s `allowAny` opens\", the kind-choice menu whose specimen draws a current-value row on `--fab-surface-active`. That control is a `SearchablePopover` today and this component is not it, so claiming the name would assert a correspondence the tree does not have and would re-import the selection reading this change exists to remove; adjudicating `<Menu>` belongs to the `allowAny` menu’s own change. Its family is rooted at the two namespace classes the component writes — 8 `.manager-action-menu-*` rules under `.fabricate-action-menu-panel` in `styles/fabricate.css`, plus one root rule on `.fabricate-action-menu` — because the panel is portaled out of the component’s own root and a portaled node keeps its classes and loses its ancestors. It carries no `<style>` block. Broad because it sits under `src/ui/svelte/components/`, where the directory leg of `BROAD_SIGNAL_PATTERN` claims it whatever anyone thinks of the count, and its `BROAD_SIGNAL_CASE_OVERRIDES` entry names the one published frame that OPENS a menu — established from the static import closure, not from the frames: neither representative case can reach it, because `ComponentsBrowserView` and `FabricateAppRoot` hold neither caller in their transitive `.svelte` import closures."
    },
    {
      "path": "src/ui/svelte/components/ChanceSlider.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "A single-value percentage slider (4 callers). `<RangeBar>` (`library.html:714`) specifies a TILED strip of spans whose dividers are dragged, which is `ThresholdBandStrip`, not this."
    },
    {
      "path": "src/ui/svelte/components/CollapsibleGroupHeader.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "The category-group header of a grouped browser list (2 callers). `<ListRow>` (`library.html:1033`) specifies the ROW; the group header above a run of rows is not one of the four entries under that head."
    },
    {
      "path": "src/ui/svelte/components/EssenceSourceSelector.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "The essence source picker (2 importing files, confirmed by measurement rather than carried forward: `essences/EssenceOnCraftTab.svelte` and `essences/EssenceBrowserInspector.svelte`). `library.html:784` names essences among the catalogues its four existing pickers COLLAPSE INTO `<SearchPopover>`. ISSUE 1458 MEASURED WHAT THE COLLAPSE COSTS AND LEFT IT AT FOUR CAPABILITIES FOR ONE CALLER; ISSUE 1503 BUILT ALL FOUR FOR TWO CALLERS AND MADE THE COLLAPSE, and each is recorded with the mechanism that answers it. The caller-owned TRIGGER — a drop-target image tile with an empty state and a corner glyph, beside a clear button inside a `dragDrop` shell — is a `trigger` snippet, and the primitive receives the actual button element through an attachment key spread onto it, which is what keeps the panel anchored to the button rather than to the whole shell. The image-tile GRID is `as=\"grid\" columns={2}`, emitted as `data-picker-as`/`data-picker-columns` on the list and painted from the shared sheet, because the positioning action replaces the list's whole inline style on every measure. The 280-420px band is `minWidth`/`maxWidth`, with the 420 WITHDRAWN to the shared panel's own 340 ceiling. THE REASON FIRST RECORDED FOR THAT WITHDRAWAL WAS WRONG AND IS CORRECTED HERE RATHER THAN DELETED: it claimed the sheet capped the panel at 340 whatever was requested, and there was no such cap — at `b9eb1ce3` the block styling `.fabricate-source-picker-popover.essence-source-picker-popover` declared no `width`, no `min-width` above 0 and no `max-width`, and `.fabricate-picker-popover.manager-travel-popover`'s 340 ceiling did not reach it because that panel did not carry that class. THE CORRECTED REASON WAS ITSELF UNMEASURED AND IS NOW MEASURED, and it does not hold at both sites. `computeIconPickerPopoverLayout` sizes the panel to `clamp(max(triggerWidth, minWidth), minWidth, maxWidth)`, so a ceiling above the floor binds only for a trigger WIDER than it. Measured in Chromium against the shipped sheet at the manager's 1280px width (`tests/components/manager-layout.test.js` builds both ancestor chains and reads the rendered box): `.essence-source-trigger` is 140px at its own rule, 275px in the inspector rail's drop zone (the shell's 300px third track less its border and insets) and 710px in the essence editor's, whose `.manager-edit-card` declares no `max-width` and fills the editor pane. So the inspector never reached 420 and resolves from the 280px floor under either band, while the EDITOR did reach it: the panel there narrows from 420 to the shared 340. That is a licensed change rather than a no-op, and the frame this issue adds photographs the inspector site, not the editor. The two empty messages are `emptyHint` and `noMatchesHint`, which is the distinction the primitive's own filtered-to-nothing predicate was built for. It also hands over a `measureListMetrics` of its own, measured off the rendered box exactly as the sibling icon picker's is (44px tile, 6px pitch, `listExtra: 0` because this panel pins no row), so the grid floors to WHOLE tiles: unmeasured it filled a panel sized from the viewport and cut its last row of bordered tiles partway through, which the frame this issue adds shows. `library: null` still, for the reason the `IconPicker` row states: this component renders THROUGH the specimen's panel and keeps its own trigger, its own row content and its own class family."
    },
    {
      "path": "src/ui/svelte/components/Field.svelte",
      "library": "<Field>",
      "status": "shipped",
      "evidence": "broad",
      "why": "THE manager's labelled form field, the `.manager-field` column (issue 1428). 81 callers across 23 components, second only to `ManagerButton` under `components/`. `library.html:665` heads its block `<Field> <Select> <Search>` and states that \"the kicker label above the field is part of the primitive, not something each screen re-invents\" — which is exactly the box this ships. ONE of the block's other two names is still in the specified-but-unbuilt register, not both: issue 1504 built the 38px control shell, the option list and the `value`/`onChange`/`options` API as `components/Select.svelte`, so only `<Search>` is left. This claims the entry at the same granularity `Chip` claims `<IconChip>` from a block that also names `<Kicker>` and `<StatBox>`, and `Select.svelte` claims `<Select>` from the same block on the same terms — which is also why this row's own labelled form is the one a `<Select>` renders when it is given a `label`. Carries a two-entry `BROAD_SIGNAL_CASE_OVERRIDES` entry because neither representative frame renders a `.manager-field` at all: `manager-components-normal` is a browse list and `fabricate-app-shell` is the player shell."
    },
    {
      "path": "src/ui/svelte/components/FillBar.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "The product's one horizontal fill bar (2 callers). `library.html:872` splits the instrument into `<Meter>`, `<BandedBar>` and `<StageBars>` and states that merging them leaves one component unable to carry a correct accessible role, \"which is why the shipped fill leaf renders none\" — this is that leaf, so it corresponds to no single entry by the library's own account."
    },
    {
      "path": "src/ui/svelte/components/IconButton.svelte",
      "library": "<IconButton>",
      "status": "target",
      "evidence": "broad",
      "why": "The manager's icon-only push-button (36 callers, issue 1422), extracted from 82 hand-rolled `class=\"manager-icon-button\"` sites across 37 components. `library.html:581-582` heads one specimen `<Button> <IconButton>` and specifies the icon size on that ladder (`icon 28x28 - radius 7`) with `ariaLabel` REQUIRED when icon-only, which is the entry this row names. Two divergences from that specimen are deliberate and are recorded rather than smoothed over: the shipped control is 34px (`fabricate.css:13755`), 28px only inside `.manager-pagination`, so the ladder value and the shipped pixel disagree and this change does not repaint 82 sites to settle it; and it takes NO `role` prop, because its modifiers mix roles with states and one site compounds two, so the vocabulary is a design ruling left open rather than folded into a sweep whose acceptance bar is that no frame moves. Broad by reach rather than by count alone: it lands in nearly every studio, browser, inspector and editor tab, and through `Pagination` it reaches the player apps too. Issue 1502 rooted its family at `fabricate-icon-button`, which the primitive emits as the leading token of its own `classes` array, so of its 21 family selectors, 16 are rooted at `fabricate-icon-button`, one at `fabricate-pagination`, and four name a caller's own container under `.fabricate-manager` and are exempt. It declares its own focus PAIR — the `:focus` strip at `fabricate.css:13766` and the `:focus-visible` repaint at `fabricate.css:13779` — rather than borrowing an area's, and emits `data-keyboard-focus=\"true\"` so Foundry's `KeyboardManager#hasFocus` recognises it outside a form. 36 importers: 34 under `apps/manager/**` plus `ActionMenu.svelte` and `Pagination.svelte`, which is why it is EXERCISED in the player apps and not merely capable of it. ITS SIZE CLAIM ABOVE IS UNCHANGED BY ISSUE 1504 and its RADIUS gained a second value: the shipped control takes 6 everywhere except inside `.manager-pagination`, where the pager's nine instances moved onto the specimen's published 7 so the arrows match the converted 30px page-size trigger beside them. That is a new divergence rather than a resolved one — `library.html:606` and the ruled register's new \"Icon button radius\" row both carry it — and the 73 other sites keep the 6 they were extracted with, for the reason the size divergence gives."
    },
    {
      "path": "src/ui/svelte/components/IconPicker.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "The shared icon picker (9 importing files, issue 1269) — `InlineVocabularyAdd`, `VocabularyPanel`, `EnvironmentsBrowserView`, `system/CharacterPrerequisitesCard`, `essences/EssenceBulkEditPanel`, `essences/EssenceIdentityTab`, `scoped/WorldEssenceEntryPage`, `world/WorldCurrencyTab` and `world/WorldModifiersTab`. The 8 recorded here at issue 1269 was never re-measured. `library.html:784` names icons FIRST among the catalogues collapsing into `<SearchPopover>`; same reasoning as `EssenceSourceSelector`. Carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because everything it presents exists only in the open popover and neither representative frame opens one. ISSUE 1458 MEASURED THE COLLAPSE AND LEFT IT; ISSUE 1503 MADE IT, and the five capabilities that row named as ones the primitive LACKS are each addressed by a named mechanism rather than by a redesign. The PINNED resolved row above an alphabetical list of ~1000 (without which the picker always opened on `abacus`) is a caller-ordered `options` array plus a per-option `option.class` string and the `filterOptions` seam — the seam is what lets the caller see the query the primitive now owns, and it also carries the alias tables and the result ranking a plain label-substring filter would have silently dropped. `horizontalAlign: iconOnly ? 'left' : 'right'` is a prop, defaulting to the `left` that was hard-coded. The measured `listMaxHeight` is `measureListMetrics` over `anchoredPopover`'s `targets.list`, so the callback is handed the panel, the list and the search field the primitive now owns. The `style` on the trigger for the colour swatch, the `oncontextmenu` and the `onkeydown` pass-through are all the caller's own, because the caller keeps its whole button in a `trigger` snippet and the primitive's attributes are spread onto it LAST — with the keydown composed after the primitive's rather than replacing it, which is what stops a caller silently deleting the focus model. The `.essence-icon-picker-preview` tile the rows wrap the glyph in is drawn by the caller's `option` snippet. `library: null` still, because `<SearchPopover>` is the panel this now renders THROUGH rather than a specimen this component implements: it keeps its own trigger, its own row content and its own class family, and the family is preserved rather than folded because the View Lab case registry's own walk, the live Foundry smoke and about thirty assertions address `.essence-icon-picker-trigger` by name — renaming them would edit all three to change nothing a GM sees, and belongs to the family-rename child alongside `manager-travel-*`."
    },
    {
      "path": "src/ui/svelte/components/InspectorCard.svelte",
      "library": "<Card>",
      "status": "target",
      "evidence": "broad",
      "why": "THE manager's card shell (19 callers, issue 1427), extracted from 80 hand-written `class=\"manager-inspector-card\"` sections across 20 components — every one of them a `<section>`, measured by walking each component's Svelte AST, so it takes no polymorphism prop. `library.html:941` heads one specimen `<Card> <Well> <RowDisclosure>` and states \"two container levels only; a card sits on the page, a well nests inside a card\" — this is that card, and the entry was in the specified-but-unbuilt register until it shipped. Two divergences from that specimen are deliberate and recorded rather than smoothed over: the shipped box is radius 8 with `--fab-space-3` padding on `--fab-overlay-light-04` (`fabricate.css:13673`) against the specimen's radius 11 / padding 16 / bg-2, and the sheet declares three further treatments keyed on a modifier class (`.manager-checks-card`), on an ANCESTOR (`.manager-checks-rail`, which reaches cards carrying no modifier at all) and on a positioning state (`.is-sticky`, which — measured across `src/` — no component writes at all) — so it takes NO variant prop, because a vocabulary spanning three different anchors is a design ruling and not something to settle inside a sweep whose acceptance bar is that no frame moves. `<Well>`, the level below it, remains unbuilt. Broad by reach: the Checks Studio, both environment inspectors, the essence and tool inspectors, the books-and-scrolls inspector, the Tool Studio authority card and the shared bulk-delete card all render it, and the deferred root holds 32 more sites. It carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because NEITHER representative frame contains a card at all."
    },
    {
      "path": "src/ui/svelte/components/Kicker.svelte",
      "library": "<Kicker>",
      "status": "shipped",
      "evidence": "broad",
      "why": "THE UPPERCASE MICRO-LABEL THAT NAMES WHAT FOLLOWS (issue 1505). `library.html:881` heads its block `<Kicker> <IconChip> <StatBox>` and `library.html:893` states its API; the geometry is `library.html:120` with ONE correction made in the same change, the tracking `0.11em` rather than `.14em`, on two independent issue-1373 measurements and the argument that no measured surface anywhere pairs 8.5px with `.14em`. This claims the entry at the same granularity `Chip` claims `<IconChip>` from the same three-name block. 11 importing files measured by `componentImporters.js`: nine crafting files in one directory, the recipe-item editor's Overview tab, and `StatBox.svelte`, which COMPOSES it because `library.html:894`'s specimen draws the stat box's label as the kicker itself. That pair spans a player app and the manager, which is the portability claim this programme turns on. Broad because it is a `components/` file, which is a leg of the pinned `BROAD_SIGNAL_PATTERN` source; it carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because neither representative frame draws an eyebrow this component renders. It is an IMPORT-FREE LEAF and it is TWO RUNGS DOWN in one tree, so a mounted suite that renders `ShoppingList` or `ItemPageInspector` pulls it in without naming a kicker anywhere — verbatim the `ActionMenu` shape that first reported as `# cancelled` with no failures on a suite whose tests never ran, which is why every harness that reaches either file names it. It takes no `class`, no `style` and no rest spread: a caller needing layout keeps its own wrapper and nests this inside it. `tests/kicker-source-contract.test.js` holds the contract class `fab-kicker`, a token no other file writes, against a one-entry exemption table."
    },
    {
      "path": "src/ui/svelte/components/ManagerButton.svelte",
      "library": "<Button>",
      "status": "target",
      "evidence": "broad",
      "why": "`library.html:581-582` — one component, six roles, two sizes, each role naming a VERB. The shipped `role` prop is that closed set, made required-shaped precisely because the CSS-convention version drifted. 58 importers, the most of anything under `components/`, and EVERY ONE of them under `apps/manager/**` — which makes this the only one of the three re-rooted button-family primitives that is a pure manager capability rather than a control the player apps also exercise. Issue 1502 rooted its family at `fabricate-button`, emitted as the leading literal of its own `classes` array, so of its 84 family selectors, 67 are now compounds on the class the primitive writes; the remaining 17 name a caller's own container under `.fabricate-manager` and are exempt. It declares its own focus PAIR — the `:focus` strip at `fabricate.css:13618` and the `:focus-visible` repaint at `fabricate.css:13629` — rather than borrowing an area's, and emits `data-keyboard-focus=\"true\"` on its `<svelte:element>` root."
    },
    {
      "path": "src/ui/svelte/components/ManagerSearchField.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "THE manager's search field (20 callers, issue 1039), extracted from 23 hand-written `class=\"manager-search\"` labels — every one of them a `<label>` wrapping an `<i class=\"fas fa-search\">` and an `<input type=\"search\">`, measured by walking each component's Svelte AST, so it takes no polymorphism prop and no icon prop. `library:null` is a judgement, not an omission. `library.html:665` specifies `<Search>` as ONE OF THREE modes SHARING ONE SHELL with a kicker label above the field, at height 38 / radius 9. The shipped pill is not that shell: it is 34 / radius 6 (`fabricate.css:5730`), it carries no kicker and no label prop — its accessible name is an `aria-label` on the inner input at all 20 sites — and the manager's select mode is a separate treatment (`.manager-filter select`, `:5741`) this component does not touch, while the shell itself now ships as `Field.svelte` claiming `<Field>`. Recording the correspondence would assert that the shipped tree has the one shell the library specifies, which it measurably does not, so the entry stays in the specified-but-unbuilt register and the adjudication belongs to whichever change unifies the three modes. Same call, and the same reason, as `EditorValidationSurface`. Three of the 23 sites are ADJUDICATED OPT-OUTS rather than deferrals: `GatheringTaskEditView.svelte:1747` and both `CraftingSystemManagerRoot.svelte` sites render a `.manager-tag-suggestions` typeahead list inside the label, which makes them comboboxes belonging to `SearchablePopover`, and the root's two additionally take `bind:this` on the label for popover positioning — which on a COMPONENT tag binds the component instance, not its host element. Broad by reach: every browse screen plus four editors and three rosters. It carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because the representative pair reaches only the base density."
    },
    {
      "path": "src/ui/svelte/components/ManagerToolbar.svelte",
      "library": "<FilterBar>",
      "status": "target",
      "evidence": "broad",
      "why": "THE manager's filter bar (11 callers, issue 1039), extracted from 11 hand-written `class=\"manager-toolbar\"` sections — every one of them a `<section>`, measured by walking each component's Svelte AST, so it takes no polymorphism prop. A raw grep says TWELVE and is wrong: `GatheringTaskEditView.svelte:1784` is a `.manager-toolbar-pills` chip row on a `<div>`, and `\\b` matches before a hyphen. `library.html:1231-1232` heads `<Pagination> <FilterBar>` and `spec.md`'s browse archetype names \"the filter bar\" as a fixed element of every browse screen; this is that bar, and the entry was in the specified-but-unbuilt register until it shipped. THREE divergences from the specimen are recorded rather than smoothed over. (1) The library's FilterBar API owns its controls as DATA — `query`, `toggles`, `selects`, `segments`, `sort`, `result`, `onClear` — and the shipped bar owns only the CHROME and takes its controls as children, because the corpus's filter control is three different things (a `<label class=\"manager-filter\">` wrapping a `<select>`, 16 sites; a bare `<select>` carrying its own accessible name, 2; and `<SegmentedControl>`, 2) and no bar is in a position to choose between them. (2) The sketch specifies ONE component owning both bar and query, and the corpus has TWO: 13 of the 23 search fields render inside no bar at all, so a field folded into the bar would have forced a bar around each of them. The field ships as `ManagerSearchField.svelte`, recorded separately below. (3) It takes NO variant prop — the sheet declares four further treatments anchored four different ways, two of them scrolling caps (`.manager-environments-toolbar`, `.manager-task-toolbar`), one sized from a SCOPED rule in `scoped/EntityListInspectorFrame.svelte` rather than from the sheet at all, and one (`.manager-toolbar:not(:has(.manager-toolbar-primary))`) a branch that is ALWAYS taken because — measured across `src/` — no component writes `.manager-toolbar-primary`, so the grid form at `fabricate.css:5671` is declared and never rendered. Broad by reach: every browse screen in the manager renders it. It carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because the representative pair reaches only the base treatment."
    },
    {
      "path": "src/ui/svelte/components/ManagerColorPopover.svelte",
      "library": "<TintPicker>",
      "status": "target",
      "evidence": "broad",
      "why": "`library.html:797`. 4 callers. The tint swatch panel; its sibling `ManagerColorPicker.svelte` is the single-caller trigger and is recorded in `NOT_A_PRIMITIVE`. `target` rather than `shipped` because that entry states geometry with no Svelte API section: a specimen that names no props gives nothing to match."
    },
    {
      "path": "src/ui/svelte/components/Medallion.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "The flat identity tile for recipe rows and inspectors (13 callers). `<Avatar>` (`library.html:1225`) is a person mark in a rail; this renders a record's linked image with a glyph fallback, so the correspondence is not made."
    },
    {
      "path": "src/ui/svelte/components/ModifierPillSelect.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "A dropdown-plus-removable-pills multi-select (2 callers, issue 770). `<SetPicker>` (`library.html:1333`) specifies editing membership of a set too large to render inline, and states it never dumps the whole set into its panel — this control renders every unselected option in a listbox, so it is not that primitive. Its ADD MENU is `SearchablePopover` since issue 1458, which is why the pill row and the menu are now two different things rather than one hand-rolled widget; the conversion needed the primitive to absorb `triggerAriaDisabled`, the at-cap state that must refuse to open while staying focusable, because the post-removal focus fallback lands on that very button and `focus()` on a `disabled` one silently no-ops. It carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry as of that change: `manager-recipe-edit-crafting-modifier-cap-reached` is the one frame that draws its single remaining painted rule, the at-cap trigger treatment."
    },
    {
      "path": "src/ui/svelte/components/Notice.svelte",
      "library": "<Notice>",
      "status": "shipped",
      "evidence": "broad",
      "why": "THE SURFACE THAT REPORTS SOMETHING THAT JUST HAPPENED (issue 1505). `library.html:938` heads its block and `library.html:963` states its API — which this component follows VERBATIM rather than re-designing, including its spellings, because that list was the specification before it was the record. Geometry from `library.html:221-230`. 2 importing files measured by `componentImporters.js`, which is the membership bar itself: `apps/alchemy/Workbench.svelte`'s brew banner and `apps/inventory/bulk/InventoryBulkReport.svelte`'s run banner. FOUR RECORDED DEVIATIONS, each written into the library entry: an `accent` tone, because the bulk report derives it for the destroy run and the mixed outcome and three published frames draw those states; `tabular-nums` on the detail, over a live count sentence; an `icon` prop, because those two accent states resolve to the same tone with different glyphs; and the `dataAttr`/`dataValue` hook pair with a SECOND `stateDataAttr`/`stateDataValue` pair, because the alchemy banner carries two hooks on one root. `blocking` drives the announcement — `role=\"alert\"` when blocking, `aria-live=\"polite\"` otherwise — and the PAGE-LEVEL arbitration the specification also states is explicitly NOT claimed as met: the shared region that enforces it is issue 1517's. Broad through the `components/` leg of `BROAD_SIGNAL_PATTERN`, with a `BROAD_SIGNAL_CASE_OVERRIDES` entry because neither representative frame is an inventory or an alchemy screen. Its alchemy caller reaches no published frame at all today, so that conversion is recorded UNPHOTOGRAPHED and is held by `tests/components/alchemy-workbench-mounted.test.js`, which acts on all four of its tones."
    },
    {
      "path": "src/ui/svelte/components/Pagination.svelte",
      "library": "<Pagination>",
      "status": "target",
      "evidence": "broad",
      "why": "Named at `library.html:1231-1232`. 25 importers — 19 across every browse surface in the manager, and SIX in the player apps (`apps/crafting/RecipeBrowser`, `apps/inventory/InventoryGrid`, `apps/journal/HistoryList` and three gathering panels), which is what makes it a primitive EXERCISED in both applications rather than a manager capability. Issue 1502 rooted its family at `fabricate-pagination`, written inline on the root `<section>` because this component composes no class list, so its seven owned selectors no longer descend from `.fabricate-manager`; it declares a button-only focus pair, the `:focus` strip at `fabricate.css:5616` and the `:focus-visible` repaint at `fabricate.css:5634`, deliberately not `:is(button, select)`, which would tie `.fabricate-app select:focus-visible` and delete the inset ring that exists to stop a clipped outline on the per-page select. The re-root widened the sheet's reach into all six player callers, so each one restates every newly-painted longhand at the value it rendered before — the audit is in `Pagination.svelte`'s docblock. ITS PAGE-SIZE LIST IS A `<Select>` AS OF ISSUE 1504, and three things moved with it. The rule that painted the native control is DELETED and only its `min-width: 64px` survives, re-hung on the `[data-pagination-size]` hook the trigger now carries — by ATTRIBUTE rather than by the trigger's class, because the area-scope gate reads another primitive's `fabricate-` namespace as an application root; the six non-manager pagers refuse the floor in their own blocks, exactly as they refused its predecessor. The list is UNTICKED (`showTick={false}`): the trigger already states the page size and the list is four short numbers, so a tick column would be a gutter carrying no information. And the accessible name is now the words a GM can actually see — the visible \"Per page\" caption, reached by `aria-labelledby`, rather than an `aria-label` reading \"Rows per page\" that no reader and no screen agreed on; the `PerPageLabel` key retired with the control it named. The prev/next `<IconButton>`s move from radius 6 to the published 7 at seven sites and nine pager instances, so the pair reads as one matched control beside a 30px trigger — a divergence this manifest's `IconButton` row and `library.html:1890` now both record."
    },
    {
      "path": "src/ui/svelte/components/Select.svelte",
      "library": "<Select>",
      "status": "shipped",
      "evidence": "broad",
      "why": "THE app's one select, and the primitive that stopped three shared controls drawing an operating-system drop-down (3 importers, issue 1504). `library.html:665` heads its block `<Field> <Select> <Search>` and specifies the control shell, the option list, the `value`/`onChange`/`options` API and the configurable tick; all four ship here, so `<Select>` leaves the specified-but-unbuilt register `<Field>` left at issue 1428 while `<Search>` stays in it — which is why the block's own `data-status` is still `target`.\n\nIT IS COMPOSED, NOT BUILT. `SearchablePopover` with `showSearch={false}` and `triggerHasPopup=\"listbox\"`, using the primitive's OWN trigger rather than a `trigger` snippet, so the combobox contract, the listbox key model, the option ids, the portal and the attribute-omission rule are the primitive's throughout and not one of them is restated here. What this component adds over that surface is the SELECT's own shape: the option row (a tick column, the label, a hint line, a badge, and a disabled row's reason drawn INSIDE the button so a screen-reader user is told why as well as that), the group list DERIVED from the distinct `option.group` values in first-appearance order, the three size rungs, and the `<Field as=\"label\">` labelled form. 23 declared props, and `Pagination`'s numeric page sizes are the reason two of the derivations exist: `String(value)` is forwarded beside `id: String(option.value)` so `aria-selected`'s strict equality holds, and every option carries a NON-EMPTY `dataId` — `__unchanged__` for the empty-string sentinel — so every rendered row has an identity handle a capture step, a mounted test and the Foundry smoke can address, including the default row a truthiness test would have left unhooked.\n\nITS FAMILY IS IN THE GLOBAL SHEET AND THE COMPONENT HAS NO `<style>` BLOCK AT ALL: 32 `.fabricate-select-*` rules in `styles/fabricate.css`. A scoped rule compiles unlayered against a sheet imported at `layer(modules)` and injected before it, so it silently out-ranks every global rule and is invisible to `tests/components/searchable-popover-area-scope.test.js` — the one gate that proves a shared family is not rooted at a single application. That gate's ninth entry is this family, and it is the first whose every class carries the `fabricate-` prefix the gate otherwise reads as an application root, which is why the entry declares nineteen namespace roots, the two roots it INHERITS from `SearchablePopover`, and its family as a pattern.\n\nA THIRD SIZE RUNG THE SPECIMEN DOES NOT NAME. `toolbar` — 34 / radius 9 / `0.72rem` / weight 500 / fill `--fab-bg-1` — sits beside the specimen's `form` (38 / 9 / 12.5px) and `inline` (30 / 7 / 11.5px). The scoped catalogue's toolbar is a shipped 34px control line whose search field, direction toggle and segmented control this change does not move, so a converted sort key on the 38px rung would have stood four pixels proud of its own row. 34, 9 and 500 are published ladder and ramp values, so the rung costs no debt. Its type is a LITERAL rather than `var(--fab-recipe-control-font)` because that property is declared only under `.fabricate-manager` and a primitive rooted outside every application may not read one; outside the area it is undefined and the declaration falls back to inheritance. Its weight is a ramp numeral because the shipped rule declares none and `normal` is off-ramp — which makes the weight a REAL visual difference on that row, since its three neighbours still compute 400. Issue 1523 owns collapsing the rung and equalising the row.\n\nSEVEN DIVERGENCES FROM THE SPECIMEN'S SURFACE, each with its reason. (1) `size=\"toolbar\"` above. (2) The FILL is stated per size as a token, because the specimen's own \"surface-soft in cards · bg-2 inside bg-1 rows\" is CONTEXTUAL and none of the three shipped call sites is either context. (3) `minWidth`/`maxWidth` are declared per size, because the shared panel's 240px default is wider than a two-digit page-size list AND is floored by a sheet rule the primitive has to override. (4) `ariaLabelledBy` exists, and `id`/`name` are satisfied STRUCTURALLY by the `<Field as=\"label\">` containment plus an `aria-labelledby` to the caption rather than by a `for`/`id` pair — a `<label>` names only labelable elements and this trigger is a `<button>`; `name` is emitted on that button, where it is valid and inert. (5) `triggerData` is part of the declared surface, because three shipped call sites drive their control by a `data-*` hook that the case registry, 35 test call sites and the Foundry smoke all address, and `...rest` lands on the `<Field>` root in the LABELLED form; the bare form has no home for it, because the primitive's own root takes no attribute spread, so it warns and names `triggerData`. (6) The option shape carries a `disabled?` the specimen does not list, beside the `disabledReason?` it does. (7) THE TRIGGER'S BOX IS STYLED FROM THE CALL SITE, not by a prop: a pager's own fill and its own 64px width floor are stated as descendant rules from the caller's surviving wrapper class, because the trigger's box belongs to the row it sits in and six of the seven pagers want a different one from the manager's.\n\nDECLARED WITH NO CALLER ON THIS COMMIT, and held by test rather than by a frame: `mono`, `icon`, `label`, `hint`, `error`, `placeholder`, `invalid`, `readonly`, and the option shape's `badge`, `disabled` and `disabledReason`. The badge and the unavailable reason are two of the five affordances `spec.md` names as forcing an app-drawn list, so they are built and proved here rather than deferred to the first caller that wants one.\n\nEVIDENCE IS BROAD, and judged rather than inherited from the directory. It reaches `BROAD_SIGNAL_PATTERN` through the `components/` leg, and the reach is real: three importers, but through `Pagination` it lands in 25 files across both applications and through `BulkEditSelect` in both bulk panels. It carries a TWO-entry `BROAD_SIGNAL_CASE_OVERRIDES` entry because neither representative frame opens a panel at all — `manager-recipes-bulk-edit-check-tier` photographs the ticked, grouped, hinted list and `player-inventory-page-size` the unticked one in the second application root."
    },
    {
      "path": "src/ui/svelte/components/SelectionCheckbox.svelte",
      "library": "<Checkbox>",
      "status": "shipped",
      "evidence": "broad",
      "why": "`library.html:689` — \"The box a gate list and a selection header both use\", including the tri-state a some-rows-selected header needs. 8 callers."
    },
    {
      "path": "src/ui/svelte/components/StatBox.svelte",
      "library": "<StatBox>",
      "status": "shipped",
      "evidence": "broad",
      "why": "THE AT-A-GLANCE FIGURE (issue 1505). `library.html:881` heads its block and `library.html:894` states its API; the geometry is `library.html:237-239` — `--fab-bg-1`, 1px `--fab-border`, r9, `var(--fab-space-3)`, centred, with a serif 17px/700 value — and the LABEL is a `<Kicker>`, composed rather than restated, which is what the specimen draws. 2 importing files measured by `componentImporters.js`, which is the membership bar itself, and they span both windows: `apps/crafting/ShoppingList.svelte`'s three summary cards and `apps/manager/ItemPageInspector.svelte`'s three Books & Scrolls tiles. THREE RECORDED DEVIATIONS from the specimen, each written into the library entry: an `icon` prop, because the Shopping list renders a leading glyph on the value line in all three cards; `tabular-nums`, which both callers declare so a 9 to 10 change cannot reflow the tile; and the `tone` prop at all, since `.k-stat` declares no tone variant, so each of `default`, `info` and `danger` is derived from a shipped caller state. `size`, `unit`, `tone=\"warning\"` and `tone=\"accent\"` are WITHDRAWN rather than carried, because no caller reaches them and no test acts on one. Four hook props — `dataAttr`/`dataValue` on the box root, `valueDataAttr` on the value and `labelDataAttr` forwarded into the composed `Kicker` — are what let the item-page conversion preserve all six of its existing `data-*` hooks verbatim, so not one of the nine assertions pinned on them moved. Broad through the `components/` leg of `BROAD_SIGNAL_PATTERN`, with a `BROAD_SIGNAL_CASE_OVERRIDES` entry because neither representative frame draws a stat grid. `tests/stat-box-source-contract.test.js` holds the contract class `fab-stat-box`, records the hook props as attribute-only, and requires every literal hook value at a call site to start with `data-`, which is what keeps its \"emits no interactive element\" clause exhaustive."
    },
    {
      "path": "src/ui/svelte/components/StatusPill.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "why": "The row and inspector state pill, six tones (13 callers). The library's mark vocabulary (`library.html:835`, `:858`) specifies marks that carry KIND; this pill carries STATE, and `library.html:348` states that colouring a chip background by kind would double-code kind against state — so it is deliberately not one of those marks."
    },
    {
      "path": "src/ui/svelte/components/StatusToggle.svelte",
      "library": "<Toggle>",
      "status": "target",
      "evidence": "broad",
      "why": "`library.html:595` — \"Binary state that takes effect immediately\", and the manager's one on/off switch (25 callers, issue 1040). It was a CSS convention over an element TREE — track span, nested knob span, optional reading — hand-written at 37 sites in 26 components, and `scoped/InheritRow.svelte` and `scoped/MembershipActions.svelte` each recorded in their own docblocks that they had to. Carries a three-entry `BROAD_SIGNAL_CASE_OVERRIDES` entry because its `as` prop is a closed set of three element HOSTS and no representative frame draws even one of them: `fabricate-app-shell` is the player window and `manager-components-normal` holds only the track-only filter switch. The shipped 34x20 track against this ladder's 38x22 is recorded as a migration in the entry's own delta, not resolved here."
    },
    {
      "path": "src/ui/svelte/components/Stepper.svelte",
      "library": "<Stepper>",
      "status": "target",
      "evidence": "broad",
      "why": "`library.html:551` — \"The most-used control in the app\", and 23 callers here agree. Carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry naming the gathering economy actor frame, which is the only published frame that renders filled steppers, disabled steppers and a rolled-max placeholder at once."
    },
    {
      "path": "src/ui/svelte/components/ThresholdBandStrip.svelte",
      "library": "<RangeBar>",
      "status": "target",
      "evidence": "broad",
      "why": "`library.html:714` — a tiled strip of spans whose dividers are dragged, \"outcome tiers against a DC\", which is exactly this. Exactly 2 callers, `checks/CraftingCheckEditor.svelte` and `checks/SimpleCraftingCheckEditor.svelte`, and it carries a two-entry `BROAD_SIGNAL_CASE_OVERRIDES` entry because those are its two modes and each renders chrome the other does not."
    }
  ],
  "notAPrimitive": [
    {
      "path": "src/ui/svelte/apps/manager/SystemOverviewView.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/manager/SystemEditView.svelte"
      ],
      "why": "ADJUDICATED against `EditorValidationSurface` at issue 1444 and ruled a different surface AND a different kind of thing: a whole ROUTE, not a tab. Measured: `.manager-main manager-system-overview-main` (`:190`) with a `.manager-section-header`, a `.manager-chip-row` of `Chip` counts (`:209`) where the surface has a counts rail, and `.manager-system-overview-list` (`:259`) grouped by entity kind rather than by check category. It writes zero tokens of either family. One caller: `src/ui/svelte/apps/manager/SystemEditView.svelte`."
    },
    {
      "path": "src/ui/svelte/apps/manager/downtime/WorldDowntimeTabs.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/manager/downtime/WorldDowntimeExtensionHost.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/manager/downtime/WorldDowntimeExtensionHost.svelte`. That caller count is what puts the row on THIS list, exactly as it does for the other nine, and the maintainer's divergence ruling does not touch it: one caller is below the two-caller bar however the file is drawn. What the ruling does touch is the second half of the row, the reasoning that told the next audit not to re-propose converting it onto `EditorTabs` — and issue 1429 re-read that reasoning and found it DOES NOT SURVIVE. It is recorded here as CONVERSION-PENDING, not as a justified divergence. Issue 1038's ground (1) was `177 lines of its own scoped <style> ... so the component owns a layout, not just a tablist`, and re-reading the block splits it three ways. `.downtime-tab-card`, `.downtime-connected-studio`, its `strong`/`p` and `.downtime-connected-icon` author a CARD and a sibling identity block that are not the tablist at all; a host that wrapped `<EditorTabs>` would keep every one of them, so they show that the FILE owns a layout and show nothing about the strip. `.downtime-tabs`, the bare `button` rule, its `:hover`/`:focus-visible`/`.is-active` re-tone and the `@container (max-width: 720px)` collapse draw the tablist and its buttons as bordered, filled, 8px-radius pills instead of the shared `.manager-editor-tab*` underline — which is a STYLE divergence, and spec.md requirement \"Near-neighbour primitives are routed by a stated rule\" states that a deliberate style divergence is precisely what a shared primitive exists to remove. Only the padlock (`.downtime-tab-lock` plus the currency re-tone) and the visible hover/focus tooltip are per-tab CONTENT and BEHAVIOUR the primitive has no vehicle or slot for, and those are the functional-or-informational kind the ruling admits. Ground (2), `activeTabId` rather than `activeTab` and no `badges`, is a RENAME and was never a divergence. Ground (3) is real and behavioural — `focusableTabId` falls back to the first tab when `activeTabId` names no rendered tab, so the strip keeps a tab stop while `aria-selected` stays bound to `activeTabId` — but the ruling admits a functional difference as something the primitive ABSORBS AS A CAPABILITY, and `EditorTabs` binding `tabindex` to `activeTab === tab.id` means any strip whose active id names no rendered tab loses its tab stop entirely. That is a defect in the primitive, not a licence for a second strip. So conversion is owed, and issue 1429 deliberately did not attempt it: it needs four capabilities on `EditorTabs` (a per-tab lock affordance, a per-tab accessible-name override, a visible tooltip carrying `aria-describedby` with its own hover/focus state, and the focus fallback) plus hoisting roughly a hundred lines of scoped button and container-query CSS into shipped `styles/fabricate.css` classes, because a scoped rule left on the caller would match nothing once the button is rendered by a child. That is a larger change than the conversion it would accompany and it touches a shared stylesheet, so it is follow-up work rather than a silent extension. Targeted: the `manager-world-downtime-*` frames render it and claim it by `sourceMatches`."
    },
    {
      "path": "src/ui/svelte/apps/manager/environment/EnvironmentValidationTab.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/manager/EnvironmentEditView.svelte"
      ],
      "why": "ADJUDICATED against `EditorValidationSurface` at issue 1444 and ruled a DIFFERENT SURFACE rather than an unconverted one, so the next audit does not re-propose it. Measured: it renders `.manager-environment-card`, `.manager-environment-check-list` and `.manager-environment-issue-list`, carries no status medallion, no counts rail and no grouped row stack, and expresses severity through `Chip` (`:140`) rather than a `manager-recipe-val-pill`. It writes zero `manager-recipe-val-*` and zero `manager-recipe-rail-*` tokens, and each of its three classes appears in exactly one file. Converting it would be a visual REDESIGN of the environment editor, not an adoption. One caller: `src/ui/svelte/apps/manager/EnvironmentEditView.svelte`."
    },
    {
      "path": "src/ui/svelte/apps/ActorSelectTopBar.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/FabricateAppRoot.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/FabricateAppRoot.svelte`. This is an UNADJUDICATED SCREEN REGION and not a candidate that cannot work as a primitive, and TWO reasons keep it here, neither of which this row's history settles: at 506 lines it is a screen region rather than a primitive, and `<AppTitleBar>` (`library.html:965`) specifies the strip above the page header and nav rail, but whether this bar implements that entry has never been decided. Both are open questions rather than objections, so a re-proposal here is answered by adjudicating the correspondence — a design ruling, not a measurement. Issue 1475 converted the actor picker inside the bar and changed neither reason: a screen region that reuses a primitive is still a screen region. The row's THIRD reason, a structural non-conversion recorded against that picker at issue 1458, is WITHDRAWN, and is written out rather than deleted because a row that quietly loses an argument is how this register drifted in the first place. It said the picker renders in the PLAYER window under `.fabricate-app-shell` while all 113 rules in the `.manager-travel-*` family were scoped under `.fabricate-manager`, so converting it would portal to a host that is not there and render the panel entirely unstyled. Neither half survives. Issue 1466 replaced the hard-coded `closest()` on `.fabricate-manager` with `resolveOverlayHost`, whose root set carries `fabricate-app`, and `tests/components/overlay-portal-host-position.test.js` measures a `SearchablePopover` landing at its trigger inside the player window. Issue 1464 re-rooted the primitive’s own family at `.fabricate-picker`, so the 35 `.manager-travel-*` rules under `.fabricate-manager` in `styles/fabricate.css` that remain are the World — Travel screen plus manager callers’ overrides; four of those DO name a class the primitive writes, and all four are anchored on a manager-only container — `.manager-salvage-component-trigger`, `.manager-tool-replacement-card`, `.manager-map-link-picker-cell`, `.manager-recipe-or-popover` — that the player window never renders. AND THE CONVERSION HAPPENED, at issue 1475, which is what turns the withdrawal above from a correction into a demonstration. This bar is `SearchablePopover`’s FIRST PLAYER-WINDOW ADOPTER: until it landed, the only thing depending on either of those two changes was a fixture, and a capability nothing in the product uses is a claim rather than a fact. The picker is now the primitive with `pickerClass=\"actor-bar-picker\"`, `popoverClass=\"actor-bar-popover\"` and `triggerClass=\"actor-bar-trigger\"`, and its panel is PORTALED onto the player window’s ApplicationV2 frame instead of being absolutely positioned inside a 64px bar; `overlay-portal-host-position.test.js` measures that panel against this bar’s own trigger and hit-tests it clear of the bar, which is the acceptance question a DOM assertion cannot ask. The bar’s own `<style>` block fell from thirty-one rules to 18 scoped rules. Ten popover-subtree rules were DELETED rather than wrapped in `:global()`, because they named markup the primitive renders now and no repair could have reached it; the seven trigger rules that remain are `:global()` at their original specificity. Measured on the converted markup: had all fourteen dead rules been kept, TWELVE would have compiled with this component’s scoping hash attached, matched nothing and raised no compiler warning at all, and only `.actor-bar-popover` — whose token travels as `popoverClass` — is one `manager-button-scoped-class-reach.test.js` can see. `lint:svelte:warnings` would have named the other two, both of them descendant selectors. That is the silent mode this register keeps recording, measured on the file it was predicted for. AND IT LIVES UNDER `src/ui/svelte/apps/` AS OF ISSUE 1500, which is this row's first sentence acted on rather than a new judgement: a 506-line UNADJUDICATED SCREEN REGION with one caller is not a shared component, and the directory it sat in is the one whose premise is that everything in it works wherever it is mounted. The move changes its ROUTING and nothing else. It matched `BROAD_SIGNAL_PATTERN` through the directory leg before, so it published the representative pair plus the one frame its `BROAD_SIGNAL_CASE_OVERRIDES` entry named; under `apps/` it matches neither leg, so `evidence` is `targeted`, the override is DELETED as configuration that could no longer fire, and the two frames it actually appears in name it in their own `sourceMatches` — `player-actor-picker`, which opens its picker, and `fabricate-app-shell`, which draws it closed. `manager-components-normal` no longer publishes for it, and should not: it is the manager, and it only ever drew this player bar as a side effect of being half of the broad-signal pair."
    },
    {
      "path": "src/ui/svelte/components/DropZone.svelte",
      "library": null,
      "evidence": "broad",
      "callers": [],
      "why": "ZERO callers — dead code. Nothing under `src/` imports it. (`src/ui/svelte/apps/manager/ItemDropZone.svelte` is a DIFFERENT FILE: it is the shipped link field and a member of the set. A basename search that does not anchor on the path separator conflates the two and credits this dead component with every one of that file’s importers, which is why `scripts/lib/componentImporters.js` resolves each specifier against the directory that wrote it, compares whole repository-relative paths, and never compares a basename at all.)"
    },
    {
      "path": "src/ui/svelte/components/ImagePathPicker.svelte",
      "library": "<ArtPathPicker>",
      "evidence": "broad",
      "callers": [],
      "why": "ZERO callers — dead code, and the one row where the library and the tree disagree in the interesting direction: `library.html:797` specifies `<ArtPathPicker>`, and a file implementing it ships, and nothing imports it. Specified and built is not the same as shipped."
    },
    {
      "path": "src/ui/svelte/components/ManagerColorPicker.svelte",
      "library": null,
      "evidence": "broad",
      "callers": [
        "src/ui/svelte/apps/manager/EnvironmentsBrowserView.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/manager/EnvironmentsBrowserView.svelte`. The `<TintPicker>` entry is carried by its sibling `ManagerColorPopover.svelte`, which clears the bar and is a member of the set."
    },
    {
      "path": "src/ui/svelte/components/RowDisclosure.svelte",
      "library": "<RowDisclosure>",
      "evidence": "broad",
      "callers": [
        "src/ui/svelte/apps/manager/ComplicationSummaryRow.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/manager/ComplicationSummaryRow.svelte`. Named at `library.html:941`, so it is specified and shipped and still below the entry bar. It is separately on `SHARED_PRIMITIVES` in `tests/components/mounted-harness-primitive-allowlist.test.js:83` for a reason the caller count cannot express — omitting it there HANGS a mounted tree — which is why that list is not derived from this manifest."
    },
    {
      "path": "src/ui/svelte/apps/manager/recipe-item/RecipeItemLimitsTab.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/manager/RecipeItemEditor.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/manager/RecipeItemEditor.svelte`, and it is a route tab rather than a candidate primitive. The caller recorded here until issue 1446 was `recipe-item/RecipeItemEditorTabs.svelte`, a real sibling that imports `EditorTabs` and does not import this tab at all: the COUNT was right and the FILE was wrong, which is the half of the drift a count check on its own cannot see. It is recorded because issue 1458 adjudicated the two `role=\"listbox\"` SITES inside it (`:531`, `:631`) against `SearchablePopover` and ruled them a DIFFERENT WIDGET, which is a decision a later audit would otherwise re-propose from the same grep. They are typeahead COMBOBOXES: neither has a trigger button at all, the suggestion list hangs off an `<input>` whose `aria-expanded={suggestions.length > 0}` (`:537`, `:637`) is driven by the query rather than by a control, and the input IS the field rather than a filter over a panel. `SearchablePopover` opens a panel FROM a trigger; a combobox has no closed state to open from. `manager-filter-bar-source-contract.test.js` already records the same verdict for the three `.manager-search` comboboxes, from the other side. The file carries 30 scoped rules and a silent-mode trigger (`<i class={expr}>`), so a conversion would have been the highest-risk one in the candidate set as well as the wrongest."
    },
    {
      "path": "src/ui/svelte/apps/crafting/ComponentSourcesBar.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/ActorSelectTopBar.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/ActorSelectTopBar.svelte`. Recorded because issue 1458 adjudicated its editor popover (`:134`, `:138`) against `SearchablePopover` and found TWO independent disqualifiers, either of which is sufficient. (1) IS WITHDRAWN at issue 1472, and is written out rather than deleted because a row that quietly loses an argument is how this register drifted: it said the panel renders in the PLAYER window under `.fabricate-app-shell` while all 113 rules in the `.manager-travel-*` family were scoped under `.fabricate-manager`, so it would portal to a host that is not there and draw completely unstyled. Issue 1466 put `fabricate-app` into `resolveOverlayHost`’s root set and issue 1464 re-rooted the primitive’s family at `.fabricate-picker`; the `apps/ActorSelectTopBar` row carries the measurement. (2) STANDS ENTIRELY ON ITS OWN, and is why this row is still here. It is a MULTI-SELECT checklist rather than a picker: `toggleAvailable` toggles membership, the popover stays open across choices, and `aria-selected` is true on every chosen actor at once, while `SearchablePopover` has a single `value` and closes on `choose`. It also has NO search field at all — the file renders no `<input>` — against a primitive whose panel is built around one. Converting it would take four clicks to add four source actors and would tell assistive technology that one option is selected when four are, so a conversion needs a multi-select mode on the primitive, which is a design question this row does not settle. Its 28 scoped rules carry NO silent-mode trigger — measured, against the file’s own AST — so they would have pruned and warned rather than dying quietly; that is the one at-risk file in the candidate set where the recorded silencing condition does not hold."
    },
    {
      "path": "src/ui/svelte/apps/manager/RollDataExpressionInput.svelte",
      "library": "<ExprInput>",
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/manager/world/WorldModifiersTab.svelte"
      ],
      "why": "DEMOTED at issue 1373, not adjudicated against anything. It cleared the two-caller bar on the Tool check bonus and `WorldModifiersTab`; the bonus stopped authoring a raw expression when the maintainer ruled that a Tool takes its modifiers FROM the world library rather than by typing one, so the picker became `ModifierLibraryRow` and this input lost that caller. The behaviour the original entry credits - cursor insertion and an actor resolver - is unchanged and still unowned elsewhere, so this is a membership fact about caller count, not a judgement that it stopped being well built. Its `sigil` branch has no caller either, since `WorldModifiersTab` passes `sigil={false}`; removing that branch is owed as its own change. One caller: `src/ui/svelte/apps/manager/world/WorldModifiersTab.svelte`."
    }
  ],
  "ruledOut": [
    {
      "name": "<MemberRow>",
      "verdict": "composition",
      "replacement": "ListRow + `leading`",
      "why": "List row geometry with a portrait in the leading cell — structurally identical to the inline-title row already declined."
    },
    {
      "name": "<ActorPicker>",
      "verdict": "composition",
      "replacement": "trigger + SearchPopover",
      "why": "A pill trigger that opens the popover — which already counted it as one of its four callers."
    },
    {
      "name": "<AddButton>",
      "verdict": "composition",
      "replacement": "Button role=dashed",
      "why": "Dashed is a role on the button that owns the meaning. \"Sits in flow with its collection\" is a usage rule, not a boundary."
    },
    {
      "name": "<RailCard>",
      "verdict": "composition",
      "replacement": "Well + Kicker + Button",
      "why": "A composition that also smuggled back the 34px button size the set retires by name."
    },
    {
      "name": "<FeatureCard>",
      "verdict": "composition",
      "replacement": "OptionCards, non-interactive",
      "why": "Two ABSENT props on an identical shape is a props difference, not a distinction."
    },
    {
      "name": "<BoundsInput>",
      "verdict": "composition",
      "replacement": "two Steppers",
      "why": "\"Two steppers with a rule between\", by its own spec."
    },
    {
      "name": "<CurrencyInput>",
      "verdict": "composition",
      "replacement": "Stepper + Select",
      "why": "A composition, and wrong about the domain: it assumed a flat world-scoped coin ladder where the model defines a per-system acyclic denomination DAG."
    },
    {
      "name": "<PremiumPanel>",
      "verdict": "out-of-scope",
      "replacement": null,
      "why": "A screen, not a primitive. Its one original claim is copy — a product decision — and binding copy to a component makes the offer untranslatable against a codebase where every primitive takes pre-localized strings. Canon also forbids any premium signal in the player window."
    },
    {
      "name": "<Toast>",
      "verdict": "foundry-owns",
      "replacement": "ui.notifications",
      "why": "Foundry owns transient feedback and renders its own toasts over the module window regardless."
    },
    {
      "name": "Destructive panel",
      "verdict": "foundry-owns",
      "replacement": "confirmDialog",
      "why": "Canon makes the dialog the default for manager confirmations; the arm is the carve-out."
    }
  ]
}
