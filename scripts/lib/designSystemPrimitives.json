{
  "designSystemPrimitives": [
    {
      "path": "src/ui/svelte/components/ArmedDangerButton.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "The two-step arm-then-confirm destructive control (11 callers, measured with `componentImporters.js` at issue 1509 against the 4 this row had said). `library.html:581-582` specifies `danger` as one of six ROLES on the single button primitive and `library.html:2002` records a bespoke destructive panel as a surface Foundry already owns, so the arm is a carve-out neither entry claims by name. It is a hand-written carrier of `ManagerButton`'s CSS contract rather than a caller of it, and since issue 1502 that contract is rooted at the class the primitive emits, so this file writes `class=\"fabricate-button manager-button is-danger\"` — the root token, first, with no `fab-manager-button`. The deferral is unchanged and still count-pinned; only the class list moved. MOVED to `src/ui/svelte/components/` at issue 1509, and it gained NO root of its own — the one member of that change's set whose adoption cost nothing in CSS, because the family it writes is already `ManagerButton`'s and already rooted. A `fabricate-danger-button` would own zero rules, fail both the family and the owned floor, and read as an APPLICATION root by name to the entry that does own them, so this component takes no area-scope entry either. ONE CLAIM THIS ROW INHERITED IS CORRECTED WITH THE MOVE: it still does NOT emit `data-keyboard-focus`. Issue 1508 added that attribute to `StatusToggle` and `ModifierPillSelect` only, so Foundry's Space and arrow bindings still fire while an armed danger button holds focus. The emission is a real behaviour change rather than a rooting one, so it is filed as a successor rather than folded into a re-rooting change, and the formless-button ledger row this component carries stays until that successor lands."
    },
    {
      "path": "src/ui/svelte/components/Avatar.svelte",
      "library": "<Avatar>",
      "status": "shipped",
      "evidence": "broad",
      "scope": "shared",
      "why": "AN ACTOR'S PORTRAIT (issue 1506), and the first entry this register gains by a primitive ARRIVING rather than by one being promoted out of the tree. `library.html:1405`/`:1406` head its block and `library.html:1429` draws it: round for people, rounded-square at r9 for parties, vehicles and places, with `shape` CALLER-SUPPLIED because actor type is system-defined and eligibility comes from a GM world setting rather than a type map. It is `<Avatar>` and not `<IconChip>` because the two differ in what they draw with no artwork — a record's tile falls back to a glyph, an actor's to INITIALS — and because one component taking a `kind` prop would have been two components sharing a file. 2 importing files measured by `componentImporters.js`, which is the membership bar itself: `apps/manager/knowledge/KnowledgeRoster.svelte`'s 34px roster row and `apps/manager/KnowledgeView.svelte`'s 50px detail header, independent in surface as well as in file. Its image state is SPECIFIED rather than inherited — `object-fit: cover`, `overflow: hidden`, the `--fab-bg-3` ground and the `1px solid var(--fab-border)` edge kept from the icon chip — so the only property that moved at either site is the corner. `size` is deliberately unrestricted and both off-ladder rungs are recorded on `offLadderArtSizes`. `status` is `shipped` because `library.html:1438` now states its Svelte API; the closing phase of issue 1506 wrote that section and flipped both halves together, and the block's own `data-status` stays `target` because `Rail`, `LogList` and `TierTrack` share it and are unbuilt. Broad through the `components/` leg of `BROAD_SIGNAL_PATTERN`, with a `BROAD_SIGNAL_CASE_OVERRIDES` entry because neither representative frame is a Knowledge screen — the first override key gained by arrival rather than by an extraction. It deliberately does NOT join `SHARED_PRIMITIVES` in the mount harnesses at two callers, which is that list's own rule read the other way and is stated beside `Medallion`'s opposite ruling. 3 IMPORTING FILES over 3 render sites as of issue 1514, and the third is what OVERTURNS the last sentence above: `apps/inventory/detail/InventoryComponentDetail.svelte` draws a source actor's portrait at `shape=\"square\" size={40}`, which is a third caller in a THIRD mounted tree — the player window — so the component joined `SHARED_PRIMITIVES` and left `ADJUDICATED_NON_MEMBERS` in the same commit, on exactly the condition that ruling named for itself. `shape` is mandatory at that site rather than conventional: the default is `round`, which draws 999px, and the well has always drawn a rounded square. The 40px box is unmoved and the corner goes 6 to 9, the ground `--fab-surface-raised` to `--fab-bg-3`, and a 1px `--fab-border` hairline arrives where the well had none — measured 40.00x40.00 on both sides in the View Lab, inside an unmoved 406.78x58.00 row. THE INITIALS FALLBACK IS REACHABLE FOR THE FIRST TIME there: both manager sites pass an image the redaction-safe projection always resolves, while a source actor with no portrait drew `<i class=\"fas fa-user\">` and now draws two letters of its own name. That is a CONTENT change, and it is why `avatar-source-contract.test.js`'s second exact-count clause had its message restated as well as its number moved. 4 IMPORTING FILES over 5 render sites as of the same issue's FIFTH phase, which added the Crafting tab's two source-actor sites in one file: `apps/crafting/ComponentSourcesBar.svelte` draws the top bar's row of portrait buttons at `shape=\"square\" size={40}` and the picker option beneath it at `size={32}`, the portrait ladder's own single mark. THE 32PX SITE MOVES THREE THINGS AND NONE OF THEM IS A SIZE, which is why they are recorded rather than framed: its deleted rule drew 32x32 at a 6px corner over a `--fab-surface-raised` ground with NO border, and the tile draws the same 32x32 at the ladder's 9px over `--fab-bg-3` with a 1px `--fab-border` hairline; the option row is `min-height: 44px` and the tile sets none, so the row is unmoved. It is also UNPHOTOGRAPHABLE - no case in `viewLabCases.js` opens that popover - so `component-sources-bar-mounted.test.js` holds it. THE DOUBLE-EDGE QUESTION THAT SITE RAISED IS ANSWERED ON THE BUTTON, NOT ON THE TILE: the button drew its own 1px `--fab-border` over an r8 `--fab-surface` ground, and this component draws a 1px `--fab-border` that cannot be turned off, so keeping both would have rendered two concentric hairlines. The button's edge, ground and corner came off in the same commit and the tile owns them; measured in the View Lab as an unmoved 40.00x40.00 button holding a 40.00x40.00 tile whose image is 38.00x38.00, which is exactly the box the button's own border used to leave. The required actor's accent ring moved to `outline: 1px solid var(--fab-accent)` at `outline-offset: -1px`, which paints OVER the tile's border rather than beside it, because a `border-color` override cannot reach a border that belongs to a child component. TWO FURTHER CALLER-SIDE MOVES the conversion forced, both recorded because they are the shape a reviewer would otherwise read as unrelated: the hover dim that reveals the remove control was declared on the `<img>`, which is inside this component now and unreachable from a caller's scoped block, so it moved onto the button and is scoped to the removable row by its own published `data-source-removable` hook — the required actor renders no \"x\" to reveal. And the initials fallback is reachable at BOTH new sites, which takes the count of reachable-fallback callers to three."
    },
    {
      "path": "src/ui/svelte/apps/manager/BulkDeleteCard.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "The shared bulk-DELETE card (3 callers, issue 1132). `library.html:1134` specifies `<SelectionBar>` and `<BulkEditPanel>` for this surface and names no delete card. Targeted because it renders in all three studios but is photographed only by the four `*-bulk-delete-*` frames, which `ui-pr-screenshot-evidence.mjs` routes it to; the bulk-EDIT frames do not contain it. Scoped `manager-only`: both callers are manager studio bulk panels, and the card confirms a delete against a manager selection model that no player surface holds."
    },
    {
      "path": "src/ui/svelte/apps/manager/BulkEditPanelShell.svelte",
      "library": "<BulkEditPanel>",
      "status": "target",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "The bulk panel shell (3 callers, issue 1010). `library.html:1134` names it and calls this \"the largest uncovered surface in the corpus\". Targeted: exactly two studios consume it and both claim it by `sourceMatches`. Scoped `manager-only`: it is the manager studios’ bulk rail, and its own scoped block writes four `:global` Apply rules at four classes against the manager button cascade specifically."
    },
    {
      "path": "src/ui/svelte/apps/manager/BulkEditSection.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "A section inside the bulk panel (3 callers, issue 1010). `library.html:1134` specifies the PANEL, not its internal sectioning. Targeted for the same reason as its shell. Scoped `manager-only`: it sections that shell and is meaningless outside it."
    },
    {
      "path": "src/ui/svelte/apps/manager/BulkSelectionToolbar.svelte",
      "library": "<SelectionBar>",
      "status": "target",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "The bulk selection toolbar (3 callers, issue 1010). Named at `library.html:1134`. Targeted for the same reason as its shell. Scoped `manager-only`: it reports a manager studio’s selection count and drives that studio’s bulk actions."
    },
    {
      "path": "src/ui/svelte/apps/manager/BulkStagingInset.svelte",
      "library": "<BulkStagingInset>",
      "status": "shipped",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "The staging inset INSIDE the bulk panel (2 callers, issue 1371 rulings M23-M25): a recessed card holding a 28px search well, a fixed five-row window and a pager, drawing a row in one of four kinds. The system Component Rules list's panel and the world Component catalogue's panel are the same object over different data, which is the membership bar. Its specimen sits beside `<BulkEditPanel>`'s and its Svelte API was read from the shipped `$props()`, so it is shipped rather than a target. Targeted: both panels are claimed by `sourceMatches` through `BULK_EDIT_CHROME_PATTERN`, and the `*-bulk-edit*` frames photograph it. Scoped `manager-only`: it stages rows inside the manager’s two bulk panels and nothing else renders one."
    },
    {
      "path": "src/ui/svelte/apps/manager/Callout.svelte",
      "library": "<Callout>",
      "status": "shipped",
      "evidence": "broad",
      "scope": "shared",
      "why": "Named at `library.html:1037`, and `shipped` as of issue 1505, which wrote the Svelte API that entry had for `<Notice>` alone — the missing API section was this row's whole stated reason for `target`, and it is now `library.html:1061`. THREE FACTS IN THE SENTENCE THIS REPLACES WERE STALE AND ARE CORRECTED TOGETHER. It said `library.html:986`, which is neither the specimen nor its heading. It said \"7 callers across the manager\": measured by `componentImporters.js` at this head, 18 FILES imported it over 26 call sites, and two of them — `apps/inventory/detail/InventorySalvagePanel.svelte` and `apps/inventory/detail/salvage/SalvageProgressiveBody.svelte` — are in the PLAYER window, so \"across the manager\" is false as well as the count. THREE OF THE EIGHTEEN ARRIVED WITH ISSUE 1371 BENEATH THIS CHANGE: `apps/manager/ComponentEditView.svelte`, `apps/manager/scoped/ComponentCatalogueBulkPanel.svelte` and `apps/manager/scoped/WorldComponentEntrySourceCard.svelte` each already passed an explicit tone, so none of them inherits the default that moved. Two carry that recorded judgement unchanged — the world component bulk note stays `info` on a prototype anchor this change does not overrule, and the duplicate-source band stays `warning` — while the salvage roll-budget note in `ComponentEditView.svelte` took a thirteenth re-authoring, `info` to `neutral`, matching the recipe editor's twin of the same standing rule. And it said \"so no frame is a non-arbitrary choice\", which issue 1505 falsified by CHOOSING one: it carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry naming the salvage frames that draw the widened component, and it left `PRIMITIVES_WITH_NO_FRAME` in the same change, which is the direction that list is meant to move. `evidence` stays `broad`: `primitivePathsByEvidence` filters on `evidence` alone and never on `status`, so the `target` → `shipped` flip cannot widen `MANAGER_PRIMITIVES` or move the pinned `BROAD_SIGNAL_PATTERN` source. THE WIDENING IS WHAT MAKES `shipped` HONEST rather than merely recorded: the component converged onto the specimen's own geometry — r11, `--fab-surface-soft`, `--fab-border`, 11.5px/1.6 `--fab-text-muted`, a 13px `--fab-text-subtle` glyph, `align-items: flex-start` — and its default tone is `neutral`, so the info tint is opt-in and reached only when the note is about live state. It gained a `title` borrowing the notice title's treatment whole, an `actions` snippet, and the `accent`/`success`/`danger` tones its callers derive. Two caller overrides deleted with it, the `[data-failure-salvage-note]` block and `.manager-recipe-info-strip`, and the docblock that called its old geometry \"the one already approved visually\" was rewritten rather than left to contradict the shipped component. ISSUE 1514 MOVES BOTH FIGURES BY ONE: 19 files over 27 call sites, the nineteenth being `apps/PlayerViewState.svelte`, which draws this strip at `tone=\"danger\"` for the error branch of all five player views — so the count of player-window importers is three rather than two, and the third is a shared composition rather than a screen. ITS SECOND PHASE MOVES THEM AGAIN, to 21 FILES over 31 CALL SITES: the gathering tab's two restricted-tier event statements are this strip now, four call sites across `apps/gathering/GatheringTasksPanel.svelte` and `apps/gathering/GatheringEventsPanel.svelte`, at `tone=\"info\"`. Both files converted together rather than one of them, because the same `EventSafeHint` sentence renders in both and splitting it would have drawn one sentence two ways. Each was a BARE 12px muted paragraph with no box of its own, so the conversion GAINS the strip's edge, fill and inset - a published frame move, not a preserved one, and the only site in that phase that gains a box. ITS THIRD PHASE TAKES THEM TO 23 FILES over 33 CALL SITES, both in the alchemy-and-journal pair: `apps/alchemy/KnownRecipesColumn.svelte`'s not-yet-revealed footer at \"neutral\", and the WHOLE of `apps/journal/TimeRemainingBox.svelte`, whose scoped block this strip replaced outright because every declaration in it - the amber edge, fill and glyph, the 600-weight lead line over a muted sentence, `align-items: flex-start` and the 2px between them - is already what \"warning\" draws. The footer GAINS 31.11px, measured 71.25 to 102.36 in the View Lab, because its count and its guidance ran inline as one sentence and are a `title` over a `text` now. THE PHASE ALSO REFUSED THIS STRIP TWICE, and both refusals are content model rather than taste. `apps/alchemy/Workbench.svelte`'s still-needed well is a label over a `flex-wrap` row of N chips, and this component has NO `children` - only `title` and `text` as strings and one `actions` snippet that is `flex: none; inline-flex; align-items: center`, which would put the chips beside the sentence and stop them wrapping. The same file's live status strip was refused the other way round: this component HAS the neutral tone that strip's resting mode needs, and cannot carry the `aria-live` region the strip exists for. Scoped `shared` DESPITE its `apps/manager/` path, and the measurement above is the reason: six of the twenty-two importers are player-window surfaces, so `manager-only` would be a false claim about a component the player window already renders. The path is the debt; the scope is the fact. REVIEW ROUND 1 TOOK TWO SITES BACK OFF THIS COMPONENT, on the routing rule rather than on geometry, leaving 22 importing files over 31 render sites. The player view-state composition ERROR branch is a `Notice` now: a callout is documentation that is always true and stays put, a failed load is STATE, and this component emits `role=\"note\"` or - with neither title nor actions, which is how that branch called it - no role at all, so the strip announced nothing. And the `GatheringEventsPanel` event-chance sentence is NOT converted: \"Your chance of encountering an event while gathering here\" is a CAPTION for the bar on the line directly above it, documenting a control rather than stating something about the world, and a caption is neither banner primitive. Measured, that conversion rendered 432.3x44.39 against the 432.3x15 its bare paragraph draws - a +29.39px growth plus a 1px `--fab-info-border` edge, an r11 corner, a `--fab-info-soft` fill and a 12px inset around a caption for a 6px track. THAT DEFERRAL IS A REGISTER ENTRY AND NOW LIVES IN ONE. A shape the set does not name is exactly what the \"ruled-out register is part of the specification\" requirement in `openspec/specs/design-system/spec.md` maintains, so `control caption` is recorded there and in section 15 of `library.html` beside the other refusals. It previously lived only in this string and a source comment, which is not recorded at all: a deferral no register holds is re-litigated by the next reader. An earlier draft cited it to issue 1519, which is the geometry sweep and a different programme. It goes to the register (issue 1519) as a shape the set does not name. Its twin `EventSafeHint` DOES convert, in both files, because \"no events can occur here\" is true of the environment whether or not anything is drawn beside it."
    },
    {
      "path": "src/ui/svelte/components/Chip.svelte",
      "library": "<Chip>",
      "status": "shipped",
      "evidence": "broad",
      "scope": "shared",
      "why": "The app's ONE chip, and the highest-traffic primitive in the set at 91 importing files measured by `componentImporters.js`. The sentence this replaces said 67 callers and pointed at a specimen belonging to a different entry: `library.html:882`/`:883` now head a block of its own, written at issue 1506 from this component's own `$props()` — thirteen tones, three emphases, six densities, the six `iconOnly` square sides and the conditional `role=\"img\"` contract. That API section is why `status` is `shipped` rather than `target`, on the precedent the `apps/manager/Callout.svelte` row above now RECORDS rather than implies (it reads `shipped` \"as of issue 1505, which wrote the Svelte API that entry had\"), and against `components/ManagerColorPopover.svelte`'s row, which still states the rule verbatim: \"`target` rather than `shipped` because that entry states geometry with no Svelte API section: a specimen that names no props gives nothing to match.\" Four status look-alikes across two windows retired into it in the same change, 48 render sites between them, and the first of the four took its own manifest row with it. The file moved from `apps/manager/` to `components/` because two shipped `components/` controls already render it into the PLAYER window. Broad by an enormous margin — attributing a chip change to one window would be a lie about where it lands — and the status flip is pin-neutral, because `primitivePathsByEvidence` filters on `evidence` alone and never on `status`."
    },
    {
      "path": "src/ui/svelte/apps/manager/ComplicationSummaryRow.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "scope": "shared",
      "why": "The complication summary row (5 callers, reaching the player crafting and inventory apps as well as the component editor). `library.html:1509` names component complications among the rows `<SortableList>` \"opens in place\", so this row is specified as a STATE of that primitive rather than as an entry of its own. Targeted: three complication frames claim it, led by `manager-component-complications-collapsed`. Scoped `shared` DESPITE its `apps/manager/` path, for the reason the first sentence already measures: two of its five callers are the player crafting and inventory applications."
    },
    {
      "path": "src/ui/svelte/components/EditorTabs.svelte",
      "library": "<TabBar>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "The editor tab strip. THIRTEEN importers under `src/`, which is the membership bar's own predicate, and NINE of them are wrapping strips: the system, environment, recipe-item, Checks, Knowledge, recipe, essence, tool and vocabulary strips each wrap it, while `ComponentEditView` and the world component, essence and tool entry pages render it inline without a wrapper. Both numbers are written out because this row said `8 callers` while nine files imported it — the inline caller was uncounted, and a single figure cannot say whether it means importers or wrappers. Issue 1509 re-measured both with `componentImporters.js` and found THIRTEEN importers and FOUR inline callers, not ten and one: the wrapper figure had not moved at all while the importer figure had drifted by three, which is exactly the failure a single number hides. `library.html:1137` specifies `<PageHeader> <TabBar>` and states that a studio never puts its tabs in a side rail. Its specimen also draws TWO Rail Marker Family vehicles on one bar — `Tiers 3` and `Businesses 23` as bare counts beside a warning badge on Validation — so drawing the family is part of what the entry specifies, not an extension of it, and spec.md requirement \"Near-neighbour primitives are routed by a stated rule\" forbids substituting one vehicle for another. Issue 1038 ruled the Checks section strip OUT of conversion, led by the reason that its count \"is a bare mono numeral, not a chip — the two treatments are deliberately different\"; that ruling was never recorded in this manifest, and it was wrong twice over — a deliberate STYLE divergence is the one kind a shared primitive may not record, and the strip was RIGHT about the marks while the primitive was too narrow to express them. Issue 1429 made the family a capability instead, and the Checks strip is a caller as of that change. Issue 1038's OTHER three conversions stand and are counted above — the recipe, essence and tool editors — and the capabilities they needed (a chip accessible name, a chip modifier class, the `positive` tone spelling and a suppressible tab hook) live on the primitive rather than in a hand-rolled strip. Issue 1429 converted the LAST hand-rolled strip, `TagsCategoriesView`'s inlined vocabulary tablist, by extracting it as `VocabularyTabs` first — it had survived issue 1038 only because it was not a standalone component and so was never counted. Its record counts moved from the neutral chip onto the count vehicle in the same change, the same substitution the Knowledge strip carried. The one manager strip still recorded as NOT a caller is `downtime/WorldDowntimeTabs`, in {@link NOT_A_PRIMITIVE}, and that row records CONVERSION-PENDING work rather than a justified divergence — read it there before re-deriving the question. ROOTED AT `fabricate-tabs` as of issue 1509, written at the head of its own tablist `class`, with its three vocabulary classes read out of a frozen `DEFAULT_CLASSES` map so the gate sees them as emission rather than as `$props()` defaults it has no reader for — without that hoist eleven of the family’s thirteen selectors would have read caller-owned and stayed application-rooted with the gate reporting the family clean. It is the ONE of that change’s five families that owns a control of its own, the `<button role=\"tab\">` it renders, so it is the one that declares both a font floor (`.fabricate-tabs button`, (0,1,1), in the shared group below the area baseline) and a NEW focus pair, strip above repaint. Broad as of issue 1509, and by DIRECTORY rather than by judgement: the move into `src/ui/svelte/components/` puts this file behind the components leg of `BROAD_SIGNAL_PATTERN`, which `selectRenderFileCases` consults BEFORE any case’s `sourceMatches`. The six cases that claimed it — one frame per wrapping editor — were re-expressed as this row’s `BROAD_SIGNAL_CASE_OVERRIDES` entry in the same commit, so the routing is preserved and the judgement now says where it comes from."
    },
    {
      "path": "src/ui/svelte/components/EditorValidationSurface.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "THE editor validation surface, and a CLOSED one since issue 1444 (5 direct callers measured with `componentImporters.js` at issue 1509, against the 4 this row had said; 7 renderers): every surface in the manager that draws the aggregate header over the grouped row stack goes through it, so `manager-recipe-val-*` and `manager-recipe-rail-*` have exactly one writer. `library.html` splits validation into `<ValidationList>` and `<ValidationSummary>`; this one component covers both roles, so the correspondence is still not established and is left for the conformance change to adjudicate rather than guessed at here. ROOTED AT `fabricate-validation` as of issue 1509, at the head of the class array the component composes, and MOVED to `src/ui/svelte/components/` in the same change. The root name leans toward NEITHER specimen deliberately, for the reason above: it collides with nothing and claims nothing about which of the two this component is. It declares NEITHER a font floor NOR a focus pair, and both are refusals rather than absences — this family owns no control of its own, and a `.fabricate-validation button:focus-visible` at (0,2,1) would out-rank the composed `ManagerButton`'s own ring at (0,2,0) and replace it on the row's View action."
    },
    {
      "path": "src/ui/svelte/apps/manager/EmptyState.svelte",
      "library": "<Empty>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "Named at `library.html:986`. 57 importing files measured by `componentImporters.js`, over 104 call sites - four added by issue 1514's second phase, the gathering tab's four empty-bearing panes at seven call sites between them (six one-line `note` panels and one `filtered` panel whose recovery button is the primitive's own `children`), and five more by its THIRD, the alchemy and journal tabs' seven panel empties. Those seven split by VARIANT and the split is a measurement rather than a house style: two take `filtered` (the two search-empties, each carrying the hint sentence ALONE, which is what all six shipped callers of that variant do and what its own docblock means by \"deliberately skips the icon/title apparatus\"), two take the base hero panel, two take `compact` because they share a half-height column with a sibling list, and `apps/journal/RunDetail.svelte`'s centre-column empty takes the base panel inside a caller-owned `height: 100%` wrapper. AN EIGHTH WAS CONVERTED AND THEN MEASURED BACK OUT: `apps/alchemy/Workbench.svelte`'s bench empty sits inside a DROP ZONE that is already `1.5px dashed` at r14, and every variant here that CENTRES its stack also keeps a dashed box - only `note` releases the panel, at the price of `place-items: start` - so the base panel rendered 478x203.25 inside 520x285.25, two concentric dashed rounded boxes 40px apart, and the bench grew 121.25 to 203.25. That is the shape this component's own popover note refuses in as many words, and the outer box there is a drop affordance whose dragover cue is that same border, so it cannot be given up to the panel either. (The sentence this replaces said 43, which was stale by four before issue 1514 touched it; the figure is re-derived from the tree here rather than adjusted.) THE PLAYER WINDOW IS THE SECOND APPLICATION TO RENDER IT as of issue 1514: `apps/PlayerViewState.svelte` composes it for the no-actor and empty branches of the crafting, alchemy, gathering, journal and inventory views, and the two claims in that component's OWN docblock that refused those callers as \"a different area shell\" were retired in the commit that falsified them. Carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because both representative frames are POPULATED states, so the dashed panel it draws appears in neither. `target` rather than `shipped` because that entry states a Svelte API for `<Notice>` alone: a specimen that names no props gives nothing to match. 65 IMPORTING FILES over 119 render sites as of issue 1514's FOURTH phase, which added fourteen of them in the inventory tab: thirteen one-line `note` sites — the ten that retired the shell's published `.inventory-detail-empty-note` leaf and the three `salvage-empty` lines — plus one full PANEL, `InventoryDetail`'s no-selection pane, nested inside a caller-owned fill wrapper on the same terms `PlayerViewState` and `GatheringEnvironmentList` take. Every `note` site measures 26.00px tall against the 15.00 its bare `<p>` drew, at 10px/1.4 in `--fab-text-subtle` behind `var(--fab-space-chip)`. ONE MEASURED LIMIT was found and is recorded on the component: `is-note` declares `place-items: start` and `text-align: left` on ITSELF, so a caller cannot restore a centred line through a wrapper — `InventoryGrid`'s \"No items match the current filters.\" stays hand-rolled rather than becoming a 146.55px line in the top-left corner of a 630px-tall empty column, and `filtered`, which centres, keeps the dashed panel. A centred one-line form would close it. 70 IMPORTING FILES over 124 render sites as of the FIFTH phase - the largest caller set in the register, second only to `Chip` - which added five in the crafting tab and refused one. Four are one-line `note` panels — the source picker's no-owned-actors line, the consumption plan's nothing-planned line, the essence pool's no-carriers line and the outcome table's no-outcomes line — and the fifth is a full PANEL, the shopping planner's empty, nested inside a caller-owned fill wrapper on the same terms `InventoryDetail` took. THAT FIFTH SITE IS A CORRECTION TO THE PLAN'S OWN CENSUS: it was listed as a one-liner because its markup is a single `<p>`, and it RENDERS a 24px glyph over a centred sentence filling the whole planner column, measured 288.86x574.72 in the View Lab. Classification is by rendered shape, and a shape measured rather than read off the markup is what the two answers differ on. The refusal is `RecipeBrowser`'s no-recipes line, and it is the limit above holding a second time at the same shape: a full-width centred sentence standing in for a whole list, carrying its filtered/zero distinction in the TEXT through an `isFiltering` ternary, exactly as `InventoryGrid` does. FOUR OF THE FIVE ARE UNPHOTOGRAPHABLE and the fifth is the only one a frame carries: measured across every crafting case in the registry, no `.manager-empty.is-note` renders anywhere in this tab - the source picker's line needs a popover no case opens, and the other three need a pane state no case seeds - while the shopping planner's panel draws in fourteen of them. One further site converted through the shared view-state COMPOSITION rather than directly — `RecipeDetail`'s no-selection pane, the sixth copy of the five player views' own chrome — and it is counted under that composition rather than here. Scoped `shared` DESPITE its `apps/manager/` path. Issue 1509 introduced this field and recorded the reason as TRANSITIVE rather than direct: 46 of its then-47 measured importers were manager surfaces, and the forty-seventh was `components/SearchablePopover.svelte`, which renders `<EmptyState note>` for its own no-results panel — and the player window's `ActorSelectTopBar` renders that picker, so the component already painted in the player application by the route spec.md requirement \"A shared primitive's class family is rooted at the primitive, not at an app\" calls exercised-transitively. ISSUE 1514 MAKES THE SAME ANSWER DIRECT: 46 of the 70 importers are manager surfaces, and of the other twenty-four, twenty-three are player-window views that render it directly — the last being that picker. `manager-only` would have been a false claim under either route; the path is the debt, not the scope. REVIEW ROUND 1 MOVED THE INK AND ONE CALLER, and 71 IMPORTING FILES over 125 render sites is the count after both. The `note` and `filtered` variants painted `--fab-text-subtle` and `--fab-text-disabled`, which is what `proto:2262` and `proto:2545` state. On the default `fabricate` theme, whose three text tones are ALPHAS over the surface rather than opaque values, those composite to 3.69:1 and 2.66:1 on `--fab-surface` at 10px and 11.5px - small text, under the 4.5:1 floor. Both variants take `--fab-text-muted` now: 5.42:1 on `--fab-surface`, 5.00:1 on `--fab-surface-soft`, and clearing the floor in all seven palettes with `ironblood-forge` worst at 5.19:1 and 4.77:1. It is the ONE figure of the reference that this family does not take, corrected on the VARIANTS rather than at a call site so that it reaches all 26 `note` and all 9 `filtered` sites - including the six manager filtered-empties and `components/SearchablePopover.svelte` own no-results panel, none of which this issue otherwise touches. ONE palette in seven is what hid it: `mythwright` alone states `muted` and `subtle` as opaque hues rather than as alphas over the surface, and it is the only palette whose subtle tone clears the floor at all, reading 5.28:1 from the same declaration. Six fail and one passes. All seven state `disabled` as an alpha, which is why the `filtered` variant's 2.66:1 fails everywhere with no outlier to hide behind. AND THE SIXTH COMPOSITION CALLER BECAME A DIRECT ONE. `RecipeDetail` no-selection pane is a PANE inside a `--fab-surface-soft` bordered column, and the view-state composition carries the opaque view-ROOT fill each of its five roots declared for itself - so routed through it, the pane filled that column edge to edge in a colour the column did not choose, directly comparable in the same frame to the tinted right column beside it. It keeps its own wrapper and nests this panel inside it, which is the caller-owned answer five other sites in this change already take, and a `background` prop for one caller is refused."
    },
    {
      "path": "src/ui/svelte/apps/manager/ExplainerCard.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "manager-only",
      "why": "The explainer card (3 callers). `library.html:986` groups `<Callout> <Notice> <InfoStrip> <Empty>` under one head and carries no prose distinguishing `<Notice>` from `<InfoStrip>`, so which of the two this card is cannot be read off the library. Scoped `manager-only`: it explains a world-authoring concept to a GM, which is a manager-side job."
    },
    {
      "path": "src/ui/svelte/apps/manager/IconFactRow.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "manager-only",
      "why": "The labelled icon-and-fact row (5 callers). `<StatBox>` (`library.html:860`) is the nearest mark, but a stat box is a boxed value and this is a row of label-plus-fact, so the correspondence is not made. Scoped `manager-only`: every measured importer is a manager inspector or editor."
    },
    {
      "path": "src/ui/svelte/apps/manager/InlineVocabularyAdd.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "The inline vocabulary add field (2 callers, `VocabularyPanel.svelte` and `ImportFolderMappingModal.svelte`). No `library.html` entry names it. Targeted: `manager-tags-categories-normal` claims it. Scoped `manager-only`: it edits a world vocabulary, which only the GM manager does."
    },
    {
      "path": "src/ui/svelte/apps/manager/InspectorActionButton.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "THE right-hand inspector's action button, and the row issue 1446 finally moved. `src/ui/svelte/apps/manager/essences/EssenceBrowserInspector.svelte` (issue 1036) and `src/ui/svelte/apps/manager/scoped/WorldEssenceCataloguePage.svelte` (issue 1400) both import it, so it clears the two-caller bar. It sat in {@link NOT_A_PRIMITIVE} through two issues after the second adopter landed, with its own text saying it was OWED A MOVE nobody had made — which is why the change that moved it also ships the guard that re-measures a caller claim against the tree, rather than only the move. `library: null` is a judgement, and it follows `ArmedDangerButton` rather than `InspectorCard`. `library.html:570` heads its block `<Button> <IconButton>` and specifies \"One component, six roles, two sizes\"; `ManagerButton.svelte` claims that entry and IS that closed role set. This file's four tones — neutral, primary, danger and warning — are four of those six roles by name, and it exists precisely BECAUSE it could not be `.manager-button`: its own header records that `.fabricate-button.manager-button.is-primary` is three classes against a scoped rule's two, so reusing the class would put the primitive's own tones behind the global sheet's. A second file claiming the entry would assert that the tree holds ONE button component where it measurably holds two. That is the `ManagerSearchField` call — `library: null` rather than claim `<Search>` and assert a shell the tree does not have — not the `InspectorCard` one, which claims `<Card>` and diverges on radius and padding, a divergence in VALUES on the file that IS the entry. The one library entry that names this surface at all rules itself out: `library.html:1319`'s inspector rail section is \"Read-only by convention — it shows consequences and links out, and never hosts the editing controls\", and this is the editing controls. Targeted, and its claim set is complete only as of issue 1446. The four `manager-essences-*` frames name this path in `sourceMatches`. `world-essence-catalogue` renders it too — its `essenceInspectorFoot` snippet is the `Open definition` primary, and that case asserts `[data-scoped-list-inspector-foot]` is inside the inspector — and it did NOT claim it, so a change here published four frames of one caller and none of the other. That claim is added. No `BROAD_SIGNAL_CASE_OVERRIDES` entry is owed: an override is consulted only for a BROAD signal, and a targeted row reaches its frames through `sourceMatches`. Scoped `manager-only`: the manager’s inspector rail is the only surface that has one."
    },
    {
      "path": "src/ui/svelte/components/ItemDropZone.svelte",
      "library": "<LinkField>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "`library.html:769` — \"How a Fabricate record points at a game-world document. Three states in one component.\" 9 callers spread across the checks, component, essence, recipe-item and tool editors, which is why no single frame is the honest evidence; the count was re-measured with `componentImporters.js` at issue 1509 against the 6 this row had said, and the citation moved with issue 1508’s five `library.html` insertions. ROOTED AT `fabricate-link-field` as of issue 1509 and MOVED to `src/ui/svelte/components/` in the same change, which also replaced its thirteen `kind ===` attribute branches with one `hookAttrs` bag over a closed region set. It declares NEITHER a font floor NOR a focus pair, and the pair is the sharper refusal of the two: both action buttons are `IconButton`’s, and `.fabricate-link-field button:focus-visible` at (0,2,1) would out-rank that primitive’s own ring at (0,2,0) and REPLACE it on both."
    },
    {
      "path": "src/ui/svelte/apps/manager/ManagerModal.svelte",
      "library": "<Modal>",
      "status": "target",
      "evidence": "broad",
      "scope": "manager-only",
      "why": "`library.html:1282` and `:1315` name it and its two callers — the import folder-mapping step and the import report — and `:1315` records that ruling it out as single-caller was wrong. Scoped `manager-only`: it is the manager window’s own modal chrome, and the name says so."
    },
    {
      "path": "src/ui/svelte/apps/manager/ModifierLibraryRow.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "The manager's library ENTRY ROW — glyph tile, name, value in the mono face, and the caller's own trailing control (3 callers, issue 1373). It was inline markup in `checks/CraftingModifierCatalogueCard.svelte` and moved out at round 4, when the Tool Studio's check-bonus picker was ruled onto the same row rather than a stack of option cards; round 5 added the third caller, the Tool Studio's character-prerequisite list, because `proto:4741` states the reference's prerequisite row with the same string as `proto:4752`'s bonus row and the two lists sat one section apart drawn as two different rows. The control is the seam: an eligibility toggle on the Checks screen, a radio on the bonus (one modifier), a checkbox on the prerequisites (several at once). Round 6 made the row's ANATOMY two declared variants of it rather than one shape forced on both lists: `controlPlacement` ('trailing' | 'leading') and `textLayout` ('inline' | 'stacked'), each defaulting to the shipped rendering, because `proto:2331`-`2333` and `proto:2361`-`2364` are one geometry string with two anatomies and round 5 had recorded that difference as a deviation instead of carrying it. The glyph follows `controlPlacement` rather than taking a third prop - the tile is what makes the glyph a row's leading anchor, and a row that leads with a control already has one - so `proto:2332`'s bare accent glyph and `proto:2363`'s tile are one rule. Only the prerequisite list opts in; the Checks Studio caller passes neither prop and its frame is byte-identical. `library` is null because `library.html` specifies no entry row: `:626`'s `<OptionCards>` is the CARD group this row replaced at one of the call sites and is emphatically not it, and no row primitive there covers a library entry. Targeted rather than broad because all three consumers are named and the frames that photograph each claim it by `sourceMatches` — `manager-checks-crafting-modifiers` for the catalogue, the three `*-tool-*bonus*` and `world-tool-entry-requirements` cases for the bonus picker, and `manager-tool-prerequisites-selected-1280x720` for the prerequisite list. Scoped `manager-only`: it is a row of the world modifier library, a GM authoring surface."
    },
    {
      "path": "src/ui/svelte/components/RadioCardGroup.svelte",
      "library": "<OptionCards>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "`library.html:646` — \"A radio group with room to explain itself.\" 11 callers, measured with `componentImporters.js` at issue 1509 against the 9 this row had said: that change deleted the `ResolutionModeCard` shim, which made `CraftingSettingsView` and `GatheringEconomyView` direct importers, so 10 at its base became 11 as it landed. The citation moved with issue 1508’s five `library.html` insertions and read `:626` until this row was re-derived. ROOTED AT `fabricate-option-cards` as of issue 1509, written at the head of the class this component hands `Field`, and MOVED to `src/ui/svelte/components/` in the same change. It declares NO font floor, and the reason is CO-ROOTING rather than composition: its root element IS `Field`’s fieldset, so it always carries `fabricate-field` beside this root and that family’s (0,1,1) floor already reaches every radio in every host. Its focus pair already existed and was RE-ROOTED IN PLACE rather than restated at the root, which is `StatusToggle`’s case arriving a second time."
    },
    {
      "path": "src/ui/svelte/components/SearchablePopover.svelte",
      "library": "<SearchPopover>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "`library.html:786` — \"One panel for picking anything from a catalogue\". 22 importing files over 23 call sites as issue 1503 lands (20 over 24 was recorded at issue 1458 and was never re-measured; 20 files over 21 sites is what it actually was before the two pickers joined, and both site figures are the `<SearchablePopover>` component NODES `tests/helpers/primitiveAdoptionContract.js` parses rather than `grep` hits, which count `ActorSelectTopBar`'s own docblock mention of the tag as one site more): three hand-rolled trigger-plus-listbox add menus (`GatheringEventEditView`, `GatheringTaskEditView`, `ModifierPillSelect`) and the recipe item editor’s Link-recipe menu converted onto it. That change also gave it the `triggerHasPopup` capability, which absorbs the one axis the ten hand-rolled popovers disagreed on — four announced `aria-haspopup=\"dialog\"` and four `listbox`, and the split tracked whether the panel rendered a query field. Carried a THREE-entry `BROAD_SIGNAL_CASE_OVERRIDES` entry at that point, for its three deliberate presentations (inline-search trigger, value-bearing trigger with the search row, and the search-less option list), and neither representative frame opens a popover at all; it is EIGHT as of issue 1503, whose grid-form frame is the eighth. Its class family is still the legacy World › Travel name `.manager-travel-*`, which this conversion knowingly concentrates rather than renames, so the rename that follows is a one-file edit plus the sheet. The SCOPING half of that sentence is withdrawn at issue 1472 and written out rather than deleted: it claimed 113 rules in that family and every one of them under `.fabricate-manager`, which was true of the sheet the plan was drafted against and false of the sheet it merged into — issue 1464 re-rooted the family thirteen minutes before the first row repeating it landed, and nothing re-measured. The family is 68 `.manager-travel-*` rules in `styles/fabricate.css`: 32 `.manager-travel-*` rules under `.fabricate-picker` in `styles/fabricate.css`, which are the primitive’s own on its own namespace root, and 35 `.manager-travel-*` rules under `.fabricate-manager` in `styles/fabricate.css`, which are the World › Travel SCREEN plus manager callers’ overrides of their own trigger sites. The picker-rooted figure rose by one at issue 1373: the empty branch took its own class, `.manager-travel-popover-empty`, because a list owns a scroll and an inset and a one-line note owns neither. Those three figures are re-derived from the stylesheet by `tests/design-system-coverage.test.js` rather than asserted, which is exactly what the sentence they replace lacked. The scoping is therefore no longer a reason anything cannot convert, and the two PLAYER-app rows in {@link NOT_A_PRIMITIVE} no longer rest on it. AND IT LIVES UNDER `src/ui/svelte/components/` AS OF ISSUE 1500. The move is the reading of this row's own last sentence carried out: the primitive stopped being a manager component when its family re-rooted at `.fabricate-picker` and the player top bar adopted it, and a file under `apps/manager/` says otherwise to every reader and to `MANAGER_PRIMITIVES`, which named it in `BROAD_SIGNAL_PATTERN`'s manager-name alternation. It reaches the same pattern through the DIRECTORY leg now, so `evidence: \"broad\"` and its then seven-frame `BROAD_SIGNAL_CASE_OVERRIDES` entry are unchanged and the pinned pattern source shortens by the 18 characters of `SearchablePopover|`. Its positioning is the shared `anchoredPopover` action over `computeIconPickerPopoverLayout`, and the `parentElement` clipping walk it used to carry is its `bounds` prop's default, from `src/ui/svelte/util/overlayBounds.js` — a shared component must not name an application's own scroller. AND IT KEEPS DOM FOCUS ON ONE ELEMENT AS OF ISSUE 1503, which is `openspec/specs/design-system/spec.md`'s listbox requirement shipped rather than specified. The HOLDER — the search field, or the trigger where `showSearch={false}` suppresses one, at seven call sites — carries `role=\"combobox\"`, `aria-controls`, `aria-expanded` and `aria-activedescendant`, and the option rows keep `role=\"option\"` on a `tabindex=\"-1\"` `<button>` that never receives focus: each declares `data-keyboard-focus=\"true\"`, and each suppresses `mousedown`'s default so a click cannot move focus onto it. The cursor index and the option ids are defined over the FLAT RENDERED order, so the grouped branch cannot emit duplicate ids, and the arithmetic lives in `src/ui/svelte/util/listboxNavigation.js` so it is unit-testable without mounting. The keyboard cursor is painted as an inset accent outline rather than a fourth fill rung, because the interaction ladder's three fill rungs are already spent by rest, hover and the marked value — it COMPOSES over whichever fill the row carries. TEN CAPABILITIES ARRIVED WITH THE SAME CHANGE, and they exist because two shipped pickers needed them rather than because the specimen names them: a caller-owned `trigger` snippet (with the primitive omitting every undefined-valued key AND `disabled`/`aria-disabled` from the object it hands over, so a spread-last caller cannot have its own name or its own disabled state erased) receiving the trigger element through a stable `createAttachmentKey()`; an `option` snippet rendered as the row's sole content; `as=\"list\"|\"grid\"` with `columns`, emitted as `data-picker-as`/`data-picker-columns` because `anchoredPopover` replaces the list's whole inline style on every measure; `filterOptions(options, query)`, called on every pass including an empty one; a per-option `class` string; `horizontalAlign`; `measureListMetrics`; `ignoreScrollWithin`; a `triggerOnKeydown` composed AFTER the primitive's own; and `searchClass`/`listClass`/`optionClass` beside the existing `pickerClass`/`popoverClass`. Async `source`, `loading`, `error` and `multiple` are still unbuilt and stay `target` in the library, on a measurement rather than a preference: ZERO of the 23 importers passes a promise, and the only multi-select in the corpus is `ActorSelectTopBar`'s checklist, which is an adjudicated non-member in this same register.\n\nAND IT IS THE SUBSTRATE OF EVERY ONE-OF-N LIST AS OF ISSUE 1504. `components/Select.svelte` composes it with `showSearch={false}` and `triggerHasPopup=\"listbox\"` and supplies no `trigger` snippet, so the primitive's OWN button is the combobox holder at that shape — which is why `triggerClass` and `valueClass` are class props of a real element there and dead letters at the two snippet-supplying pickers. 23 importing files over 24 call sites by the same AST walk, `Select` among them, and through `Select` this primitive now draws the page size of nine pagers across both applications, the scoped catalogue toolbar's lane filters and sort key, and two bulk-edit axes. FIVE CAPABILITIES ARRIVED WITH THAT CHANGE: a per-option `disabled` with its `disabledReason`, which the key map steps OVER rather than parking a cursor on a row Enter cannot act on; `triggerAriaLabelledBy`, so a caller can name the trigger by a caption it already renders instead of duplicating the words in an `aria-label`; `dialogAriaLabelledBy`, which names the portaled `role=\"dialog\"` and the `role=\"listbox\"` inside it by the same route and SUPPRESSES `dialogAriaLabel` where both are passed, because a composing caller that holds a caption rather than a string used to resolve the panel's name to `''` and open an unnamed dialog wrapping an unnamed list; an `isDisabled` predicate on `src/ui/svelte/util/listboxNavigation.js`, which returns the current index rather than looping when every option is disabled; and listbox TYPE-AHEAD on that same module, from the CLOSED trigger as well as the open panel, because the closed trigger is where a native `<select>`'s type-ahead is actually used. The type-ahead moves an ACTIVE OPTION and never a value, so a dismissal leaves the value untouched and no provisional-value machinery joins this surface."
    },
    {
      "path": "src/ui/svelte/apps/manager/SegmentedControl.svelte",
      "library": "<Segmented>",
      "status": "shipped",
      "evidence": "broad",
      "scope": "shared",
      "why": "`library.html:613` — two to four named options, exactly one chosen, rendered as a radio group. 17 importing files over 24 render sites, measured by `componentImporters.js` at issue 1514's fourth phase, replacing a stale count of 11. Scoped `shared` DESPITE its `apps/manager/` path. It read `manager-only` because every measured importer was a manager editor, and it recorded that a caller elsewhere is what would move the row rather than a re-root. **The flip is decided by the FIRST caller outside `apps/manager/`, regardless of audience.** That caller is `src/ui/svelte/apps/interactables/InteractablesManagerRoot.svelte` — the GM-only Manage Interactables scene panel, a canvas application outside that directory, NOT a player surface. The gate `tests/design-system-coverage.test.js` enforces scope by import PATH, not by audience, so a manager-audience caller outside the directory falsifies `manager-only` exactly as a player one would. Of the two remedies that gate states, the importer is NOT the defect: the control was always a general one, `library.html` specifies `<Segmented>` for the whole product, and nothing about the component changed — only the location decision did. The path is the debt; the scope is the fact. A FURTHER non-manager caller is additional evidence for the same decision rather than a new reason for it, so a later change that renders this control from its own non-manager root adds its file to the list above and does not re-justify the scope. Issue 1514 is that later change and adds `apps/inventory/InventoryFilters.svelte`, the first PLAYER-window caller, under this clause rather than as a fresh justification. What that conversion records is about the SITE, not the scope: the kind filter was five `<button aria-pressed>` in a `role=\"group\"`, which announces five independent states for a genuinely one-of-N choice, and is one radiogroup now, drawn `shape=\"pill\" tone=\"accent-soft\"` because that pair reproduces the construction and the paint the hand-rolled strip already had. Measured at the narrowest supported window the strip goes 527.64x28.00 to 571.89x30.39 with 221.14px of headroom left in its 906px row, so it does not overflow — but the track is `inline-flex` with no `flex-wrap` where `.inventory-pills` wrapped, and that capability is lost rather than unused. `data-inventory-pill` survives through `optionDataAttr`; `data-inventory-pill-count` is DROPPED, because the primitive renders a fixed `data-segment-count` and nothing in `src/`, `tests/` or `scripts/` read the old name. The tab strip in the same tab was measured against this component and REFUSED: it is a real tablist wired to two `role=\"tabpanel\"` regions by `aria-controls`, which a radiogroup has no equivalent for."
    },
    {
      "path": "src/ui/svelte/apps/manager/SubjectModifierPicker.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "The subject-and-modifier picker (2 callers, `ComponentEditView.svelte` and `GatheringTaskEditView.svelte`). `library.html:1486` specifies `<RuleRow>` and `<RuleSentence>` as the condition editor behind \"triggers, gates and modifiers\", so this picker is specified as part of that editor rather than as an entry of its own. Targeted: one modifier-pick frame per caller claims it. Scoped `manager-only`: it authors a check’s subject modifiers, which is a GM authoring task."
    },
    {
      "path": "src/ui/svelte/components/ToggleCard.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "A titled card wrapping a binary control (8 callers, re-measured with `componentImporters.js` at issue 1509 against the 7 this row had said). `<Toggle>` (`library.html:611`) is the bare control and `<Card>` (`:1068`) is the container, so a card-plus-toggle is a COMPOSITION of two entries under spec.md requirement \"The primitive set is a closed, versioned vocabulary\" rather than an entry itself. Recorded here because it SHIPS with eight callers; whether the set should contain it is the conformance change's question. ROOTED AT `fabricate-toggle-card` as of issue 1509, at the head of its own root template, and MOVED to `src/ui/svelte/components/` in the same change — the docblock reason it had given for living under `apps/manager/`, that it wears classes styled only under `.fabricate-manager`, is precisely what that re-root retired, so the location decision fell with its own premise in the commit that falsified it. It declares NEITHER a font floor NOR a focus pair: the only control inside the card is `StatusToggle`'s switch, which paints its own."
    },
    {
      "path": "src/ui/svelte/apps/manager/VocabularyPanel.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "The vocabulary EDITOR panel: the add form, the search field, the entry count, the row list and its two empty states, which the row's own delete confirm is armed from. TWO callers, and the second is what put it over the bar rather than anything about the component changing — `src/ui/svelte/apps/manager/TagsCategoriesView.svelte`, the tabbed system-scope screen, and `src/ui/svelte/apps/manager/scoped/WorldVocabularyPage.svelte`, the world screen issue 1392 added, which mounts it three times against three different world vocabularies. Independent is worth stating rather than assuming here: the two are different SCREENS at different scopes, each passing a different vocabulary, a different row attribute and a different cascade sentence, so a change made for one is a change the other has to survive. It is the register asking the question once rather than never: the component shipped at one caller, crossed the bar the day the second screen landed, and the gate that reported it is the closed-vocabulary register. `library: null`, and the nearest entry rules itself out on the same sentence that makes it near. `<SetPicker>` (`library.html:1446`) is \"editing membership of a set too large to render inline\" — one RECORD's tags — and this edits the vocabulary those tags are drawn FROM, a world-level list with no record in the frame at all; it also renders every entry, which is what that entry says it never does. No other entry names a vocabulary editor, so this is the ordinary undocumented kind rather than an adjudicated divergence. Its own add field is `InlineVocabularyAdd.svelte`, a member already, and `library: null` there for the same reason. Targeted: `manager-tags-categories-normal` and `world-vocabulary` both claim this path in `sourceMatches`, so a change here publishes the system-scope tab AND the world screen. No `BROAD_SIGNAL_CASE_OVERRIDES` entry is owed: an override is consulted only for a BROAD signal. Scoped `manager-only`: it authors a world vocabulary."
    },
    {
      "path": "src/ui/svelte/apps/manager/components/EssenceChip.svelte",
      "library": "<EssenceChip>",
      "status": "shipped",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "An essence drawn as a chip in the colour the world Essence Catalogue gave it (four callers, issue 1371 maintainer ruling M29): the system Component Rules list's row badges in `src/ui/svelte/apps/manager/components/ComponentRow.svelte`, the system inspector's essence run in `src/ui/svelte/apps/manager/components/ComponentBrowserInspector.svelte`, the world Component entry's preview rail in `src/ui/svelte/apps/manager/scoped/WorldComponentEntryPreviewRail.svelte` (M27), and the world Component catalogue's rows in `src/ui/svelte/apps/manager/scoped/WorldComponentCataloguePage.svelte` (M30), which arrives on it in this same revision. It composes the manager's one chip through its `tint` axis and owns only the essence-to-chip mapping — glyph fallback, `{name} {quantity}` accessible name, the count in the mono face — which three sites had each restated and each got wrong on colour. Its specimen sits beside the icon chip's and its Svelte API was read from the shipped `$props()`, so it is shipped rather than a target. ONE SITE IS DEFERRED AND SAID SO: `src/ui/svelte/apps/alchemy/EssenceChips.svelte` draws the same meaning at four call sites in the PLAYER app and is not converted, because it belongs to the player app's visual language rather than the manager's and tints through its own `--alchemy-essence-color` vehicle rather than the manager chip's `tint`; converting it is a player-app decision with its own frames, not a side effect of M29. The specimen's delta block records the same deferral, so the next reader adds a caller here rather than a third implementation. Targeted: it lives in the components browser's directory, which `manager-components-*` claim by `sourceMatches` and the components-browser evidence recipe by glob, so those frames are where a change to it lands. Scoped `manager-only`: every measured importer is a manager essence surface, and the shared `Chip` is what a player surface reaches for."
    },
    {
      "path": "src/ui/svelte/apps/manager/environment/CompositionList.svelte",
      "library": null,
      "status": "target",
      "evidence": "targeted",
      "scope": "manager-only",
      "why": "The environment editor's composition list (issue 1321). `src/ui/svelte/apps/manager/environment/EnvironmentTasksTab.svelte` and `src/ui/svelte/apps/manager/environment/EnvironmentEventsTab.svelte` both import it, parameterised by `kind`, so it clears the two-caller bar. Its row claimed ONE caller and named `environment/EnvironmentCompositionTab.svelte` for it — a file that has never existed anywhere in this repository — and every gate passed on it. That is the finding issue 1446 was opened on, and the reason the non-member rows carry a MEASURED `callers` field now instead of a sentence. The two callers are sibling TABS of one editor, so \"independent\" is worth stating rather than assuming. Each renders the list against a different entity kind, a different composition model and a different set of menu verbs, so a change made for one is a change the other has to survive, which is what the bar protects. What they are not is two different SCREENS, and the reading applied here is the mechanical one this module states: an importer is any other file under `src/` that imports the component by path. That is the reading a guard can decide, and a bar decided by judgement alone is the bar this row was recorded against wrongly. `library: null`, and three entries were considered rather than one. `library.html:1125` heads `<ListRow> <BrowseCard> <ViewToggle> <XrefList>` and states that \"every browse list in the app is this row\"; this is measurably not that row, because it renders FOUR sections at once — Included, Available to add, Excluded and a paged Non-matching — each an independent `<ul>` with its own empty state, and its verbs move a row BETWEEN them. `<SetPicker>` (`library.html:1428`) is the entry for editing set membership and rules itself out in its own sentence: it \"never dumps the whole set into the panel it sits in\", and dumping the whole set into the panel is exactly what this does. `<SortableList>` (`library.html:1527`) requires \"both affordances always\", the grip AND the chevrons, and this offers drag alone and only in the ranked-event mode. Claiming any of the three would assert a shape the tree does not have, which is the `ManagerSearchField` call again. Its issue 1458 adjudication was CORRECT AND IS NOW SPENT, and both halves are recorded because a row that quietly loses an argument is how this register drifted before. Its four popovers were adjudicated against `SearchablePopover` and ruled a different widget: each was `aria-haspopup=\"menu\"` opening a `role=\"menu\"` of `role=\"menuitem\"` children — an ACTION menu — while the picker renders `role=\"listbox\"`/`role=\"option\"`, a SELECTION control, so converting them would have changed what a screen reader announces from \"menu, four items\" to \"listbox, four options, none selected\". That reasoning stands. What it did not do is look in the other direction: `component/ComponentIdentityStrip.svelte` had ALREADY gone the way the adjudication forbade, and its two source commands were being announced as selectable options in a listbox under an `aria-haspopup=\"dialog\"` trigger. Issue 1477 answered the register’s own open question — this row said in as many words that the group was \"still a legitimate candidate for a shared ACTION-menu primitive of its own\" with the strip as a fifth site — by extracting `components/ActionMenu.svelte`. So this file is that primitive’s CALLER now, at four sites, and the strip is the second caller that clears the two-caller bar. It carries no `<style>` block at all, so nothing was at risk from the scoped-rule failure a class moving onto a component tag causes; the family it painted lived in `styles/fabricate.css` and is rooted at the primitive. Targeted, and no `BROAD_SIGNAL_CASE_OVERRIDES` entry is owed. All four cases that claim it do so through `^src/ui/svelte/apps/manager/environment/`, the same directory-anchored pattern that claims both callers, so every frame that renders either caller already publishes on a change here — `manager-environment-edit-blind-weights` and `manager-environment-edit-automatic-force-add` are named for two of this component’s own controls. An override would be inert regardless: it is consulted only for a BROAD signal. Scoped `manager-only`: it edits an environment’s composition, a manager authoring surface."
    },
    {
      "path": "src/ui/svelte/components/ActionMenu.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "THE overflow action menu (2 callers, issue 1477). It exists because ONE meaning was implemented twice and the copy that reused a shared primitive was the semantically wrong one: `environment/CompositionList.svelte` hand-rolled four correct `role=\"menu\"` blocks, while `component/ComponentIdentityStrip.svelte` reached for `SearchablePopover` — its own comment called that \"the house action-menu vehicle\" — and announced \"Copy source UUID\" and \"Unlink Source Item\" as `role=\"option\"` rows of a `role=\"listbox\"`, with `aria-selected` on each, under an `aria-haspopup=\"dialog\"` trigger. That is exactly the conversion issue 1458 refused to make from the other side. The bar is cleared by those two importing files and by nothing else: `role=\"menu\"` appeared in a single file before this change, so an extraction from `CompositionList` alone would have been a local component rather than a primitive, and the SECOND caller is the one that had gone wrong. `library: null`, and `<Menu>` was considered rather than assumed away. `library.html:822` names it in the `<TintPicker> <ArtPathPicker> <Menu>` block and `:852` says what it is — \"what `<PickerRow>`’s `allowAny` opens\", the kind-choice menu whose specimen draws a current-value row on `--fab-surface-active`. That control is a `SearchablePopover` today and this component is not it, so claiming the name would assert a correspondence the tree does not have and would re-import the selection reading this change exists to remove; adjudicating `<Menu>` belongs to the `allowAny` menu’s own change. Its family is rooted at the two namespace classes the component writes — 8 `.manager-action-menu-*` rules under `.fabricate-action-menu-panel` in `styles/fabricate.css`, plus one root rule on `.fabricate-action-menu` — because the panel is portaled out of the component’s own root and a portaled node keeps its classes and loses its ancestors. It carries no `<style>` block. Broad because it sits under `src/ui/svelte/components/`, where the directory leg of `BROAD_SIGNAL_PATTERN` claims it whatever anyone thinks of the count, and its `BROAD_SIGNAL_CASE_OVERRIDES` entry names the one published frame that OPENS a menu — established from the static import closure, not from the frames: neither representative case can reach it, because `ComponentsBrowserView` and `FabricateAppRoot` hold neither caller in their transitive `.svelte` import closures."
    },
    {
      "path": "src/ui/svelte/components/ChanceSlider.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "A single-value percentage slider (4 callers). `<RangeBar>` (`library.html:716`) specifies a TILED strip of spans whose dividers are dragged, which is `ThresholdBandStrip`, not this. Issue 1508 rooted its family at `fabricate-slider`, written inline on the component’s own root span exactly as `Pagination` writes its root: of its 35 family selectors 22 are compounds or descendants of that class, one more is a caller-CLASS compound that moved with the fill rule it overrides, and the remaining 12 name the gathering task editor’s or the drop editor card’s own container and are exempt. It owns TWO controls — a number half and a range track — and its root is a span that is neither, so it declares a (0,1,1) font floor over `.fabricate-slider input` and its focus pair over that same bare element. It leaves `PRIMITIVES_WITH_NO_FRAME` in the same change: its new `BROAD_SIGNAL_CASE_OVERRIDES` entry names `world-tool-entry`, whose `sm-tool-hammer` fixture declares the `breakageChance` breakage mode and whose walk clicks the breakage tab, so that frame DRAWS a slider rather than merely claiming the file that renders one."
    },
    {
      "path": "src/ui/svelte/components/CollapsibleGroupHeader.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "The category-group header of a grouped browser list (2 callers). `<ListRow>` (`library.html:1109`) specifies the ROW; the group header above a run of rows is not one of the four entries under that head."
    },
    {
      "path": "src/ui/svelte/components/EssenceSourceSelector.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "The essence source picker (2 importing files, confirmed by measurement rather than carried forward: `essences/EssenceOnCraftTab.svelte` and `essences/EssenceBrowserInspector.svelte`). `library.html:786` names essences among the catalogues its four existing pickers COLLAPSE INTO `<SearchPopover>`. ISSUE 1458 MEASURED WHAT THE COLLAPSE COSTS AND LEFT IT AT FOUR CAPABILITIES FOR ONE CALLER; ISSUE 1503 BUILT ALL FOUR FOR TWO CALLERS AND MADE THE COLLAPSE, and each is recorded with the mechanism that answers it. The caller-owned TRIGGER — a drop-target image tile with an empty state and a corner glyph, beside a clear button inside a `dragDrop` shell — is a `trigger` snippet, and the primitive receives the actual button element through an attachment key spread onto it, which is what keeps the panel anchored to the button rather than to the whole shell. The image-tile GRID is `as=\"grid\" columns={2}`, emitted as `data-picker-as`/`data-picker-columns` on the list and painted from the shared sheet, because the positioning action replaces the list's whole inline style on every measure. The 280-420px band is `minWidth`/`maxWidth`, with the 420 WITHDRAWN to the shared panel's own 340 ceiling. THE REASON FIRST RECORDED FOR THAT WITHDRAWAL WAS WRONG AND IS CORRECTED HERE RATHER THAN DELETED: it claimed the sheet capped the panel at 340 whatever was requested, and there was no such cap — at the merged main `58719d9d` the block styling `.fabricate-source-picker-popover.essence-source-picker-popover` declared no `width`, no `min-width` above 0 and no `max-width`, and `.fabricate-picker-popover.manager-travel-popover`'s 340 ceiling did not reach it because that panel did not carry that class. THE CORRECTED REASON WAS ITSELF UNMEASURED AND IS NOW MEASURED, and it does not hold at both sites. `computeIconPickerPopoverLayout` sizes the panel to `clamp(max(triggerWidth, minWidth), minWidth, maxWidth)`, so a ceiling above the floor binds only for a trigger WIDER than it. Measured in Chromium against the shipped sheet at the manager's 1280px width (`tests/components/manager-layout.test.js` builds both ancestor chains and reads the rendered box): `.essence-source-trigger` is 140px at its own rule, 275px in the inspector rail's drop zone (the shell's 300px third track less its border and insets) and 710px in the essence editor's, whose `.manager-edit-card` declares no `max-width` and fills the editor pane. So the inspector never reached 420 and resolves from the 280px floor under either band, while the EDITOR did reach it: the panel there narrows from 420 to the shared 340. That is a licensed change rather than a no-op, and the frame this issue adds photographs the inspector site, not the editor. The two empty messages are `emptyHint` and `noMatchesHint`, which is the distinction the primitive's own filtered-to-nothing predicate was built for. It also hands over a `measureListMetrics` of its own, measured off the rendered box exactly as the sibling icon picker's is (44px tile, 6px pitch, `listExtra: 0` because this panel pins no row), so the grid floors to WHOLE tiles: unmeasured it filled a panel sized from the viewport and cut its last row of bordered tiles partway through, which the frame this issue adds shows. `library: null` still, for the reason the `IconPicker` row states: this component renders THROUGH the specimen's panel and keeps its own trigger, its own row content and its own class family."
    },
    {
      "path": "src/ui/svelte/components/Field.svelte",
      "library": "<Field>",
      "status": "shipped",
      "evidence": "broad",
      "scope": "shared",
      "why": "THE manager's labelled form field, the `.manager-field` column (issue 1428). 81 callers across 23 components, second only to `ManagerButton` under `components/`. `library.html:666` heads its block `<Field> <Select> <Search>` and states that \"the kicker label above the field is part of the primitive, not something each screen re-invents\" — which is exactly the box this ships. ONE of the block's other two names is still in the specified-but-unbuilt register, not both: issue 1504 built the 38px control shell, the option list and the `value`/`onChange`/`options` API as `components/Select.svelte`, so only `<Search>` is left. This claims the entry at the same granularity `Chip` claims `<IconChip>` from a block that also names `<Kicker>` and `<StatBox>`, and `Select.svelte` claims `<Select>` from the same block on the same terms — which is also why this row's own labelled form is the one a `<Select>` renders when it is given a `label`. Carries a two-entry `BROAD_SIGNAL_CASE_OVERRIDES` entry because neither representative frame renders a `.manager-field` at all: `manager-components-normal` is a browse list and `fabricate-app-shell` is the player shell. Issue 1508 rooted its family at `fabricate-field`, emitted as the leading literal of its own `classes` array: of its 27 family selectors 9 are owned compounds on that root, three more are caller-CLASS compounds sharing a selector list with owned members and moved with them, and the other 15 name a caller’s own container — or, for two, an application root qualified by a view attribute — and are exempt. The root is a WRAPPER and the control is a bare `input`, `select` or `textarea`, so its font floor is written at the family root PLUS the element it owns, (0,1,1), and declares `font: inherit` and nothing else, because at that rank it TIES the manager’s own bare-element baseline rather than losing to it. The `appearance`, `-webkit-appearance` and `min-height: 34px` chrome is a SEPARATE rule restating that baseline’s element-TYPED predicate leg for leg, because written over `:is(input, select, textarea)` the same two declarations would reach the radios, ranges, steppers and selects the predicate deliberately excludes. It declares its own focus PAIR over `input` and `textarea` only, excluding `select` for the reason the pager’s ring records. `status` does not move: the re-root alters neither this entry’s API nor its geometry, so the flip condition never fires."
    },
    {
      "path": "src/ui/svelte/components/FillBar.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "The product's one horizontal fill bar (4 callers, three of them in the player window). Issue 1514 added `apps/gathering/GatheringTaskDrops.svelte`'s per-drop chance track at `size=\"sm\"`, and that conversion is PIXEL-EXACT rather than approximate. The hand-rolled track declared `height: 6px` with a 1px edge and no `box-sizing` of its own, which the plan read as an 8px content box and therefore as a 2px shrink; measured in the View Lab it renders 143.53x6 before and 143.53x6 after, because Foundry core's own `@layer reset` declares `*, *::before, *::after { box-sizing: border-box }` over the whole document. The track was ALREADY border-box and `sm` is its exact height. The same issue's THIRD phase added `apps/journal/RunCard.svelte`'s run progress track on that measurement and it holds a second time: 300.84x6 before and 300.84x6 after, with the fill moving 51.14x6 to 50.80x4 as this bar's own hairline takes 1px off each edge. That site is also where the missing ROLE this entry's last sentence explains is answered in practice - the element that used to BE the track survives as a wrapper carrying `role=\"progressbar\"` and all three `aria-value*` attributes, because this leaf renders none of them. ITS FIFTH PHASE TAKES IT TO 6 IMPORTING FILES over 6 render sites, two more in the player window: `apps/crafting/detail/EssencePoolPanel.svelte`'s per-essence meter and `apps/ActorSelectTopBar.svelte`'s contextual stamina track. Both are answers to a question the two conversions before them did not ask. The pool meter painted its fill FIVE different ways from its own scoped block — a base tint, a met/partial/short trio and a `has-tint` triple — and a caller's scoped block cannot reach this leaf's element, so all five became the `tone` and `color` props, with `color` handed `var(--fab-chip-color)` rather than a value because the essence's own colour is declared on an ancestor and INHERITS. The stamina track is the `flex: 1 1 auto` case stated: dropped bare into a shrink-to-fit `inline-flex` row it has no width at all, so the element that used to BE the track survives as a 72px WIDTH PIN, which is the same caller-owns-the-layout answer the run progress track takes for the ROLE. That site is also the one conversion in this change that NO FRAME CAN SHOW: the strip renders only when the gathering system is in stamina mode with an environment selected, and the View Lab world declares no stamina-mode gathering system, so its 72x6 is the caller's `width` against this leaf's border-box `sm` height and `actor-select-top-bar-mounted.test.js` is what holds it. All four sites gain a 1px `--fab-border` edge and take this leaf's ground in place of their own, and none of the four boxes moves, because Foundry core's own `@layer reset` makes every element border-box before this repository's sheet is read. ONE THING BOTH SITES LOSE and it is in the register rather than worked around: each fill had a `transition: width 0.2s ease`, the pool's with a `prefers-reduced-motion` escape, and this leaf declares none — so both bars now move instantly. A transition this component owns, with its own reduced-motion escape, is what would close it. `library.html:874` splits the instrument into `<Meter>`, `<BandedBar>` and `<StageBars>` and states that merging them leaves one component unable to carry a correct accessible role, \"which is why the shipped fill leaf renders none\" — this is that leaf, so it corresponds to no single entry by the library's own account."
    },
    {
      "path": "src/ui/svelte/components/IconButton.svelte",
      "library": "<IconButton>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "The manager's icon-only push-button (36 callers, issue 1422), extracted from 82 hand-rolled `class=\"manager-icon-button\"` sites across 37 components. `library.html:581-582` heads one specimen `<Button> <IconButton>` and specifies the icon size on that ladder (`icon 28x28 - radius 7`) with `ariaLabel` REQUIRED when icon-only, which is the entry this row names. Two divergences from that specimen are deliberate and are recorded rather than smoothed over: the shipped control is 34px (`fabricate.css:13979`), 28px only inside `.manager-pagination`, so the ladder value and the shipped pixel disagree and this change does not repaint 82 sites to settle it; and it takes NO `role` prop, because its modifiers mix roles with states and one site compounds two, so the vocabulary is a design ruling left open rather than folded into a sweep whose acceptance bar is that no frame moves. Broad by reach rather than by count alone: it lands in nearly every studio, browser, inspector and editor tab, and through `Pagination` it reaches the player apps too. Issue 1502 rooted its family at `fabricate-icon-button`, which the primitive emits as the leading token of its own `classes` array, so of its 22 family selectors, 16 are rooted at `fabricate-icon-button`, one at `fabricate-pagination`, and five name a caller's own container under `.fabricate-manager` and are exempt. It declares its own focus PAIR — the `:focus` strip at `fabricate.css:13990` and the `:focus-visible` repaint at `fabricate.css:14003` — rather than borrowing an area's, and emits `data-keyboard-focus=\"true\"` so Foundry's `KeyboardManager#hasFocus` recognises it outside a form. 35 importers: 33 under `apps/manager/**` plus `ActionMenu.svelte` and `Pagination.svelte`, which is why it is EXERCISED in the player apps and not merely capable of it. ITS SIZE CLAIM ABOVE IS UNCHANGED BY ISSUE 1504 and its RADIUS gained a second value: the shipped control takes 6 everywhere except inside `.manager-pagination`, where the pager's nine instances moved onto the specimen's published 7 so the arrows match the converted 30px page-size trigger beside them. That is a new divergence rather than a resolved one — `library.html:606` and the ruled register's new \"Icon button radius\" row both carry it — and the 73 other sites keep the 6 they were extracted with, for the reason the size divergence gives."
    },
    {
      "path": "src/ui/svelte/components/IconPicker.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "The shared icon picker (9 importing files, issue 1269) — `InlineVocabularyAdd`, `VocabularyPanel`, `EnvironmentsBrowserView`, `system/CharacterPrerequisitesCard`, `essences/EssenceBulkEditPanel`, `essences/EssenceIdentityTab`, `scoped/WorldEssenceEntryPage`, `world/WorldCurrencyTab` and `world/WorldModifiersTab`. The 8 recorded here at issue 1269 was never re-measured. `library.html:786` names icons FIRST among the catalogues collapsing into `<SearchPopover>`; same reasoning as `EssenceSourceSelector`. Carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because everything it presents exists only in the open popover and neither representative frame opens one. ISSUE 1458 MEASURED THE COLLAPSE AND LEFT IT; ISSUE 1503 MADE IT, and the five capabilities that row named as ones the primitive LACKS are each addressed by a named mechanism rather than by a redesign. The PINNED resolved row above an alphabetical list of ~1000 (without which the picker always opened on `abacus`) is a caller-ordered `options` array plus a per-option `option.class` string and the `filterOptions` seam — the seam is what lets the caller see the query the primitive now owns, and it also carries the alias tables and the result ranking a plain label-substring filter would have silently dropped. `horizontalAlign: iconOnly ? 'left' : 'right'` is a prop, defaulting to the `left` that was hard-coded. The measured `listMaxHeight` is `measureListMetrics` over `anchoredPopover`'s `targets.list`, so the callback is handed the panel, the list and the search field the primitive now owns. The `style` on the trigger for the colour swatch, the `oncontextmenu` and the `onkeydown` pass-through are all the caller's own, because the caller keeps its whole button in a `trigger` snippet and the primitive's attributes are spread onto it LAST — with the keydown composed after the primitive's rather than replacing it, which is what stops a caller silently deleting the focus model. The `.essence-icon-picker-preview` tile the rows wrap the glyph in is drawn by the caller's `option` snippet. `library: null` still, because `<SearchPopover>` is the panel this now renders THROUGH rather than a specimen this component implements: it keeps its own trigger, its own row content and its own class family, and the family is preserved rather than folded because the View Lab case registry's own walk, the live Foundry smoke and about thirty assertions address `.essence-icon-picker-trigger` by name — renaming them would edit all three to change nothing a GM sees, and belongs to the family-rename child alongside `manager-travel-*`."
    },
    {
      "path": "src/ui/svelte/components/InspectorCard.svelte",
      "library": "<Card>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "THE manager's card shell (19 callers, issue 1427), extracted from 80 hand-written `class=\"manager-inspector-card\"` sections across 20 components — every one of them a `<section>`, measured by walking each component's Svelte AST, so it takes no polymorphism prop. `library.html:1016` heads one specimen `<Card> <Well> <RowDisclosure>` and states \"two container levels only; a card sits on the page, a well nests inside a card\" — this is that card, and the entry was in the specified-but-unbuilt register until it shipped. Two divergences from that specimen are deliberate and recorded rather than smoothed over: the shipped box is radius 8 with `--fab-space-3` padding on `--fab-overlay-light-04` (`fabricate.css:13779`) against the specimen's radius 11 / padding 16 / bg-2, and the sheet declares three further treatments keyed on a modifier class (`.manager-checks-card`), on an ANCESTOR (`.manager-checks-rail`, which reaches cards carrying no modifier at all) and on a positioning state (`.is-sticky`, which — measured across `src/` — no component writes at all) — so it takes NO variant prop, because a vocabulary spanning three different anchors is a design ruling and not something to settle inside a sweep whose acceptance bar is that no frame moves. `<Well>`, the level below it, remains unbuilt. Broad by reach: the Checks Studio, both environment inspectors, the essence and tool inspectors, the books-and-scrolls inspector, the Tool Studio authority card and the shared bulk-delete card all render it, and the deferred root holds 32 more sites. It carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because NEITHER representative frame contains a card at all. Issue 1508 rooted its family at `fabricate-card`, emitted as the leading literal of its own `classes` array: both selectors the gate judges owned — the flex stack at `fabricate.css:13035` and the box cited above — are compounds on that root, the Checks Studio’s `.manager-checks-card` treatment is a caller-CLASS compound that carries the root too rather than be split from the rule it out-ranks, and the other four name a caller’s own rail or inspector and are exempt. It declares NEITHER a font floor NOR a focus pair, and that is a recorded decision rather than an omission: it renders a section and owns no control of its own, so chrome declared here would displace an area’s chrome for a control the primitive does not own. A negative control asserts that no rule under this root declares either half."
    },
    {
      "path": "src/ui/svelte/components/Kicker.svelte",
      "library": "<Kicker>",
      "status": "shipped",
      "evidence": "broad",
      "scope": "shared",
      "why": "THE UPPERCASE MICRO-LABEL THAT NAMES WHAT FOLLOWS (issue 1505). `library.html:955` heads its block `<Kicker> <IconChip> <StatBox>` and `library.html:967` states its API; the geometry is `library.html:120` with ONE correction made in the same change, the tracking `0.11em` rather than `.14em`, on two independent issue-1373 measurements and the argument that no measured surface anywhere pairs 8.5px with `.14em`. This claims the entry at the same granularity `Chip` claims `<IconChip>` from the same three-name block. 18 importing files measured by `componentImporters.js` over 29 call sites: eight crafting files in one directory, the recipe-item editor's Overview tab, `StatBox.svelte`, which COMPOSES it because `library.html:963`'s specimen draws the stat box's label as the kicker itself, six gathering files added by issue 1514's second phase, and two more from its third - `apps/alchemy/Workbench.svelte` at three call sites and `apps/journal/JournalCard.svelte` at one, which is the shared chrome for four of the Journal's right-column cards. Those six are the MEASURED cases rather than the candidate list: `LinkedScene.svelte`'s label was refused because it is an `inline-flex` row laying out its own glyph child, which this component declares no `display` to reproduce. THE THIRD PHASE REFUSED THREE OF ITS SIX CANDIDATES, on three grounds and none of them size. `apps/journal/StepDetails.svelte`'s two section titles are `<h4>` and the host set is `{p, span, h3}` with a SILENT `p` fallback, so converting would drop two headings out of the document outline. `apps/journal/RunDetail.svelte`'s results heading paints `--fab-success-text` inside a success-soft well, and `apps/alchemy/Workbench.svelte`'s still-needed label paints `--fab-info`, where this component's tones are `default` and `accent` alone - so the plan's claim that every candidate paints `--fab-text-muted` is false at two of the six, measured. The one candidate that DID need a caller-owned wrapper is the Produces label, whose rule's last declaration was `margin: 18px 0 10px` (measured 18/10 in the View Lab) and whose `<div>` host takes the `p` fallback. Its line box moves 11.875 to 11.05; `JournalCard`'s moves 12.00 to 11.05, which is -0.95px and not the -3.95 the plan's arithmetic predicted, because a bare heading there computes `line-height: 12px` rather than the inherited 1.25. That pair spans a player app and the manager, which is the portability claim this programme turns on. Broad because it is a `components/` file, which is a leg of the pinned `BROAD_SIGNAL_PATTERN` source; it carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because neither representative frame draws an eyebrow this component renders. It is an IMPORT-FREE LEAF and it is TWO RUNGS DOWN in one tree, so a mounted suite that renders `ShoppingList` or `ItemPageInspector` pulls it in without naming a kicker anywhere — verbatim the `ActionMenu` shape that first reported as `# cancelled` with no failures on a suite whose tests never ran, which is why every harness that reaches either file names it. It takes no `class`, no `style` and no rest spread: a caller needing layout keeps its own wrapper and nests this inside it. `tests/kicker-source-contract.test.js` holds the contract class `fab-kicker`, a token no other file writes, against a one-entry exemption table. 23 IMPORTING FILES over 34 render sites as of issue 1514's fourth phase, which added five in the inventory tab and refused three. The three `.salvage-body-title` eyebrows were BYTE-IDENTICAL copies of one rule in three files — the drift this component exists to end — and each keeps its own `<p>` as a caller-owned flex row with `as=\"span\"` nested inside it, because each places a mono DC or a trailing hint on the label's own line. Their line box moves 10.00 to 11.05, the `line-height: 1` band GROWING rather than shrinking, and the bulk report's roll label moves 11.875 to 11.05. ONE MEASURED HAZARD came out of that, and it is general rather than local: the eyebrow rule set `text-transform` and `letter-spacing` on the ROW, so a sibling span inherited both — `.salvage-body-hint` fell from 89.52px to 55.64px wide because \"ROLL TO RESOLVE\" had silently stopped being uppercase, and the two declarations are restated on the sibling's own rule. THREE REFUSALS, none about size: `SalvageToolRequirements`'s eyebrow is an `<h4>` carrying an `id` its section names in `aria-labelledby`, and this component renders `{p, span, h3}` and forwards no `id`; `InventorySystemSelector`'s is a `<label for>` bound to a select, routed to the player-selects issue; and the shell's published `.inventory-detail-section-title` family converts together or not at all, which one of its eight consumers blocks by putting a modifier CLASS on the element this component would replace. 24 IMPORTING FILES over 37 render sites as of the FIFTH phase, which added ONE file at THREE call sites and refused one. `apps/crafting/ShoppingList.svelte` heads its queue, its components and its tools card with the same rule three times over, which is the drift this component exists to end; each keeps its `<p>` host, so no heading enters the player app's outline, and the line box moves 13.75 to 11.05 with the tracking 0.55px to 0.935px and the ink `--fab-text-muted` to `--fab-text-subtle`. The refusal is NOT this change's to make and is recorded so it is not re-litigated: `crafting/detail/StepRequirementsList`'s step label was measured and refused at issue 1505, because it is the step's HEADLINE and the 8.5px subtle rung ranked it below the \"Requirements\" label beneath it and below the 10px duration chip to its right. Re-measured here at 12.00/15.00, unmoved. REVIEW ROUND 1 CORRECTED THE INK, which is the SECOND correction to `library.html:120` and is made in the specimen as well as in the component. Every conversion above was taken with the specimen ink `--fab-text-subtle`. At 8.5px this is the smallest type the product draws, and SIX of the seven palettes declare `muted` and `subtle` as ALPHAS over the surface, so on the default `fabricate` theme that composites to 3.69:1 on `--fab-surface` and 3.50:1 on `--fab-surface-soft` - under the 4.5:1 small-text floor. The component paints `--fab-text-muted` now: 5.42:1 and 5.00:1 at those two grounds, clearing the floor in all seven palettes with `ironblood-forge` worst at 5.19:1 and 4.77:1. ONE palette hid it - `mythwright` alone states those two tones as opaque hues, and it is the only palette of the seven whose subtle tone clears the floor at all, reading 5.28:1 from the SAME declaration - which is why the reduction was recorded as accepted rather than caught. Six fail and one passes, so the specimen's figure was set from the single outlier. Corrected on the component, so it moves the two sites no phase converted as well: the manager recipe-item Overview tab at three call sites, and the label `StatBox` composes, which reaches the manager item page and the crafting shopping summary. The `classOnlyRemedy` sentence in `tests/kicker-source-contract.test.js` names the new ink. 24 importing files over 37 render sites, unmoved by the correction."
    },
    {
      "path": "src/ui/svelte/components/ManagerButton.svelte",
      "library": "<Button>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "`library.html:581-582` — one component, six roles, two sizes, each role naming a VERB. The shipped `role` prop is that closed set, made required-shaped precisely because the CSS-convention version drifted. 64 importers, the most of anything under `components/`: 63 under `apps/manager/**`, and `components/SearchablePopover.svelte`, which renders it only for a caller passing `triggerButton` and no caller outside the manager does — which makes this the only one of the three re-rooted button-family primitives that is a pure manager capability rather than a control the player apps also exercise. Issue 1502 rooted its family at `fabricate-button`, emitted as the leading literal of its own `classes` array, so of its 109 family selectors, 81 are now compounds on the class the primitive writes; the remaining 28 are exempt — 27 name a caller's own container under `.fabricate-manager`, and one is a per-view override whose application root is qualified by an attribute. It declares its own focus PAIR — the `:focus` strip at `fabricate.css:13637` and the `:focus-visible` repaint at `fabricate.css:13648` — rather than borrowing an area's, and emits `data-keyboard-focus=\"true\"` on its `<svelte:element>` root."
    },
    {
      "path": "src/ui/svelte/components/ManagerSearchField.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "THE manager's search field (20 callers, issue 1039), extracted from 23 hand-written `class=\"manager-search\"` labels — every one of them a `<label>` wrapping an `<i class=\"fas fa-search\">` and an `<input type=\"search\">`, measured by walking each component's Svelte AST, so it takes no polymorphism prop and no icon prop. `library:null` is a judgement, not an omission. `library.html:666` specifies `<Search>` as ONE OF THREE modes SHARING ONE SHELL with a kicker label above the field, at height 38 / radius 9. The shipped pill is not that shell: it is 34 / radius 6 (`fabricate.css:5582`), it carries no kicker and no label prop — its accessible name is an `aria-label` on the inner input at all 20 sites — and the manager's select mode is a separate treatment (`.manager-filter select`, `:5614`) this component does not touch, while the shell itself now ships as `Field.svelte` claiming `<Field>`. Recording the correspondence would assert that the shipped tree has the one shell the library specifies, which it measurably does not, so the entry stays in the specified-but-unbuilt register and the adjudication belongs to whichever change unifies the three modes. Same call, and the same reason, as `EditorValidationSurface`. Three of the 23 sites are ADJUDICATED OPT-OUTS rather than deferrals: `GatheringTaskEditView.svelte:1747` and both `CraftingSystemManagerRoot.svelte` sites render a `.manager-tag-suggestions` typeahead list inside the label, which makes them comboboxes belonging to `SearchablePopover`, and the root's two additionally take `bind:this` on the label for popover positioning — which on a COMPONENT tag binds the component instance, not its host element. Broad by reach: every browse screen plus four editors and three rosters. It carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because the representative pair reaches only the base density. Issue 1508 rooted its family at `fabricate-search`, emitted as the leading literal of its own `classes` array: 7 of its 31 family selectors are owned compounds on that root and the other 24 name a caller’s own toolbar, roster or library card and are exempt. THREE of those 24 changed ANCESTOR rather than root — from the `[data-manager-tools-search]` attribute to the `manager-tools-library-card` class the caller writes on that same element, at unchanged rank and unchanged position — because with the attribute standing second the family compound stands third and neither re-rooting form is available. The match set is identical and measured: the attribute occurs on exactly two elements repo-wide and both carry that class. It owns its own search `input`, so it declares a (0,1,1) font floor over `.fabricate-search input` and a focus pair over that same element. `library: null` is unmoved by the re-root, and the note a `library: null` row is owed sits in the library’s Planned migrations table rather than under a specimen this row is recorded as not implementing."
    },
    {
      "path": "src/ui/svelte/components/ManagerToolbar.svelte",
      "library": "<FilterBar>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "THE manager's filter bar (11 callers, issue 1039), extracted from 11 hand-written `class=\"manager-toolbar\"` sections — every one of them a `<section>`, measured by walking each component's Svelte AST, so it takes no polymorphism prop. A raw grep says TWELVE and is wrong: `GatheringTaskEditView.svelte:1784` is a `.manager-toolbar-pills` chip row on a `<div>`, and `\\b` matches before a hyphen. `library.html:1373-1374` heads `<Pagination> <FilterBar>` and `spec.md`'s browse archetype names \"the filter bar\" as a fixed element of every browse screen; this is that bar, and the entry was in the specified-but-unbuilt register until it shipped. THREE divergences from the specimen are recorded rather than smoothed over. (1) The library's FilterBar API owns its controls as DATA — `query`, `toggles`, `selects`, `segments`, `sort`, `result`, `onClear` — and the shipped bar owns only the CHROME and takes its controls as children, because the corpus's filter control is three different things (a `<label class=\"manager-filter\">` wrapping a `<select>`, 16 sites; a bare `<select>` carrying its own accessible name, 2; and `<SegmentedControl>`, 2) and no bar is in a position to choose between them. (2) The sketch specifies ONE component owning both bar and query, and the corpus has TWO: 13 of the 23 search fields render inside no bar at all, so a field folded into the bar would have forced a bar around each of them. The field ships as `ManagerSearchField.svelte`, recorded separately below. (3) It takes NO variant prop — the sheet declares four further treatments anchored four different ways, two of them scrolling caps (`.manager-environments-toolbar`, `.manager-task-toolbar`), one sized from a SCOPED rule in `scoped/EntityListInspectorFrame.svelte` rather than from the sheet at all, and one (`.manager-toolbar:not(:has(.manager-toolbar-primary))`) a branch that is ALWAYS taken because — measured across `src/` — no component writes `.manager-toolbar-primary`, so the grid form at `fabricate.css:5531` is declared and never rendered and the branch that displaces it, `fabricate.css:5539`, is the one every bar actually takes. Broad by reach: every browse screen in the manager renders it. It carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry because the representative pair reaches only the base treatment. Issue 1508 rooted its family at `fabricate-filter-bar`, emitted as the leading literal of its own `classes` array: 3 of its 10 family selectors are owned — the shared box at `fabricate.css:5518`, the grid form and the `select.is-size-38` rung at `fabricate.css:26843` — two more are caller-CLASS compounds on `:not(:has(.manager-toolbar-primary))` that carry the root for the same reason, and the remaining five name a caller’s own view or scoped list and are exempt. It declares NEITHER a font floor NOR a focus pair, on the same ground the card row states: it renders a section and owns no control of its own. The `select.is-size-38` rung is the recorded residue of that decision — it travels with the family and paints a caller’s select in a bare host with no floor beneath it."
    },
    {
      "path": "src/ui/svelte/components/ManagerColorPopover.svelte",
      "library": "<TintPicker>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "`library.html:799`. 4 callers. The tint swatch panel; its sibling `ManagerColorPicker.svelte` is the single-caller trigger and is recorded in `NOT_A_PRIMITIVE`. `target` rather than `shipped` because that entry states geometry with no Svelte API section: a specimen that names no props gives nothing to match."
    },
    {
      "path": "src/ui/svelte/components/Medallion.svelte",
      "library": "<IconChip>",
      "status": "shipped",
      "evidence": "broad",
      "scope": "shared",
      "why": "The app's ONE art tile: a record's linked image drawn flat on `--fab-bg-3` with a glyph fallback, at 53 importing files and 77 render sites. Twelve of those sites arrived with issue 1514's third phase - the whole of the alchemy and journal tabs' raw thumbnails - and ALL TWELVE converted: the `.is-fallback` obstruction that deferred five of the seven gathering tiles in the phase before has no instance in either tab, read off every one of the eleven `object-fit` rules covering them. Five of the twelve pass `glyph`, `tint` and `icon` explicitly, because the alchemy markup they replaced carried a real `{#if img}`/`{:else}` branch drawing `fa-flask` at the row's own inherited size in `--fab-tag-peach`; a tile taking this component's `fa-scroll` default at `0.9rem` in the accent would have kept the right BOX and changed the face, the size and the ink. It absorbed both crafting thumbnails at issue 1506, which is why `library` moves from `null` to a named entry and this row leaves the undocumented register. `library.html:955` heads its three-name block, `:959`-`:961` draw the four rungs and `:968` states its Svelte API — which is why `status` is `shipped`. THREE THINGS THE SENTENCE THIS REPLACES GOT WRONG, corrected together: it said 13 callers; it cited an anchor for the avatar entry that is not that entry; and it ruled the correspondence out on the ground that this tile \"renders a record's linked image with a glyph fallback\", which `library.html:2003` makes the CORRESPONDENCE rather than the objection — the ruled-out register records the thumbnail tile as `art` on this very entry. The two art entries are separated by what the mark is ABOUT and not by the fallback: this carries a record's artwork and `components/Avatar.svelte` carries an ACTOR's, which is what section 14's routing row states. Broad through the `components/` leg, with a `BROAD_SIGNAL_CASE_OVERRIDES` entry naming the one published frame that draws a TINTED glyph-chip tile — the state neither representative frame reaches — which is how it leaves `PRIMITIVES_WITH_NO_FRAME`."
    },
    {
      "path": "src/ui/svelte/components/ModifierPillSelect.svelte",
      "library": null,
      "status": "target",
      "evidence": "broad",
      "scope": "manager-only",
      "why": "A dropdown-plus-removable-pills multi-select (2 callers, issue 770). `<SetPicker>` (`library.html:1410`) specifies editing membership of a set too large to render inline, and states it never dumps the whole set into its panel — this control renders every unselected option in a listbox, so it is not that primitive. Its ADD MENU is `SearchablePopover` since issue 1458, which is why the pill row and the menu are now two different things rather than one hand-rolled widget; the conversion needed the primitive to absorb `triggerAriaDisabled`, the at-cap state that must refuse to open while staying focusable, because the post-removal focus fallback lands on that very button and `focus()` on a `disabled` one silently no-ops. It carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry as of that change: `manager-recipe-edit-crafting-modifier-cap-reached` is the one frame that draws its single remaining painted rule, the at-cap trigger treatment. Issue 1508 re-rooted six primitive families and DEFERRED this one, recorded here rather than left as an absence. Its family is not its own: every class it writes — `manager-availability-multi`, `-menu-button`, `-pill-row`, `-pill`, `-remove` and `-any` — is hand-written by six manager views at 37 further sites, and one of those, the required-tools pill row in `GatheringTaskEditView.svelte`, sits inside `.manager-task-required-tools-attached` with no `manager-availability-multi` ancestor at all, so no root class placed on a `ModifierPillSelect` could reach it. Rooting the family at a class only this component emits would un-style all 37, and the gate has no residue mechanism — its owned-and-app-rooted set is asserted EMPTY — so an entry leaving one such selector standing reds rather than records. Issue 1515 converts those sites to the chip’s removable form and the re-root belongs after it; issue 1508 touched this component for one edit only, the `data-keyboard-focus` attribute on its remove button. Scoped `manager-only` DESPITE its shared-directory path, and the deciding change is named above rather than left implicit: the family it writes is hand-written by six manager views at 37 further sites, so it cannot be re-rooted — and therefore cannot be rendered outside the manager without going unstyled — until issue 1515 converts them. Issue 1515 is what flips this row to `shared`."
    },
    {
      "path": "src/ui/svelte/components/Notice.svelte",
      "library": "<Notice>",
      "status": "shipped",
      "evidence": "broad",
      "scope": "shared",
      "why": "THE SURFACE THAT REPORTS SOMETHING THAT JUST HAPPENED (issue 1505). `library.html:1037` heads its block and `library.html:1062` states its API — which this component follows VERBATIM rather than re-designing, including its spellings, because that list was the specification before it was the record. Geometry from `library.html:221-230`. 4 importing files measured by `componentImporters.js` over 5 call sites, twice the membership bar: `apps/alchemy/Workbench.svelte`'s brew banner, `apps/inventory/bulk/InventoryBulkReport.svelte`'s run banner, and two gathering surfaces added by issue 1514 - `apps/gathering/GatheringTaskDetail.svelte`'s two node banners, which routed here rather than to `Callout` because both carried `role=\"status\"` and `Callout` emits only `role=\"note\"` or nothing, and `apps/gathering/GatheringTaskDrops.svelte`'s newly surfaced drop-breakdown fetch failure. FOUR RECORDED DEVIATIONS, each written into the library entry: an `accent` tone, because the bulk report derives it for the destroy run and the mixed outcome and three published frames draw those states; `tabular-nums` on the detail, over a live count sentence; an `icon` prop, because those two accent states resolve to the same tone with different glyphs; and the `dataAttr`/`dataValue` hook pair with a SECOND `stateDataAttr`/`stateDataValue` pair, because the alchemy banner carries two hooks on one root. `blocking` drives the announcement — `role=\"alert\"` when blocking, and `role=\"status\"` with `aria-live=\"polite\"` otherwise, because the ROLE carries it: a live region created in the same mutation as its content is not announced, and both shipped callers insert this component together with its text — and the PAGE-LEVEL arbitration the specification also states is explicitly NOT claimed as met: the shared region that enforces it is issue 1517's. Broad through the `components/` leg of `BROAD_SIGNAL_PATTERN`, with a `BROAD_SIGNAL_CASE_OVERRIDES` entry because neither representative frame is an inventory or an alchemy screen. Its alchemy caller reaches no published frame at all today, so that conversion is recorded UNPHOTOGRAPHED and is held by `tests/components/alchemy-workbench-mounted.test.js`, which acts on all four tones that caller derives. 6 IMPORTING FILES over 7 render sites as of issue 1514's fourth phase, every one of them in the player window. The two the inventory tab adds are both `role=\"status\"` banners that route here rather than to `Callout` for the reason this row already states: `apps/inventory/detail/InventoryComponentDetail.svelte`'s read-only broken-tool banner (`tone=\"danger\"`, measured 406.78x35.25 to 406.78x41.00 as the padding goes 8/10 to 12 and the corner 8 to 11) and the whole of `apps/inventory/detail/salvage/SalvageMisconfiguredBody.svelte`, which was already a title-over-detail warning well and becomes one outright, passing `fas fa-screwdriver-wrench` rather than taking the default alert triangle because a configuration fault a GM fixes is not a hazard the player is walking into. A THIRD inventory banner was measured and REFUSED: `InventorySalvagePanel`'s success ribbon is the tone's own worked example and its trailing slot holds an underlined inline link in one state and a bare `<span>` in the other, where this component has one `action` button and NO children slot. A children slot, or a `link`-shaped action, is what would close it. 8 IMPORTING FILES over 9 render sites as of the FIFTH phase, which added two more, both in the player window and both routed here by the role they already carried. `apps/crafting/RecipeDetailHeader.svelte`'s blocking-reasons well was a `role=\"status\"` element switching between `--fab-warning-*` and `--fab-danger-*` on an `is-uncraftable` class, and both tones are this component's own — measured 450.30x42.25 to 450.30x41.00 with the corner 8 to 11, the glyph 14px to 13px and the sentence 13px/400 to 12px/600. Its `<ul>` is what made the conversion a question rather than a swap: this component takes `title` and `detail` as STRINGS and has no children slot, and `CraftingListingBuilder._blockingReasons` returns `key ? [localize(key)] : []` — at most ONE reason for every browse status there is — so the bulleted list was always a one-item list, and a second reason lands in `detail` rather than being dropped. `apps/FabricateAppRoot.svelte`'s companion-fault strip is the FIRST `blocking` caller: it carried `role=\"alert\"` already, and `blocking` is the only prop that keeps it. Its docblock refused `Callout` on three grounds and the commit restates which one survived — the `<p>` root objection is false (`Callout.svelte:122,129-131` renders a `<div role=\"note\">` when structured) and the two-tones objection is false (`Callout.svelte:105` lists six); what stands is that a callout is a standing statement and this is an announced error state. The tone is a PUBLISHED FRAME MOVE rather than a preserved one: the deleted rule drew a NEUTRAL band, `--fab-border-strong` over `--fab-surface-raised`, with only the glyph in the danger ink, and `tone=\"danger\"` paints the whole band — measured 560.00x85.09 to 560.00x77.09, unmoved in width because the `max-width: 560px` and the 20px margin stayed on a caller-owned wrapper. REVIEW ROUND 1 ADDED THE NINTH IMPORTER AND SPLIT ONE STRING, leaving 9 importing files over 10 render sites, every one of them in the player window. The `apps/PlayerViewState.svelte` error branch moves here from `Callout` on the routing rule the library states in four lines: a callout is documentation and a notice is state, a view that failed to load is state, and the companion fault strip in `FabricateAppRoot.svelte` had already reached that conclusion in this same change. It also gains what the callout could not carry - `role=\"status\"` with `aria-live=\"polite\"`, which a failure appearing without a focus change wants and which the strip it replaces had no role for at all. The SPLIT is the second correction and it is about this component slots rather than its routing: `title` is 12px/600 in the tone ink with no declared `line-height`, which is a HEADING slot, and the gathering drops failure handed it a 120-character two-sentence string that rendered as a three-line shouty heading. The sentences already split at the seam this component draws - what went wrong, then what to do next - so the second becomes `detail`, and `lang/en.json` gains `DropsErrorDetail` for it."
    },
    {
      "path": "src/ui/svelte/components/Pagination.svelte",
      "library": "<Pagination>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "Named at `library.html:1373-1374`. 25 importers — 19 across every browse surface in the manager, and SIX in the player apps (`apps/crafting/RecipeBrowser`, `apps/inventory/InventoryGrid`, `apps/journal/HistoryList` and three gathering panels), which is what makes it a primitive EXERCISED in both applications rather than a manager capability. Issue 1502 rooted its family at `fabricate-pagination`, written inline on the root `<section>` because this component composes no class list, so its seven owned selectors no longer descend from `.fabricate-manager`; it declares a button-only focus pair, the `:focus` strip at `fabricate.css:5717` and the `:focus-visible` repaint at `fabricate.css:5735`, deliberately not `:is(button, select)`, which would tie `.fabricate-app select:focus-visible` and delete the inset ring that exists to stop a clipped outline on the per-page select. The re-root widened the sheet's reach into all six player callers, so each one restates every newly-painted longhand at the value it rendered before — the audit is in `Pagination.svelte`'s docblock. ITS PAGE-SIZE LIST IS A `<Select>` AS OF ISSUE 1504, and three things moved with it. The rule that painted the native control is DELETED and only its `min-width: 64px` survives, re-hung on the `[data-pagination-size]` hook the trigger now carries — by ATTRIBUTE rather than by the trigger's class, because the area-scope gate reads another primitive's `fabricate-` namespace as an application root; the six non-manager pagers refuse the floor in their own blocks, exactly as they refused its predecessor. The list is UNTICKED (`showTick={false}`): the trigger already states the page size and the list is four short numbers, so a tick column would be a gutter carrying no information. And the accessible name is now the words a GM can actually see — the visible \"Per page\" caption, reached by `aria-labelledby`, rather than an `aria-label` reading \"Rows per page\" that no reader and no screen agreed on; the `PerPageLabel` key retired with the control it named. The prev/next `<IconButton>`s move from radius 6 to the published 7 at seven sites and nine pager instances, so the pair reads as one matched control beside a 30px trigger — a divergence this manifest's `IconButton` row and `library.html:2040` now both record."
    },
    {
      "path": "src/ui/svelte/components/Select.svelte",
      "library": "<Select>",
      "status": "shipped",
      "evidence": "broad",
      "scope": "shared",
      "why": "THE app's one select, and the primitive that stopped three shared controls drawing an operating-system drop-down (3 importers, issue 1504). `library.html:666` heads its block `<Field> <Select> <Search>` and specifies the control shell, the option list, the `value`/`onChange`/`options` API and the configurable tick; all four ship here, so `<Select>` leaves the specified-but-unbuilt register `<Field>` left at issue 1428 while `<Search>` stays in it — which is why the block's own `data-status` is still `target`.\n\nIT IS COMPOSED, NOT BUILT. `SearchablePopover` with `showSearch={false}` and `triggerHasPopup=\"listbox\"`, using the primitive's OWN trigger rather than a `trigger` snippet, so the combobox contract, the listbox key model, the option ids, the portal and the attribute-omission rule are the primitive's throughout and not one of them is restated here. What this component adds over that surface is the SELECT's own shape: the option row (a tick column, the label, a hint line, a badge, and a disabled row's reason drawn INSIDE the button so a screen-reader user is told why as well as that), the group list DERIVED from the distinct `option.group` values in first-appearance order, the three size rungs, and the `<Field as=\"label\">` labelled form. 23 declared props, and `Pagination`'s numeric page sizes are the reason two of the derivations exist: `String(value)` is forwarded beside `id: String(option.value)` so `aria-selected`'s strict equality holds, and every option carries a NON-EMPTY `dataId` — `__unchanged__` for the empty-string sentinel — so every rendered row has an identity handle a capture step, a mounted test and the Foundry smoke can address, including the default row a truthiness test would have left unhooked.\n\nITS FAMILY IS IN THE GLOBAL SHEET AND THE COMPONENT HAS NO `<style>` BLOCK AT ALL: 34 `.fabricate-select-*` rules in `styles/fabricate.css`, three of them CALL-SITE overrides on the trigger box — the bulk panel's full width, and the two the scoped catalogue toolbar keeps from issue 1371 (its opt-in 38px lead rung and its secondary ink). A scoped rule compiles unlayered against a sheet imported at `layer(modules)` and injected before it, so it silently out-ranks every global rule and is invisible to `tests/components/searchable-popover-area-scope.test.js` — the one gate that proves a shared family is not rooted at a single application. That gate's ninth entry is this family, and it is the first whose every class carries the `fabricate-` prefix the gate otherwise reads as an application root, which is why the entry declares nineteen namespace roots, the two roots it INHERITS from `SearchablePopover`, and its family as a pattern.\n\nA THIRD SIZE RUNG THE SPECIMEN DOES NOT NAME. `toolbar` — 34 / radius 9 / `0.72rem` / weight 500 / fill `--fab-bg-1` — sits beside the specimen's `form` (38 / 9 / 12.5px) and `inline` (30 / 7 / 11.5px). The scoped catalogue's toolbar is a shipped 34px control line whose search field, direction toggle and segmented control this change does not move, so a converted sort key on the 38px rung would have stood four pixels proud of its own row. 34, 9 and 500 are published ladder and ramp values, so the rung costs no debt. Its type is a LITERAL rather than `var(--fab-recipe-control-font)` because that property is declared only under `.fabricate-manager` and a primitive rooted outside every application may not read one; outside the area it is undefined and the declaration falls back to inheritance. Its weight is a ramp numeral because the shipped rule declares none and `normal` is off-ramp — which makes the weight a REAL visual difference on that row, since its three neighbours still compute 400. Issue 1523 owns collapsing the rung and equalising the row.\n\nSEVEN DIVERGENCES FROM THE SPECIMEN'S SURFACE, each with its reason. (1) `size=\"toolbar\"` above. (2) The FILL is stated per size as a token, because the specimen's own \"surface-soft in cards · bg-2 inside bg-1 rows\" is CONTEXTUAL and none of the three shipped call sites is either context. (3) `minWidth`/`maxWidth` are declared per size, because the shared panel's 240px default is wider than a two-digit page-size list AND is floored by a sheet rule the primitive has to override. (4) `ariaLabelledBy` exists, and `id`/`name` are satisfied STRUCTURALLY by the `<Field as=\"label\">` containment plus an `aria-labelledby` to the caption rather than by a `for`/`id` pair — a `<label>` names only labelable elements and this trigger is a `<button>`; `name` is emitted on that button, where it is valid and inert. (5) `triggerData` is part of the declared surface, because three shipped call sites drive their control by a `data-*` hook that the case registry, 84 test call sites across 16 files and the Foundry smoke all address, and `...rest` lands on the `<Field>` root in the LABELLED form; the bare form has no home for it, because the primitive's own root takes no attribute spread, so it warns and names `triggerData`. (6) The option shape carries a `disabled?` the specimen does not list, beside the `disabledReason?` it does. (7) THE TRIGGER'S BOX IS STYLED FROM THE CALL SITE, not by a prop: a pager's own fill and its own 64px width floor are stated as descendant rules from the caller's surviving wrapper class, because the trigger's box belongs to the row it sits in and six of the seven pagers want a different one from the manager's.\n\nDECLARED WITH NO CALLER ON THIS COMMIT, and held by test rather than by a frame: `mono`, `icon`, `label`, `hint`, `error`, `placeholder`, `invalid`, `readonly`, and the option shape's `badge`, `disabled` and `disabledReason`. The badge and the unavailable reason are two of the five affordances `spec.md` names as forcing an app-drawn list, so they are built and proved here rather than deferred to the first caller that wants one.\n\nEVIDENCE IS BROAD, and judged rather than inherited from the directory. It reaches `BROAD_SIGNAL_PATTERN` through the `components/` leg, and the reach is real: three importers, but through `Pagination` it lands in 25 files across both applications and through `BulkEditSelect` in the recipe studio's two staged axes. It carries a TWO-entry `BROAD_SIGNAL_CASE_OVERRIDES` entry because neither representative frame opens a panel at all — `manager-recipes-bulk-edit-check-tier` photographs the ticked, grouped, hinted list and `player-inventory-page-size` the unticked one in the second application root."
    },
    {
      "path": "src/ui/svelte/components/SelectionCheckbox.svelte",
      "library": "<Checkbox>",
      "status": "shipped",
      "evidence": "broad",
      "scope": "shared",
      "why": "`library.html:690` — \"The box a gate list and a selection header both use\", including the tri-state a some-rows-selected header needs. 8 callers."
    },
    {
      "path": "src/ui/svelte/components/StatBox.svelte",
      "library": "<StatBox>",
      "status": "shipped",
      "evidence": "broad",
      "scope": "shared",
      "why": "THE AT-A-GLANCE FIGURE (issue 1505). `library.html:955` heads its block and `library.html:969` states its API; the geometry is `library.html:237-239` — `--fab-bg-1`, 1px `--fab-border`, r9, `var(--fab-space-3)`, centred, with a serif 17px/700 value — and the LABEL is a `<Kicker>`, composed rather than restated, which is what the specimen draws. 2 importing files measured by `componentImporters.js`, which is the membership bar itself, and they span both windows: `apps/crafting/ShoppingList.svelte`'s three summary cards and `apps/manager/ItemPageInspector.svelte`'s three Books & Scrolls tiles. FOUR RECORDED DEVIATIONS from the specimen, each written into the library entry: an `icon` prop, because the Shopping list renders a leading glyph on the value line in all three cards; `tabular-nums`, which both callers declare so a 9 to 10 change cannot reflow the tile; the `tone` prop at all, since `.k-stat` declares no tone variant, so each of `default`, `info` and `danger` is derived from a shipped caller state; and the four hook props, which the specimen has no test harness to need. `size`, `unit`, `tone=\"warning\"` and `tone=\"accent\"` are WITHDRAWN rather than carried, because no caller reaches them and no test acts on one. Those hook props — `dataAttr`/`dataValue` on the box root, `valueDataAttr` on the value and `labelDataAttr` forwarded into the composed `Kicker` — are what let the item-page conversion preserve all six of its existing `data-*` hooks verbatim, so not one of the nine assertions pinned on them moved. Broad through the `components/` leg of `BROAD_SIGNAL_PATTERN`, with a `BROAD_SIGNAL_CASE_OVERRIDES` entry because neither representative frame draws a stat grid. `tests/stat-box-source-contract.test.js` holds the contract class `fab-stat-box`, records the hook props as attribute-only, and requires every literal hook value at a call site to start with `data-`, which is what keeps its \"emits no interactive element\" clause exhaustive."
    },
    {
      "path": "src/ui/svelte/components/StatusToggle.svelte",
      "library": "<Toggle>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "`library.html:595` — \"Binary state that takes effect immediately\", and the manager's one on/off switch (25 callers, issue 1040). It was a CSS convention over an element TREE — track span, nested knob span, optional reading — hand-written at 37 sites in 26 components, and `scoped/InheritRow.svelte` and `scoped/MembershipActions.svelte` each recorded in their own docblocks that they had to. Carries a three-entry `BROAD_SIGNAL_CASE_OVERRIDES` entry because its `as` prop is a closed set of three element HOSTS and no representative frame draws even one of them: `fabricate-app-shell` is the player window and `manager-components-normal` holds only the track-only filter switch. The shipped 34x20 track against this ladder's 38x22 is recorded as a migration in the entry's own delta, not resolved here. Issue 1508 rooted its family at `fabricate-toggle`, emitted as the leading literal of its own `classes` array: 18 of its 25 family selectors are compounds or descendants of that root and the other seven name a caller’s own row or card and are exempt. TWO of the 18 are owned ONLY because the gate learned to read a declared class MAP — the checkbox host’s `manager-tool-setting-toggle` reaches the DOM from a frozen `HOST_CLASSES` object in the component’s script rather than from the class array or the markup, so an array-only reader reported the family clean while both of those rules stayed application-rooted. Its focus PAIR was re-rooted IN PLACE at unchanged (0,3,0) and unchanged declarations rather than replaced by a second pair at the root, and the checkbox host gained the strip it never had — declared on the transparent `input` that can TAKE focus rather than on the `label` its `:has()` ring paints, and costing zero pixels because that input resolves `opacity: 0`. Here the root IS the control, so its font floor is written at the family root ALONE, (0,1,0), the same form the two button families take."
    },
    {
      "path": "src/ui/svelte/components/Stepper.svelte",
      "library": "<Stepper>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "`library.html:551` — \"The most-used control in the app\", and 23 callers here agree. Carries a `BROAD_SIGNAL_CASE_OVERRIDES` entry naming the gathering economy actor frame, which is the only published frame that renders filled steppers, disabled steppers and a rolled-max placeholder at once."
    },
    {
      "path": "src/ui/svelte/components/ThresholdBandStrip.svelte",
      "library": "<RangeBar>",
      "status": "target",
      "evidence": "broad",
      "scope": "shared",
      "why": "`library.html:716` — a tiled strip of spans whose dividers are dragged, \"outcome tiers against a DC\", which is exactly this. Exactly 2 callers, `checks/CraftingCheckEditor.svelte` and `checks/SimpleCraftingCheckEditor.svelte`, and it carries a two-entry `BROAD_SIGNAL_CASE_OVERRIDES` entry because those are its two modes and each renders chrome the other does not."
    }
  ],
  "notAPrimitive": [
    {
      "path": "src/ui/svelte/apps/manager/SystemOverviewView.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/manager/SystemEditView.svelte"
      ],
      "why": "ADJUDICATED against `EditorValidationSurface` at issue 1444 and ruled a different surface AND a different kind of thing: a whole ROUTE, not a tab. Measured: `.manager-main manager-system-overview-main` (`:190`) with a `.manager-section-header`, a `.manager-chip-row` of `Chip` counts (`:209`) where the surface has a counts rail, and `.manager-system-overview-list` (`:259`) grouped by entity kind rather than by check category. It writes zero tokens of either family. One caller: `src/ui/svelte/apps/manager/SystemEditView.svelte`."
    },
    {
      "path": "src/ui/svelte/apps/manager/downtime/WorldDowntimeTabs.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/manager/downtime/WorldDowntimeExtensionHost.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/manager/downtime/WorldDowntimeExtensionHost.svelte`. That caller count is what puts the row on THIS list, exactly as it does for the other nine, and the maintainer's divergence ruling does not touch it: one caller is below the two-caller bar however the file is drawn. What the ruling does touch is the second half of the row, the reasoning that told the next audit not to re-propose converting it onto `EditorTabs` — and issue 1429 re-read that reasoning and found it DOES NOT SURVIVE. It is recorded here as CONVERSION-PENDING, not as a justified divergence. Issue 1038's ground (1) was `177 lines of its own scoped <style> ... so the component owns a layout, not just a tablist`, and re-reading the block splits it three ways. `.downtime-tab-card`, `.downtime-connected-studio`, its `strong`/`p` and `.downtime-connected-icon` author a CARD and a sibling identity block that are not the tablist at all; a host that wrapped `<EditorTabs>` would keep every one of them, so they show that the FILE owns a layout and show nothing about the strip. `.downtime-tabs`, the bare `button` rule, its `:hover`/`:focus-visible`/`.is-active` re-tone and the `@container (max-width: 720px)` collapse draw the tablist and its buttons as bordered, filled, 8px-radius pills instead of the shared `.manager-editor-tab*` underline — which is a STYLE divergence, and spec.md requirement \"Near-neighbour primitives are routed by a stated rule\" states that a deliberate style divergence is precisely what a shared primitive exists to remove. Only the padlock (`.downtime-tab-lock` plus the currency re-tone) and the visible hover/focus tooltip are per-tab CONTENT and BEHAVIOUR the primitive has no vehicle or slot for, and those are the functional-or-informational kind the ruling admits. Ground (2), `activeTabId` rather than `activeTab` and no `badges`, is a RENAME and was never a divergence. Ground (3) is real and behavioural — `focusableTabId` falls back to the first tab when `activeTabId` names no rendered tab, so the strip keeps a tab stop while `aria-selected` stays bound to `activeTabId` — but the ruling admits a functional difference as something the primitive ABSORBS AS A CAPABILITY, and `EditorTabs` binding `tabindex` to `activeTab === tab.id` means any strip whose active id names no rendered tab loses its tab stop entirely. That is a defect in the primitive, not a licence for a second strip. So conversion is owed, and issue 1429 deliberately did not attempt it: it needs four capabilities on `EditorTabs` (a per-tab lock affordance, a per-tab accessible-name override, a visible tooltip carrying `aria-describedby` with its own hover/focus state, and the focus fallback) plus hoisting roughly a hundred lines of scoped button and container-query CSS into shipped `styles/fabricate.css` classes, because a scoped rule left on the caller would match nothing once the button is rendered by a child. That is a larger change than the conversion it would accompany and it touches a shared stylesheet, so it is follow-up work rather than a silent extension. Targeted: the `manager-world-downtime-*` frames render it and claim it by `sourceMatches`."
    },
    {
      "path": "src/ui/svelte/apps/manager/environment/EnvironmentValidationTab.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/manager/EnvironmentEditView.svelte"
      ],
      "why": "ADJUDICATED against `EditorValidationSurface` at issue 1444 and ruled a DIFFERENT SURFACE rather than an unconverted one, so the next audit does not re-propose it. Measured: it renders `.manager-environment-card`, `.manager-environment-check-list` and `.manager-environment-issue-list`, carries no status medallion, no counts rail and no grouped row stack, and expresses severity through `Chip` (`:140`) rather than a `manager-recipe-val-pill`. It writes zero `manager-recipe-val-*` and zero `manager-recipe-rail-*` tokens, and each of its three classes appears in exactly one file. Converting it would be a visual REDESIGN of the environment editor, not an adoption. One caller: `src/ui/svelte/apps/manager/EnvironmentEditView.svelte`."
    },
    {
      "path": "src/ui/svelte/apps/ActorSelectTopBar.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/FabricateAppRoot.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/FabricateAppRoot.svelte`. This is an UNADJUDICATED SCREEN REGION and not a candidate that cannot work as a primitive, and TWO reasons keep it here, neither of which this row's history settles: at 506 lines it is a screen region rather than a primitive, and `<AppTitleBar>` (`library.html:1040`) specifies the strip above the page header and nav rail, but whether this bar implements that entry has never been decided. Both are open questions rather than objections, so a re-proposal here is answered by adjudicating the correspondence — a design ruling, not a measurement. Issue 1475 converted the actor picker inside the bar and changed neither reason: a screen region that reuses a primitive is still a screen region. The row's THIRD reason, a structural non-conversion recorded against that picker at issue 1458, is WITHDRAWN, and is written out rather than deleted because a row that quietly loses an argument is how this register drifted in the first place. It said the picker renders in the PLAYER window under `.fabricate-app-shell` while all 113 rules in the `.manager-travel-*` family were scoped under `.fabricate-manager`, so converting it would portal to a host that is not there and render the panel entirely unstyled. Neither half survives. Issue 1466 replaced the hard-coded `closest()` on `.fabricate-manager` with `resolveOverlayHost`, whose root set carries `fabricate-app`, and `tests/components/overlay-portal-host-position.test.js` measures a `SearchablePopover` landing at its trigger inside the player window. Issue 1464 re-rooted the primitive’s own family at `.fabricate-picker`, so the 35 `.manager-travel-*` rules under `.fabricate-manager` in `styles/fabricate.css` that remain are the World — Travel screen plus manager callers’ overrides; four of those DO name a class the primitive writes, and all four are anchored on a manager-only container — `.manager-salvage-component-trigger`, `.manager-tool-replacement-card`, `.manager-map-link-picker-cell`, `.manager-recipe-or-popover` — that the player window never renders. AND THE CONVERSION HAPPENED, at issue 1475, which is what turns the withdrawal above from a correction into a demonstration. This bar is `SearchablePopover`’s FIRST PLAYER-WINDOW ADOPTER: until it landed, the only thing depending on either of those two changes was a fixture, and a capability nothing in the product uses is a claim rather than a fact. The picker is now the primitive with `pickerClass=\"actor-bar-picker\"`, `popoverClass=\"actor-bar-popover\"` and `triggerClass=\"actor-bar-trigger\"`, and its panel is PORTALED onto the player window’s ApplicationV2 frame instead of being absolutely positioned inside a 64px bar; `overlay-portal-host-position.test.js` measures that panel against this bar’s own trigger and hit-tests it clear of the bar, which is the acceptance question a DOM assertion cannot ask. The bar’s own `<style>` block fell from thirty-one rules to eighteen at issue 1475, and to 17 scoped rules at issue 1514, which converted the contextual stamina track onto the shared `FillBar`: the track's own rule survives as a WIDTH PIN alone — the primitive declares `flex: 1 1 auto` and a caller is what decides how wide a bar is — and the fill's rule went with the fill. Ten popover-subtree rules were DELETED rather than wrapped in `:global()`, because they named markup the primitive renders now and no repair could have reached it; the seven trigger rules that remain are `:global()` at their original specificity. Measured on the converted markup: had all fourteen dead rules been kept, TWELVE would have compiled with this component’s scoping hash attached, matched nothing and raised no compiler warning at all, and only `.actor-bar-popover` — whose token travels as `popoverClass` — is one `manager-button-scoped-class-reach.test.js` can see. `lint:svelte:warnings` would have named the other two, both of them descendant selectors. That is the silent mode this register keeps recording, measured on the file it was predicted for. AND IT LIVES UNDER `src/ui/svelte/apps/` AS OF ISSUE 1500, which is this row's first sentence acted on rather than a new judgement: a 506-line UNADJUDICATED SCREEN REGION with one caller is not a shared component, and the directory it sat in is the one whose premise is that everything in it works wherever it is mounted. The move changes its ROUTING and nothing else. It matched `BROAD_SIGNAL_PATTERN` through the directory leg before, so it published the representative pair plus the one frame its `BROAD_SIGNAL_CASE_OVERRIDES` entry named; under `apps/` it matches neither leg, so `evidence` is `targeted`, the override is DELETED as configuration that could no longer fire, and the two frames it actually appears in name it in their own `sourceMatches` — `player-actor-picker`, which opens its picker, and `fabricate-app-shell`, which draws it closed. `manager-components-normal` no longer publishes for it, and should not: it is the manager, and it only ever drew this player bar as a side effect of being half of the broad-signal pair."
    },
    {
      "path": "src/ui/svelte/components/DropZone.svelte",
      "library": null,
      "evidence": "broad",
      "callers": [],
      "why": "ZERO callers — dead code. Nothing under `src/` imports it. (`src/ui/svelte/components/ItemDropZone.svelte` is a DIFFERENT FILE: it is the shipped link field and a member of the set. A basename search that does not anchor on the path separator conflates the two and credits this dead component with every one of that file’s importers, which is why `scripts/lib/componentImporters.js` resolves each specifier against the directory that wrote it, compares whole repository-relative paths, and never compares a basename at all.)"
    },
    {
      "path": "src/ui/svelte/components/ImagePathPicker.svelte",
      "library": "<ArtPathPicker>",
      "evidence": "broad",
      "callers": [],
      "why": "ZERO callers — dead code, and the one row where the library and the tree disagree in the interesting direction: `library.html:799` specifies `<ArtPathPicker>`, and a file implementing it ships, and nothing imports it. Specified and built is not the same as shipped."
    },
    {
      "path": "src/ui/svelte/components/ManagerColorPicker.svelte",
      "library": null,
      "evidence": "broad",
      "callers": [
        "src/ui/svelte/apps/manager/EnvironmentsBrowserView.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/manager/EnvironmentsBrowserView.svelte`. The `<TintPicker>` entry is carried by its sibling `ManagerColorPopover.svelte`, which clears the bar and is a member of the set."
    },
    {
      "path": "src/ui/svelte/components/RowDisclosure.svelte",
      "library": "<RowDisclosure>",
      "evidence": "broad",
      "callers": [
        "src/ui/svelte/apps/manager/ComplicationSummaryRow.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/manager/ComplicationSummaryRow.svelte`. Named at `library.html:1016`, so it is specified and shipped and still below the entry bar. It is separately on `SHARED_PRIMITIVES` in `tests/components/mounted-harness-primitive-allowlist.test.js`, cited by construct rather than by line because the line cited here had rotted onto an unrelated entry, for a reason the caller count cannot express: omitting it there CANCELS every test in the mounted suite — `# cancelled N`, never `# fail` — rather than hanging it, which is what that guard's own header states in as many words. That is why that list is not derived from this manifest."
    },
    {
      "path": "src/ui/svelte/apps/manager/recipe-item/RecipeItemLimitsTab.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/manager/RecipeItemEditor.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/manager/RecipeItemEditor.svelte`, and it is a route tab rather than a candidate primitive. The caller recorded here until issue 1446 was `recipe-item/RecipeItemEditorTabs.svelte`, a real sibling that imports `EditorTabs` and does not import this tab at all: the COUNT was right and the FILE was wrong, which is the half of the drift a count check on its own cannot see. It is recorded because issue 1458 adjudicated the two `role=\"listbox\"` SITES inside it (`:531`, `:631`) against `SearchablePopover` and ruled them a DIFFERENT WIDGET, which is a decision a later audit would otherwise re-propose from the same grep. They are typeahead COMBOBOXES: neither has a trigger button at all, the suggestion list hangs off an `<input>` whose `aria-expanded={suggestions.length > 0}` (`:537`, `:637`) is driven by the query rather than by a control, and the input IS the field rather than a filter over a panel. `SearchablePopover` opens a panel FROM a trigger; a combobox has no closed state to open from. `manager-filter-bar-source-contract.test.js` already records the same verdict for the three `.manager-search` comboboxes, from the other side. The file carries 30 scoped rules and a silent-mode trigger (`<i class={expr}>`), so a conversion would have been the highest-risk one in the candidate set as well as the wrongest."
    },
    {
      "path": "src/ui/svelte/apps/crafting/ComponentSourcesBar.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/ActorSelectTopBar.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/ActorSelectTopBar.svelte`. Recorded because issue 1458 adjudicated its editor popover (`:134`, `:138`) against `SearchablePopover` and found TWO independent disqualifiers, either of which is sufficient. (1) IS WITHDRAWN at issue 1472, and is written out rather than deleted because a row that quietly loses an argument is how this register drifted: it said the panel renders in the PLAYER window under `.fabricate-app-shell` while all 113 rules in the `.manager-travel-*` family were scoped under `.fabricate-manager`, so it would portal to a host that is not there and draw completely unstyled. Issue 1466 put `fabricate-app` into `resolveOverlayHost`’s root set and issue 1464 re-rooted the primitive’s family at `.fabricate-picker`; the `apps/ActorSelectTopBar` row carries the measurement. (2) STANDS ENTIRELY ON ITS OWN, and is why this row is still here. It is a MULTI-SELECT checklist rather than a picker: `toggleAvailable` toggles membership, the popover stays open across choices, and `aria-selected` is true on every chosen actor at once, while `SearchablePopover` has a single `value` and closes on `choose`. It also has NO search field at all — the file renders no `<input>` — against a primitive whose panel is built around one. Converting it would take four clicks to add four source actors and would tell assistive technology that one option is selected when four are, so a conversion needs a multi-select mode on the primitive, which is a design question this row does not settle. Its 24 scoped rules — 28 before issue 1514 moved both actor portraits onto `<Avatar shape=\"square\">` and the picker's empty line onto `<EmptyState note>`, deleting the two portrait rules, their two `img` rules and the empty's own, then 23, and 24 again once that issue's review round moved the keyboard focus ring OFF the button the hover dim filters and onto the unfiltered row wrapper, where an accent outline is not composited below the 3:1 focus-indicator floor — carry NO silent-mode trigger — measured, against the file’s own AST — so they would have pruned and warned rather than dying quietly; that is the one at-risk file in the candidate set where the recorded silencing condition does not hold."
    },
    {
      "path": "src/ui/svelte/apps/manager/RollDataExpressionInput.svelte",
      "library": "<ExprInput>",
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/manager/world/WorldModifiersTab.svelte"
      ],
      "why": "DEMOTED at issue 1373, not adjudicated against anything. It cleared the two-caller bar on the Tool check bonus and `WorldModifiersTab`; the bonus stopped authoring a raw expression when the maintainer ruled that a Tool takes its modifiers FROM the world library rather than by typing one, so the picker became `ModifierLibraryRow` and this input lost that caller. The behaviour the original entry credits - cursor insertion and an actor resolver - is unchanged and still unowned elsewhere, so this is a membership fact about caller count, not a judgement that it stopped being well built. Its `sigil` branch has no caller either, since `WorldModifiersTab` passes `sigil={false}`; removing that branch is owed as its own change. One caller: `src/ui/svelte/apps/manager/world/WorldModifiersTab.svelte`."
    },
    {
      "path": "src/ui/svelte/apps/manager/BulkEditSelect.svelte",
      "library": null,
      "evidence": "targeted",
      "callers": [
        "src/ui/svelte/apps/manager/recipes/RecipeBulkEditPanel.svelte"
      ],
      "why": "ONE caller, `src/ui/svelte/apps/manager/recipes/RecipeBulkEditPanel.svelte`, since issue 1371 r16-list. The bulk panel's leave-unchanged select had two (issue 1010) until maintainer ruling M23 rebuilt the system Component Rules bulk panel's category axis as the reference's inline staging inset — a search well over rows carrying `n/N` counts — so the Component Studio stopped rendering a select there. It is still the Recipe Studio's category and check-tier control, and `library.html:666` still specifies `<Select>` as one shell shared with text, number and search; it re-enters the set at a second caller, which is a decision for the change that adds one rather than a row this file can keep true today."
    }
  ],
  "ruledOut": [
    {
      "name": "<MemberRow>",
      "verdict": "composition",
      "replacement": "ListRow + `leading`",
      "why": "List row geometry with a portrait in the leading cell — structurally identical to the inline-title row already declined."
    },
    {
      "name": "<ActorPicker>",
      "verdict": "composition",
      "replacement": "trigger + SearchPopover",
      "why": "A pill trigger that opens the popover — which already counted it as one of its four callers."
    },
    {
      "name": "<AddButton>",
      "verdict": "composition",
      "replacement": "Button role=dashed",
      "why": "Dashed is a role on the button that owns the meaning. \"Sits in flow with its collection\" is a usage rule, not a boundary."
    },
    {
      "name": "<RailCard>",
      "verdict": "composition",
      "replacement": "Well + Kicker + Button",
      "why": "A composition that also smuggled back the 34px button size the set retires by name."
    },
    {
      "name": "<FeatureCard>",
      "verdict": "composition",
      "replacement": "OptionCards, non-interactive",
      "why": "Two ABSENT props on an identical shape is a props difference, not a distinction."
    },
    {
      "name": "<BoundsInput>",
      "verdict": "composition",
      "replacement": "two Steppers",
      "why": "\"Two steppers with a rule between\", by its own spec."
    },
    {
      "name": "<CurrencyInput>",
      "verdict": "composition",
      "replacement": "Stepper + Select",
      "why": "A composition, and wrong about the domain: it assumed a flat world-scoped coin ladder where the model defines a per-system acyclic denomination DAG."
    },
    {
      "name": "<PremiumPanel>",
      "verdict": "out-of-scope",
      "replacement": null,
      "why": "A screen, not a primitive. Its one original claim is copy — a product decision — and binding copy to a component makes the offer untranslatable against a codebase where every primitive takes pre-localized strings. Canon also forbids any premium signal in the player window."
    },
    {
      "name": "<Toast>",
      "verdict": "foundry-owns",
      "replacement": "ui.notifications",
      "why": "Foundry owns transient feedback and renders its own toasts over the module window regardless."
    },
    {
      "name": "Destructive panel",
      "verdict": "foundry-owns",
      "replacement": "confirmDialog",
      "why": "Canon makes the dialog the default for manager confirmations; the arm is the carve-out."
    }
  ]
}
